# Vio Live 更新记录

## 0.48.0｜2026-08-14

### S4-Live 首次真实供应商试聊归档

- 正式验收结论为 PASS：在 `C:\VioS4\first-001` 可销毁短路径沙箱和 `disposable_test / promotionAllowed=false` 身份下，使用 Alibaba Cloud Model Studio OpenAI-compatible Provider 与 `qwen-flash-2025-07-28` 完成首次真实试聊。
- Provider execution 恰好一次并报告 177 input、9 output、186 total Token；CapabilityResult `SUCCEEDED` 首次回传 HTTP 200，result outbox completed，没有第二次 execution/result。
- Conversation Turn completed，最终主体表达“Vio首次真实试聊连接成功。”只来自 Engine `FirstRoundSuccessResult`。Engine `changed=false / revision=0`，没有内部 Event、StateMutation、越权状态写入、incident、outcome_unknown 或重复 Message。
- Vio 费用账本保持 `cost_status=not_reported`；验收时供应商概览显示 ¥0，但账单可能延迟，本版本不声明永久最终费用为零。
- 三个服务均已停止，5173/8787/8766 无监听；官方 cleanup 整根删除沙箱且未命中 protected/repository path，最终路径不存在。双方仓库保持干净，没有正式数据或仓库污染。

### 当前边界

- 本版本只归档 Markdown，没有再次调用真实供应商或产生费用，没有修改代码、测试、迁移、Schema、配置、前端或 Continuity Engine。
- Windows Engine 持久化路径预算事故与历史旧沙箱 cleanup 阻断继续作为先前版本的真实记录保留。
- 通用正式身份/Binding、生产认证、多租户、加密密钥存储、备份和部署仍未完成；当前只允许本机受控使用，不得公开部署。

## 0.47.0｜2026-08-14

- 修复路径门禁上线前的超预算隔离沙箱无法使用官方 cleanup 的问题。
- cleanup 仅在唯一错误为 `engine_persistence_path_budget_exceeded` 时启用私有兼容校验，并重新执行其余全部严格安全检查；plan 只读，apply 双确认，只能删除完整 canonical root。
- create 与 doctor 保持原门禁：新超预算路径零残留拒绝，旧超预算 Manifest 继续显示 unsafe。真实旧沙箱只运行 plan，未执行清理。
- 沙箱 18/18、L1 24/24、V1–V4 101/101、V5 19/19、S4/V5 shared 7/7 与 6/6、后端 220/220、前端 23/23、类型检查和生产构建通过；零真实调用、零费用。

## 0.46.0｜2026-08-14

- 新增 S4-Live Windows Engine 持久化路径预算门禁：按 WakeSession 最终文件与 Python 原子临时文件的代表性最坏路径计算，240 字符以上在创建前 fail closed，稳定原因码为 `engine_persistence_path_budget_exceeded`。
- 严格 manifest、sandbox inspection 与 readiness doctor 使用同一规则；已有超限沙箱不会被判为 ready，创建拒绝不会留下半成品。
- 推荐每轮验收使用全新仓库外短路径 `C:\VioS4\first-001`；不自动截断、搬移或通过 `subst`、junction、symlink/reparse point 绕过。
- 回归通过沙箱 13/13、L1 24/24、V1–V4 101/101、V5 19/19、S4/V5 shared 7/7 与 6/6、后端 215/215、前端 23/23、类型检查和生产构建。没有真实供应商调用、密钥或费用，也未执行新一轮真实试聊。

## 0.45.0｜2026-08-14

- 新增 S4-Live 可销毁验收沙箱：严格 manifest、测试专用身份、同根路径 doctor 门禁与双确认整根清理。
- 固定 v1.1 身份禁止晋升为正式主体；清理不支持逐行、逐 JSON 或 revision 回滚。

## 0.44.0｜2026-08-12

### 新增

- 新增 L1 `export:local-chat-binding`、`prepare:live-chat` 和 `doctor:live-chat` 稳定命令。
- Binding 导出直接复用正式 fixture 与 hash；plan/doctor 严格只读，apply 需要外部 Provider 与潜在费用双确认。
- 通过既有领域服务幂等准备唯一 `openai_compatible` Provider、chat Model/路由、manage/execute Permission、有限 `block` Token Budget，以及经高风险 Confirmation/AuditLog 保护的 `env:VIO_MODEL_API_KEY_LIVE` credential reference。
- 补充 Windows PowerShell 三端启动与首次真实试聊步骤。
- L1 专项 18/18、V1–V4 101/101、V5 19/19、S4 7/7、V5 shared 6/6、后端全量 196/196、前端 19/19、类型检查与生产构建通过。

### 边界

- 本阶段没有执行真实供应商 live smoke、使用真实 Key、访问公网或产生费用。
- F1 已完成；通用 Binding、生产认证、多租户、部署、MCP、Tool、设备和附件仍未完成。

## 0.43.0｜2026-08-12

### F1 对话页真实接线

- `ConversationPage` 已接入固定本地 Profile 的 Vio V5 Conversation Turn 与 Message 公共 API，不再读取静态对话消息。
- 新增严格前端响应校验、真实消息时间与顺序、发送中/等待确认/预算/重试/结果未知/失败状态、显式恢复操作和 `aria-live` 状态反馈。
- 新增单一 `sessionStorage` 待恢复事实：超时、断线或响应丢失后保留原幂等键与必要正文；取得 `turnId` 后收缩保存内容，稳定终态后清除。
- 禁用尚未接入的附件、消息编辑/重生成/删除/分支、会话切换以及模型/上下文控制；记忆、状态与工具区域改为诚实的后端/Engine 边界说明。

### 验证与边界

- 新增 Vitest、Testing Library 与 jsdom 前端测试配置；F1 自动化测试 19/19、类型检查、生产构建、后端全量 178/178 与正式 V5 shared 6/6 通过。受控浏览器已完成真实确认后回复链路及桌面/320px 窄屏回归。
- 仅完成固定本地试聊前端接线；未修改后端、Continuity Engine、迁移或 v1.1，未调用真实供应商，也未开始通用身份/Binding、生产认证、多租户或部署。

## 0.42.0｜2026-08-12

### Vio V5 固定本地公共对话轮次

- 新增固定本地 Profile 的 Conversation Turn create/get/resumption 公共 API，以开发期用户头、严格 `Idempotency-Key`、复合归属和显式确认/重试保护入口。
- 新增迁移 `022_create_continuity_conversation_turn_ledger.sql`，关联用户 Message、V1 请求、V3/V4/V2 处理结果和唯一主体 Message，并支持各持久化窗口的重启恢复。
- 最终主体 Message 只使用 V2 保存的 Engine response；Provider 原始候选不能直接落入对话。FAILED_RETRYABLE、UNKNOWN、预算和安全确认继续遵守 V4 既有状态机。
- 新增 `pnpm run prepare:local-chat`，幂等准备固定本地用户、助手、Engine Binding 与 Conversation；冲突 fail closed，零网络。
- V5 专项 17/17、V5 正式本机 shared test 6/6、V1–V4 101/101、后端全量 176/176 通过。测试零公网、零真实密钥、零真实费用；真实供应商 live smoke 与 F1 前端接线仍未开始。

## 0.41.1｜2026-08-11

### S4 Vio V4 × Engine E5-A Capability 双仓共享验收

- 新增独立 S4 shared test 与运行命令，以正式 Engine capability 服务、Vio V1–V4 和随机 loopback Provider 验证真实双进程 HTTP Capability 闭环。
- 覆盖成功、精确重放、双方重启、429 显式批准重试、400 终止、UNKNOWN fail closed、回传响应丢失和身份/冲突边界；S4 7/7 通过。
- 回归通过：S2/S3 15/15、V1–V4 101/101、Vio 159/159、Engine E5 54/54、Engine 355/355。
- 未修改生产代码、前端、Engine、旧迁移或 v1.1；测试零公网、零真实密钥、零真实费用。真实供应商 live smoke、V5 和 F1 仍未开始。

### Vio V4 Capability 合同返工

- 修正 E5-A 外层合同：`capability_required` / `capability_failed` 使用 `continuity-capability/v1`，completed 保持 `continuity-integration/v1.1`；新增独立 Engine 字面量回归。
- 修正迁移 `021` 的单 attempt 限制：允许多个不可变 execution/result/usage/outbox attempt，只允许一个成功；成功、终止和 UNKNOWN 后禁止新执行。
- `FAILED_RETRYABLE` 被 Engine 接受后进入 `waiting_retry`，只有内部明确批准并重新通过路由、权限、安全、预算和 deadline 后才能产生新 Provider 调用；`UNKNOWN` 被接受后保持 fail closed。
- 将外部模型调用安全语义固定为 `privacy_access_request + private_record`，并区分 HTTP 同 Result 重传与新 Provider 执行。
- V4 专项 46/46、V1–V4 组合 101/101 通过；自动测试继续保持零公网、零真实密钥、零真实费用。S4、V5、F1 未开始。

## 0.41.0｜2026-08-10

### Vio V4 Continuity Capability

- 新增 Engine E5-A 三份严格 Capability Schema 本地副本和验证，完成 V1 归属反查、Permission/Security/Confirmation/Token Budget 门控、Model Router 选择与 `openai_compatible` Provider HTTP 执行。
- 新增迁移 `021_create_continuity_capability_execution_ledger.sql`，持久化 CapabilityRequest、决策快照、模型执行、可信 usage/费用事实、完整 CapabilityResult、回传 outbox/attempt 和 incident/quarantine。
- API Provider 新增高风险确认保护的 `env:VIO_MODEL_API_KEY_*` secretRef 绑定/轮换；数据库和公开响应不保存或泄露密钥原值。
- CapabilityResult 先持久化再回传 Engine；结果未知时 query-first，重启只重发相同 Result，禁止不确定情况下再次调用可能收费的 Provider。Engine completed 继续复用 V2 result/projection/receipt。

### 验证与边界

- V1–V4 指定组合 93/93、后端全量 151/151 通过；迁移 fresh/upgrade/失败回滚和随机 loopback Provider HTTP 通过。
- 自动测试零公网、零真实密钥、零真实费用；真实供应商 live smoke 未执行。前端、Engine、v1.1 和旧迁移未修改；S4、V5、F1 未开始。

## 0.40.0｜2026-08-09

### 正式本机 Continuity Engine 连接验收档案

- 记录 Engine E4 正式本机 HTTP Integration Adapter、Vio V3 transport/Delivery/outbox/attempt 与迁移 020 已完成；V1 → V3 → Engine E4 → V2 已通过真实 loopback TCP/HTTP。
- 记录 S2 正常共享连接和 S3 响应丢失、Engine/Vio/双方崩溃重启、查询恢复及冲突隔离均通过。S2/S3 shared tests 15/15，Vio 64/64 与全量 113/113，Engine crash-recovery 15/15、E4 67/67、全量 301/301。
- 保留 S3 首次发现 Engine `reserved` 非终态恢复缺陷的历史：Vio 处理正确，Engine 在 `189441f9bad2a34119b4ef10365a4385ed0949cc` 定点修复后原失败场景及全部 S3 场景通过。
- 同步根/后端 README、API 与各索引、路线图、ADR 当前实现状态、v1.1/14a 当前入口和迁移 020 数据关系；不改变已固定的架构、机器契约或历史记录。

### 当前边界

- 本版本只有工程档案变化，没有修改前端、后端代码、测试、迁移、配置或 Continuity Engine。
- 正式本机连接完成不表示产品已经可用；真实模型/Capability、Vio 公共对话 API、前端真实回复、MCP/Tool/设备、生产认证、多租户与部署仍未完成。
- 下一阶段：共同确定真实模型/Capability、Vio 公共对话 API 与现有前端接线的最短施工顺序。

## 0.39.0｜2026-08-08

### Vio V3 正式本机交付基础

- 新增默认关闭的 Continuity Engine E4 本机 HTTP/JSON transport，固定精确 POST/GET/health 路径、Bearer service token、connect/response timeout、严格 JSON/UTF-8/Content-Type 和 1 MiB 响应上限。
- 新增迁移 `020_create_continuity_http_delivery_outbox.sql`，保存六态 delivery outbox 与逐次 attempt；不复制 V1 原请求、V2 Engine 结果、token、消息正文或私密状态。
- 新增 Delivery Service，从 V1 账本读取并重验首次请求，结果委托 V2；POST 结果未知时先查询，completed 直接恢复，recovery_required/not_found 才重投同一请求，查询失败保持 outcome_unknown，hash/operation 漂移进入隔离。
- 应用显式启用时才创建正式 transport；Engine 不可达不阻止 Vio 启动，`/health` 仅返回 `disabled`、`ready` 或 `degraded`。test-only fixture/JSONL bridge 仍不进入产品装配。

### 验证与仍未完成

- V3 专项 18/18、RFC 8785/Engine E4 9/9、V1 8/8、V2 29/29、后端全量 113/113 通过；`001→020`、`018→020`、`019→020` 迁移与约束、事务和 SQLite 重启恢复通过。
- RFC 跨语言测试只将 Engine SHA 断言更新为 E4 `c5ebbf9`，没有修改机器契约或 hashing 实现。
- 未修改前端、Continuity Engine、迁移 018/019、legacy SubjectState 或公共对话 API，未接真实模型、Capability、MCP、Tool 或设备。双方 S2/S3 正式本机共享验收、真实模型、对话 API 串接和前端真实回复仍未开始。

## 0.38.0｜2026-08-02

### 第一轮双方共享验收

- 新增仅测试使用的 JSONL Runner transport 和独立 `test:continuity-shared` 命令；显式读取 `CONTINUITY_ENGINE_REPO`，以 Engine `7a32a99` 为 cwd 启动真实 E3 Runner，并只使用受控临时数据。
- 从 Vio V1 SQLite 读取首次持久化请求，由 Vio V2 保存 Engine 真实 success/error、subject response 和 stateProjection；没有静态请求或 V2 fixture 冒充共享主流程。
- 真实 A/B/C 场景通过：`hello` 初始化 revision 0 head；`remember continuity test focus` 经 Action Gate/Evolution 推进至 revision 1；第三条消息保持 revision 1。
- 同进程和双方重启幂等通过；Engine 内部更新始终只有一条，Vio revision 1 投影只有一份，完整 operation/response/projection 和 completedAt 精确重放。
- 四类固定错误通过；schema/binding/revision 错误不产生投影，幂等键复用不能覆盖首次 success，只保存隔离 incident。

### 验证与仍未实现

- 共享专项 6/6、Vio V1/RFC 17/17、Vio V2 29/29、迁移 `001→019`/`018→019` 定点 1/1、后端全量 95/95、Engine Runner 16/16、Engine 全量 219/219 通过。
- RFC 8785 跨语言测试的旧 Engine SHA 断言从 `c732f35` 定点更新为正式 Runner 基线 `7a32a99`；双方规范化、UTF-8 和 SHA-256 结果未变化。
- 本次 test-only 端到端共享验收通过不表示产品连接。未新增网络、公共 API 或生产 Integration Adapter，未修改前端/Engine/legacy state_update，未接真实模型、Capability、MCP、Tool 或设备；前端尚不能通过该链路试用。

## 0.37.0｜2026-08-02

### Vio V2 新增

- 新增与 Engine `c732f35` 完全一致的第一轮 success/error envelope 严格白名单验证器、四类固定错误语义和 stateProjection/hash 规则。
- 新增默认未配置、仅允许显式 test-only fixture 的 transport seam；始终发送 Vio V1 首次持久化请求，不重建逻辑输入。
- 新增迁移 `019_create_continuity_v2_result_and_projection_ledger.sql`，保存不可覆盖结果、独立投影版本/回执/当前指针及非敏感 reconciling/quarantine 事件。
- 新增分阶段事务恢复：结果已保存、投影已保存、指针已推进三个故障点均可跨 SQLite 重启继续，且不重复调用 fixture。
- 首次合法 `changed=false / revision 0` 原子初始化已确认投影 head，但不推进 revision；已有 head 时 `changed=false` 不改变指针。`changed=true` 仅在 hash、唯一性、revision 和 CAS 全部通过后推进一次，旧/跳号 revision 和关联冲突不会自动修复。

### 验证与边界

- Vio V2 专项 29/29、Vio V1 + RFC 8785 回归 17/17、迁移相关套件 56/56、后端全量 95/95 通过。
- 已验证迁移 `019` 全新安装和 `018 → 019` 升级，约束、触发器、事务回滚、外键和真实临时 SQLite 重启恢复正常。
- Engine E1/E2/E3 基线仍为 `c732f35`，Vio V1 基线仍为 `c1e1336`。未修改 Engine、前端、legacy `state_updates`、HTTP 路由或软件运行版本。
- 本阶段没有调用 Engine、网络、Python 子进程、模型、MCP、Tool 或设备。双方共享测试和实际连接仍未开始；下一阶段是双方共享验收。

## 0.36.0｜2026-08-02

### Vio V1 新增

- 新增第一轮三份 Draft 2020-12 严格机器契约 Schema、本地 registry/validator、RFC 8785 规范化与 SHA-256 hash。
- 新增固定 SubjectBinding 测试装载，以及经 Vio User/assistant/Conversation/MessageVersion/Event 归属与来源验证的逻辑请求构造。
- 新增迁移 `018_create_continuity_v1_contract_foundation.sql`，跨重启保存固定 Binding、首次 requestId/createdAt/requestHash、revision、来源引用和规范化逻辑请求。

### 验证

- 三项正式 conformance hash 与 v1.1/Engine 完全一致。
- Vio V1 专项测试 8/8 通过；后端全量测试 57/57 通过，0 失败。

### 当前边界

- Engine E1/E2/E3 已在引擎提交 `c732f35` 完成；Vio V1 与引擎仍未连接。
- 本阶段不保存伪造的 operation、response 或 stateProjection，不创建引擎 Event/StateMutation，不推进 Vio SubjectState。
- 未修改前端、Continuity Engine、软件版本或现有状态接口；未接模型/MCP/Tool/设备/网络。
- Vio 投影接收、双方共享测试、网络连接和生产 Integration Adapter 仍未实现，留待 Vio V2/后续阶段。

## 0.35.2｜2026-07-30

### 工程档案最终确认

- Vio 与 Continuity Engine 双方工程档案同步完成。
- 最终只读核对曾发现 Continuity Engine 的旧直连拓扑和无条件 `StateMutation` 两处旧描述；Continuity Engine 已完成定点修正。
- Vio 完成九处现行阶段状态同步，并在双方再次只读核对后确认架构、Action Gate、SubjectState 唯一权威、机器契约、历史记录和运行时未实现边界一致。
- 正式结论：**双方档案一致，可以制定第一轮最小连接施工提示词。**

### 当前边界

- 第一轮最小连接施工提示词尚未共同制定；第一轮代码施工、共享测试和运行时连接仍未开始。
- 本次只更新工程文档，没有修改前端、后端业务代码、测试、数据库迁移、配置或 Continuity Engine，也没有接入任何外部能力。

## 0.35.1｜2026-07-30

### 文档状态同步

- Continuity Engine 已通过《Engine Contract Final Read-Only Short Confirmation v1》正式接受 `Continuity Integration Contract v1.1`。
- 更新 v1.1、14a、README/导航、API/架构文档、两级 ADR、两级工程日志和路线图；新增 14c 保存最终接受的只读历史证据。
- 保留第二次审核及此前“暂不接受”的历史记录，明确它们不再代表当前契约状态。

### 已完成与未实现

- 已完成长期架构、SubjectState 权威、PlatformObservation/内部 Event 边界和第一轮 Schema、SubjectBinding、hash、幂等、revision、错误、投影、ContractTestAdapter 入口语义对齐。
- 运行时 PlatformObservation/validator、SubjectBinding/hash 验证、ContractTestAdapter、双方持久化幂等账本、Vio 投影接收器、实际连接和第一轮共享测试仍未实现。
- 当前只允许双方同步档案；档案核对一致后，下一步才是共同制定第一轮施工提示词。

### 边界

- 后端运行版本保持 `0.18.0`；未修改代码、测试或数据库迁移，未接模型/MCP/Tool/设备或 continuity-engine，未开始施工，未提交或推送。

## 0.35.0｜2026-07-29

### 新增

- 新增 `Continuity Integration Contract v1`，设计 Vio 与 continuity-engine 的身份、平台事实包、Observation、Context、SubjectState 投影、摘要、私域、幂等、故障、安全、隔离和 Token 边界。
- 新增 BE-ADR-035 / 项目 ADR-032，决定 continuity-engine 是 SubjectState 唯一权威源，Vio 只保存受控投影、快照、缓存和审计记录。

### 调整

- 更新 README、总体架构、连续性、API/事件契约和后端 API 文档，明确 Vio Context 只是平台事实来源，最终认知 Context 由引擎组织。
- 将接口严格区分为“已实现”“已实现但需收口”“拟新增”和“待引擎确认”；现有开发 `state_update` 不再被视为未来权威状态接口。
- 补充前端只连接 Vio、数据库不合并、Event 只转 Observation、私域最小披露和引擎不可用时不得自行推进状态的规则。

### 验证与边界

- `cd backend && pnpm test`：49/49 项通过，0 项失败。
- `git diff --check`：通过。
- 本次只完成架构和连接契约设计；后端运行版本仍为 `0.18.0`，尚未完成 continuity-engine 接入。
- 未修改前端 `src` 或 continuity-engine，未编写连接代码，未新增数据库迁移，未接真实模型、MCP、Tool 或设备，也未新增业务模块。

## 0.34.0｜2026-07-28

### 新增

- 新增版本化 Export Schema，支持 `full`、`selected`、`migration` 和十二类稳定数据范围。
- 新增导出记录与归属、必需字段、外键完整性预检，不返回任何业务正文。
- 新增 `data_export` Permission/Security 资源和高风险逐次确认导出准备。
- 新增机器人/其他载体未配置迁移契约与 Schema 兼容准备接口。

### 调整

- 后端版本更新为 `0.18.0`，开发 SQLite 新增 `017_create_data_export_and_migration_foundation.sql`。
- 导出记录固定 `payload=not_generated`、`file=not_created`、`externalStorage=not_connected`、`migration=not_executed`。
- Export Schema、完整性、安全资格和未来执行明确分层；`ready` 不代表真实导出或迁移完成。

### 验证

- `pnpm test`：49 项通过，0 项失败。
- 验证旧安全历史迁移、十二类范围、缺失字段阻止、确认消费、记录持久化、复合隔离和未执行载体契约。

### 当前限制

- 不生成文件、下载链接或业务数据包，不连接外部存储、机器人或其他载体。
- 不执行备份、恢复或真实迁移；前端 `src` 未修改。

## 0.33.0｜2026-07-28

### 新增

- 新增 Wake Framework，支持 `voice`、`desktop`、`schedule`、`event` 规则、启停、触发条件与应用内授权。
- 新增主动提示规则、四级消息优先级、Event 触发与固定未投递的准备记录。
- 新增每日/会话 Token Budget、显式 Token 使用账本和 `block` / `require_confirmation` / `defer` 超额策略。
- 新增 AI 后台 `idle` / `active` 状态、后台开关、频率限制、允许 Wake 类型与安静时段。

### 调整

- 后端版本更新为 `0.17.0`，开发 SQLite 新增 `016_create_proactive_interaction_and_token_controls.sql`。
- Permission、Security Policy、Confirmation 与 AuditLog 增加 `proactive_interaction` 资源类型；需要确认的主动提示/Token 超额强制高风险逐次确认。
- Event 增加 Wake 配置/准备、提示准备、Token 预算/使用和后台策略六类最小事件。
- Token 使用记录固定标记调用方显式输入、平台未调用模型、未计费；预算检查不写入或预留 Token。

### 验证

- `pnpm test`：46 项通过，0 项失败。
- 验证迁移保留、外键、Wake 授权/后台限制、提示优先级与确认、Token 日/会话累计、超额策略和跨用户/主体隔离。

### 当前限制

- 不申请麦克风权限，不监听音频，不连接系统级唤醒或后台调度器。
- 不生成或投递提示正文，不调用真实模型、continuity-engine 或外部 API；前端 `src` 未修改。

## 0.32.0｜2026-07-28

### 新增

- 新增每用户唯一的 User Space 数据结构，保存开发期身份模式、空间状态和当前助手指针。
- 新增助手列表、当前助手读取/切换，以及用户、AI、设备、生活、事件五类数据访问边界 API。
- 新增 13 种预定义资源复合归属查询；敏感资源在所有权命中后继续进入 Permission 与 Security Policy。

### 调整

- 后端版本更新为 `0.16.0`，开发 SQLite 新增 `015_create_user_spaces_and_data_isolation.sql`。
- User 创建与 User Space 建立原子提交；首个 Subject 自动成为当前助手，既有用户迁移按最早活动 Subject 稳定回填。
- 当前助手只作为 User Space 指针，不修改或合并 Assistant Global Settings、Assistant Private Space、SubjectState、对话、事件或生活数据。
- 数据访问检查固定返回未执行状态；`development_unverified` 明确不是登录或身份凭证。

### 验证

- `pnpm test`：43 项通过，0 项失败。
- 验证旧库升级、外键完整性、空间/当前助手持久化、跨用户切换拒绝、多助手设定与状态隔离，以及五类数据复合过滤。
- 验证所有权检查优先于 Permission/Security Policy，默认拒绝、高风险确认和策略收紧保持有效。

### 当前限制

- 不接入真实登录、第三方账号、生产数据库行级安全或租户分区；当前开发路由不得公开部署。
- 不开发语音、模型、continuity-engine 或外部服务；前端 `src` 未修改。

## 0.31.0｜2026-07-28

### 新增

- 新增生活管理数据层：管账记录、预算与提醒规则、四类月历记录、身体指标与目标、本地记忆。
- 新增分类统计、月度汇总、身体趋势和本地记忆 Context 投影接口。
- 新增 `life_event_created`、`budget_changed`、`health_record_updated` Event；既有 `life_record_created` 保留兼容。

### 调整

- 后端版本更新为 `0.15.0`，开发 SQLite 新增 `014_create_life_management_foundation.sql`。
- Permission、Security Policy、Confirmation 与 AuditLog 增加 `life_data` 资源类型；财务、月历、身体和本地记忆按主体使用固定资源范围。
- 金额按最小货币单位整数保存并以两位小数字符串返回，避免二进制浮点累计；月度统计使用 UTC 月份。
- 生活 Event 只保存模块、记录 ID 和变化类型，不复制金额、身体数值、亲密内容、备注或记忆正文。

### 验证

- `pnpm test`：40 项通过，0 项失败。
- 验证收入/支出、预算、分类统计、月度汇总、四类月历、提醒、身体目标/趋势、显式 AI 建议字段、本地记忆标记和 Context 投影。
- 验证 Permission → Security Policy → Confirmation、策略拒绝、跨用户/主体隔离、持久化、事件脱敏、`013` 旧数据升级保留和外键完整性。

### 当前限制

- 不连接真实支付、银行、穿戴设备或健康服务，不提供医疗或财务专业结论。
- AI 建议仅保存调用方显式文本，不调用真实模型；本地记忆投影不自动进入通用 Context。
- 未实现生活记录删除、真实导出文件、设备同步或页面数据迁移；前端 `src` 未修改。

## 0.30.0｜2026-07-28

### 新增

- 新增 AI Private Space、五类私域内容和不可变版本数据结构，按用户与助手复合归属隔离。
- 新增私域 Space、Content、版本历史、独立 Context 投影与 Export Manifest API。
- 新增 `private_space_created`、`private_memory_updated`、`private_state_changed` Event。

### 调整

- 后端版本更新为 `0.14.0`，开发 SQLite 新增 `013_create_ai_private_spaces.sql`。
- User Space 与 Assistant Private Space 改为明确独立存储；通用 Conversation Context 不自动读取私域。
- 私域读取、写入、状态与导出准备统一接入 `private_domain` Permission → Security Policy → Confirmation。
- Event、AuditLog 和 Export Manifest 均不复制私域正文；导出当前只保留结构与版本元数据。

### 验证

- `pnpm test`：37 项通过，0 项失败。
- 验证五类内容、陈旧版本保护、不可变历史、重启持久化、用户/助手/Space 隔离、权限与策略拒绝、高风险确认、事件脱敏、Context 投影、导出清单及外键完整性。
- 前端 `src` 无修改；没有模型、外部 API、机器人或真实设备调用。

### 当前限制

- 私域只保存调用方显式输入，不实现真实意识、自主行为或 AI 自动写入。
- 未实现目录申请/开放结果、删除、生产加密、真实导出文件或 continuity-engine 生成。
- 未连接机器人、真实设备、真实模型、MCP、Tool 或第三方服务；当前路由仍无真实认证，不得公开部署。

## 0.29.0｜2026-07-28

### 新增

- 新增 Security Policy 数据结构、仓储、服务和用户范围 CRUD API，支持 `always_allow`、`session_allow`、`always_confirm`、`deny`、`deny_without_confirm`。
- 新增用户安全偏好读取/更新，支持默认安全等级、高风险操作策略、低/中风险自动确认范围和禁止范围。
- 新增确认原因、风险说明、用户选择、策略版本与开发期安全会话记录，以及确认后的 30 分钟精确会话授权。
- 新增 `permission_created`、`permission_revoked`、`confirmation_required` Event；保留 `permission_changed` 表达修改和单次消费。

### 调整

- 后端版本更新为 `0.13.0`，开发 SQLite 新增 `012_create_custom_security_policies.sql`。
- Security 流程固定为 Permission → Security Policy → Confirmation；策略与偏好只能收紧 Permission，高/极高风险始终逐次确认。
- 策略、偏好、确认和最终安全结果写入最小 AuditLog，所有执行结果继续固定 `not_executed`。
- SQLite 迁移运行器支持显式父表重建指令，并在提交前强制外键完整性检查。
- README、API、后端与项目 ADR、开发日志、数据库、关系图、安全/事件规划和路线状态同步到本阶段。

### 验证

- `pnpm test`：34 项通过，0 项失败。
- 验证五种策略规则、用户偏好、CRUD、软删除、重启持久化、跨用户隔离和最小审计。
- 验证 Permission → Policy → Confirmation 顺序、禁止范围、自动确认、高风险平台底线和 Permission 生命周期 Event。
- 验证 `session_allow` 仅在确认后建立、精确绑定作用域、30 分钟失效、策略版本失配及高风险不建立授权。
- 验证 `011` 旧库在存在 Event/AuditLog 子表引用时升级成功且无外键错误；现有后端回归全部通过。

### 当前限制

- `securitySessionId` 只是开发期不透明关联标识，不是登录会话、Cookie 或认证凭证。
- 未执行真实 Tool、设备、支付或 AI 私域操作，未连接外部服务或真实模型。
- 前端 `src`、页面和 mock 未修改；当前接口仍无真实认证，不得公开部署。

## 0.28.0｜2026-07-28

### 新增

- 新增手机、手表、空调、扫地机器人、洗衣机、摄像头和通用家电七类 Device Registry。
- 新增查看状态、开关、调节参数和获取数据四种 Device Capability，以及对应 Device Permission action。
- 新增 Xiaomi、Midea、Apple、Android 与 Generic 未来 Adapter 契约描述，当前全部未实现。
- 新增设备授权、极高风险操作准备、`device_changed` 事件和主体范围 DeviceOperationLog API。

### 调整

- 后端版本更新为 `0.12.0`，开发 SQLite 新增 `011_create_device_adaptation_foundation.sql`。
- Device 创建默认停用；启停只改变平台注册状态，连接/状态/控制固定为 `not_connected` / `not_observed` / `not_implemented`。
- 设备能力复用现有 `device` Permission；设备操作固定通过 `Security(device_control)` 和每次 Confirmation。
- Device Event 区分连接元数据登记、授权变化、注册状态变化和操作请求，均不冒充真实设备事件。
- README、API、后端与项目 ADR、开发日志、设备/安全/事件规划和路线状态同步到本阶段。

### 验证

- `pnpm test`：30 项通过，0 项失败。
- 验证七类设备、四种能力、五个 Adapter 描述、默认停用、筛选、重启持久化和品牌分派。
- 验证 Device Permission、极高风险确认、确认消费、权限拒绝、Event/AuditLog 关联和未执行日志。
- 验证控制参数、非法类型/能力、跨用户/主体访问被拒绝，以及设备、事件、审计和日志事务回滚。
- 验证现有账号、对话、Context、模型路由、扩展能力、Permission 与 Security 回归测试继续通过。

### 当前限制

- 未连接、发现、配对或控制真实手机、家电、摄像头和穿戴设备。
- 未接入 Xiaomi/Midea/Apple/Android SDK、厂商 API、凭据或推送通道。
- 未读取或保存位置、影像、健康数据、真实状态、设备控制参数或第三方响应。
- 当前接口仍无真实认证，开发数据库不得公开部署。

## 0.27.0｜2026-07-28

### 新增

- 新增 Tool、MCP、Skill、Plugin 四类用户范围 Registry 数据结构、仓储、服务和 API。
- 新增主体范围 Capability Service，统一返回能力列表、分类、注册状态、Permission 状态和 Tool 最近使用记录。
- 新增 Tool 执行准备接口，串联注册状态、Permission、Security 和 Confirmation。
- 新增 Tool 使用记录，保存调用时间、主体、权限/安全决策、准备结果、固定未执行摘要、零外部消耗与关联 AuditLog。

### 调整

- 后端版本更新为 `0.11.0`，开发 SQLite 新增 `010_create_capability_registries.sql`。
- 四类注册项创建时默认停用；`enabled` 只表示可进入能力选择，不代表已连接、已安装或可执行。
- Tool、MCP 与 Skill 复用现有 Permission 的对应资源类型；Capability Permission 查询不消费 `allow_once`。
- Plugin 当前保持纯注册元数据，不扩张 Permission 资源类型；安装、加载和供应链治理留待后续独立设计。
- README、API、后端与项目 ADR、开发日志、扩展能力规划和路线状态同步到本阶段。

### 验证

- `pnpm test`：27 项通过，0 项失败。
- 验证四类注册、默认停用、状态更新/筛选、重启持久化、用户内唯一和跨用户隔离。
- 验证 Capability 分类与权限投影、Tool 最近使用、Permission 拒绝、中风险确认和确认后准备完成。
- 验证执行载荷和含凭据 MCP 地址被拒绝；所有 Tool 记录均为 `not_executed`，MCP 未连接，Plugin 未安装。
- 验证现有账号、对话、Context、SubjectState、Global Settings、Event、Model Routing、Permission 与 Security 回归测试继续通过。

### 当前限制

- 未连接真实 MCP 服务，没有协议客户端、握手、能力发现或网络请求。
- 未安装、更新、卸载或加载真实插件，也没有 Skill/Tool 执行器。
- 未调用手机权限、设备、家电、第三方服务、真实模型或 continuity-engine。
- 当前接口仍无真实认证，开发数据库不得公开部署。

## 0.26.0｜2026-07-28

### 新增

- 新增 Provider 接口格式/测试状态、Model 费用说明/测试状态和八类能力标签。
- 新增六类任务的 `model_routing_rules` 数据结构、仓储、服务及创建/查询/更新 API。
- 新增默认模型选择、显式备用模型选择和无启用规则时的稳定目录回退。
- 新增拒绝写入的 API Key 安全存储端口占位，以及模型路由配置与隔离测试。

### 调整

- 后端版本更新为 `0.10.0`，开发 SQLite 新增 `009_expand_model_routing_configuration.sql`。
- Provider 默认接口格式按类型映射；Model 能力扩展为 `chat`、`long_text`、`vision`、`image`、`video`、`audio`、`search`、`embedding`。
- Router 的可路由任务限定为聊天、长文本、图片、视频、语音和搜索，所有选择响应显式标记模型与外部 API 未调用。
- README、API、数据库关系、路线、后端与项目 ADR、开发日志同步到本阶段状态。

### 验证

- `pnpm test`：24 项通过，0 项失败。
- 验证 Provider/Model 字段、六类任务查询、规则 CRUD、默认/备用切换、目录回退、重启持久化和 0.9.0 数据升级。
- 验证跨用户、能力不匹配、重复规则、非法类型和密钥/凭据输入被拒绝。
- 验证现有账号、对话、Context、SubjectState、Global Settings、Event、Permission 与 Security 回归测试继续通过。

### 当前限制

- 测试状态固定为 `not_tested`；没有真实模型连通性测试、调用、Token 统计、费用计算或网络故障重试。
- API Key 不可写入，安全存储产品、加密、轮换与注入方式仍待后续 ADR。
- 未开发平台后端 7、MCP、Tool、设备或前端页面接入；Router 与 Context、SubjectState、Assistant Global Settings 保持分离。
- 当前接口仍无真实认证，开发数据库不得公开部署。

## 0.25.0｜2026-07-28

### 新增

- 新增 `assistant_global_settings` 数据结构、SQLite 仓储和 AI Assistant Global Settings 领域服务。
- 新增助手名称、头像/标识、人格描述、表达方式、关系定义、长期要求和禁止事项的统一读取与局部更新 API。
- 新增完整全局设定 Context 投影，以及默认值、持久化、隔离、校验、状态分离和事务回滚测试。

### 调整

- 后端版本更新为 `0.9.0`，开发 SQLite 新增 `008_create_assistant_global_settings.sql`。
- Subject 名称和头像保持唯一身份来源；人格、表达、关系、长期要求和禁止事项迁入一对一扩展结构，不从旧 `basicSettings` 自动猜测或迁移含义。
- Context 的 `subjectGlobalSettings` 读取新服务投影；全局设定变化与最小 `subject_updated` Event 原子提交。
- 后端 README、API、ADR、开发日志和后端规划同步到本阶段状态。

### 验证

- `pnpm test`：23 项通过，0 项失败。
- 验证新建主体默认设定、七类字段更新、跨窗口 Context 读取和重启持久化。
- 验证跨用户访问、未知/非法字段、重复要求、空更新被拒绝，无变化更新不写库或重复发事件。
- 验证 Subject 身份、扩展设定和 Event 同事务回滚，且全局设定更新不会创建或改变 SubjectState。

### 当前限制

- 全局设定只保存调用方明确提交的内容，不使用 GPT、Claude、GLM 或任何外部 API 自动生成。
- 长期要求和禁止事项不能削弱平台最低安全规则；当前未实现设定历史版本或前端页面接入。
- 未进入平台后端 6；没有新增 Memory、连续性算法、MCP、Tool、设备或 AI 私域能力。
- 当前接口仍无真实认证，开发数据库没有生产加密与保留策略，不得公开部署。

## 0.24.0｜2026-07-28

### 新增

- 新增 ConversationSummary 与 SummarySource 数据结构、仓储、服务和 API。
- 新增同主体跨窗口摘要读取，每个其他 Conversation 只投影最新摘要。
- 新增 SubjectState / `state_update` 不可变版本、当前状态指针和未解决 Event 引用。
- 新增只读 Context 装配接口，按规划顺序返回主体设定、状态、事件、近期消息、跨窗口摘要和明确占位。
- 新增 Context/Summary/SubjectState 持久化、来源、隔离、回滚和不可变测试。

### 调整

- 后端版本更新为 `0.8.0`，开发 SQLite 新增 `007_create_context_summaries_and_subject_states.sql`。
- Message 仓储增加有限数量的近期消息读取，用于 Context 投影，不改变既有消息顺序或版本语义。
- 后端 README、API 文档、后端规划、ADR、开发日志和数据库关系说明同步到本阶段状态。

### 验证

- `pnpm test`：20 项通过，0 项失败。
- 验证摘要来源必须属于同一用户/主体范围，MessageVersion 来源必须属于摘要所在 Conversation。
- 验证状态来源、未解决 Event、单调版本和当前指针，以及跨窗口摘要在重启后仍可读取。
- 验证 Context 装配顺序、当前用户消息去重、Memory 未实现状态以及零模型/零外部调用标记。
- 验证摘要与状态复合事务回滚、数据库不可变触发器和外键完整性。

### 当前限制

- 摘要和 `state_update` 由开发调用方显式提供，不调用 GPT、Claude、GLM 或其他模型/API。
- 没有真实摘要生成、相关性检索、Token 预算、Memory 持久化、状态演化或 continuity-engine。
- 分支、删除和窗口重置未实现；前端 `src`、页面和 mock 未修改。
- 当前接口仍无真实认证，开发数据库没有生产加密与保留策略，不得公开部署。

## 0.23.0｜2026-07-28

### 新增

- 新增 Conversation、Message、MessageVersion 领域服务与 SQLite 仓储。
- 新增 `conversations`、`messages`、`message_versions` 开发数据库结构及复合归属、消息序号、版本父链和当前版本约束。
- 新增会话创建/列表/详情、消息创建/列表/详情、用户编辑、主体重生成记录和版本历史/单项 API。
- 新增 `conversation_created`、`message_created`、`message_updated`、`message_regenerated` 四类软件事件。
- 新增对话持久化、版本链、事件脱敏、隔离、操作限制、陈旧写入与事务回滚测试。

### 调整

- 后端状态进入“平台后端 7｜对话系统基础”，版本更新为 `0.7.0`。
- Event 数据库约束从五类扩展为九类；`006` 迁移保留已有事件并重建原查询索引，不修改历史迁移。
- Message 作为稳定逻辑记录，通过 `current_version_id` 选择当前正文；MessageVersion 只追加且由数据库阻止普通更新/删除。
- 编辑和重生成必须携带当前 `baseVersionId`，版本追加、当前指针、会话活动时间和 Event 原子提交。

### 验证

- `pnpm test`：17 项后端测试通过，0 项失败。
- 验证三类发送者、稳定消息顺序、当前消息流、完整版本历史和重启持久化。
- 验证跨用户/主体/会话隔离、发送者操作限制、陈旧版本冲突，以及 Event 不含标题或正文。
- 验证 Event 插入失败时 Message、MessageVersion、当前指针和 Conversation 时间整体回滚。
- 前端页面、组件、mock、前端依赖和 `src/` 未修改。

### 当前限制

- 主体消息和重生成内容由开发调用方显式提交，只记录数据版本，不调用 GPT、Claude、GLM 或其他模型。
- 当前只支持线性文本消息；删除、分支、重置、附件、流式输出和自动标题未实现。
- 对话页面仍使用 mock；未接入上下文装配、摘要、Memory、SubjectState 或 continuity-engine。
- 路由没有真实认证，开发 SQLite 没有生产加密与保留策略，不得直接公开部署或保存真实敏感对话。

## 0.22.0｜2026-07-27

### 新增

- 新增统一 API 成功/错误 envelope，固定包含 `success`、`data`、`error` 和 `timestamp`。
- 新增开发期当前用户接口、Subject 列表与基础信息更新接口，以及 Dashboard 基础聚合接口。
- 新增 Subject 更新时的最小化 `subject_updated` 事件，并通过外层事务保证数据与事件一致。
- 新增类型化前端 API 客户端、请求超时/取消、结构化错误，以及 Vite 开发同源代理与启动健康握手。
- 新增后端 API 说明文档和基础 API 连接测试。

### 调整

- 后端状态进入“平台后端 6｜基础 API 层与前后端连接”，版本更新为 `0.6.0`。
- Subject 基础设定更新采用完整对象替换；无变化更新不刷新时间、不产生事件。
- Dashboard 只返回已有用户、主体和基础状态，连续性状态明确为 `not_available`。
- 更新项目与后端 README、后端规划、ADR 和开发日志，说明真实连接与页面 mock 的边界。

### 验证

- `pnpm test`：14 项后端测试通过，0 项失败。
- 验证 User、Subject、Dashboard、统一响应、持久化、跨用户隔离和 Subject/Event 事务回滚。
- 前端生产构建通过；开发代理已实际完成健康检查和 User → Subject → Dashboard 闭环。
- 前端页面、组件、模拟数据、mock 文件和依赖未修改或删除。

### 当前限制

- `x-vio-user-id` 仅用于本地开发请求上下文，不是认证机制；当前路由不得直接公开部署。
- 登录、首次设置和工作台页面仍消费 mock，尚未迁移到真实 API。
- 未接入真实模型、MCP、Skill、Tool、设备、支付、AI 私域或 continuity-engine。
- 正式数据库、生产跨域/网关、认证、限流和公开部署策略仍未完成。

## 0.21.0｜2026-07-27

### 新增

- 新增 Security Service、规则化风险分类器与统一安全检查接口。
- 新增 SensitiveData 五类分类元数据，所有分类明确禁止接收正文。
- 新增五分钟有效、作用域/策略绑定的 `security_confirmations` 与只追加 `audit_logs` 开发数据库结构、仓储和领域服务。
- 新增敏感分类、安全检查、确认读取/决定和审计只读路由。
- 新增安全风险、三种确认要求、确认隔离/防重放、审计和 `allow_once` 时点测试。

### 调整

- 后端状态进入“平台后端 5｜安全系统基础”，版本更新为 `0.5.0`。
- Permission `allow_once` 增加非消费预览，安全链满足前不会提前消耗。
- Permission 变更和 APIProvider 配置变更新增最小化 AuditLog；原有 Event 仍独立承担软件变化记录。
- SQLite 事务允许内部服务加入同一外层事务，用于权限、确认与审计原子编排。

### 验证

- `pnpm test`：12 项测试通过，0 项失败。
- 验证 `low`、`medium`、`high`、`critical` 风险及免确认、每次确认、用户自定义确认。
- 验证 Permission `ask`/`deny` 联动、高风险长期允许仍需确认，以及确认过期、策略绑定和单次使用。
- 验证安全审计持久化、条件查询、跨用户隔离与 Permission/APIProvider 配置变更记录。
- 前端代码、前端依赖和前端构建配置无修改。

### 当前限制

- 安全检查只返回执行资格并明确标记未执行，不连接真实支付、设备、手机权限或外部能力。
- 本阶段 SensitiveData 没有正文写入接口，AuditLog 没有 payload/正文列；按契约不得把 API Key、身份/支付原文、私密记录正文或 AI 私域正文作为资源引用传入。已有账号邮箱等基础身份字段不属于该分类模块。
- 资源 ID 的常见凭据形态拦截是启发式防线，不是生产级 DLP；真实资源接入仍需可信资源注册表和秘密扫描。
- 真实认证、用户确认偏好存储、可配置确认有效期、加密、生产审计保留和法律策略仍未完成。

## 0.20.0｜2026-07-26

### 新增

- 新增 Permission 领域服务、Permission Checker 与 SQLite 仓储。
- 新增 `permissions` 数据表、用户/主体复合归属、当前规则唯一约束和判断索引。
- 新增七类权限资源、五档权限等级、八种基础操作和四种生命周期状态。
- 新增权限创建、条件/单项查询、更新、软删除和三态判断路由。
- 新增权限 CRUD、判断、单次消费、事件联动、资源类型和隔离测试。

### 调整

- 后端状态进入“平台后端 4｜权限系统基础”，版本更新为 `0.4.0`。
- Checker 只使用用户、主体、资源实例和操作完全匹配的活动规则；无匹配默认返回 `deny`。
- `allow_once` 首次判断为允许后转为 `consumed`；删除转为 `deleted`，均不再参与判断。
- 权限创建、实际更新、删除和单次消费与 `permission_changed` Event 同事务提交。

### 验证

- 34 个后端 JavaScript 文件通过语法检查。
- `pnpm test`：9 项测试通过，0 项失败。
- 验证五档到 `allow`/`ask`/`deny` 的映射、`canAsk` 区分和无规则默认拒绝。
- 验证七类资源、跨用户/主体隔离、重启持久化及权限变化事件。
- 前端代码、前端依赖和前端构建配置无修改。

### 当前限制

- 未实现会话级授权、通配范围、继承、优先级、有效期或高风险操作确认。
- 未执行或接入真实 MCP、Skill、Tool、设备、手机权限、AI 私域或连续性引擎。
- 正式数据库、审计保留周期和生产并发策略仍未完成。

## 0.19.0｜2026-07-26

### 新增

- 新增 APIProvider 领域服务与 SQLite 仓储。
- 新增 Model 领域服务、能力标签目录与 SQLite 仓储。
- 新增只执行本地规则匹配的 Model Router 服务。
- 新增 Provider 创建、查询、列表和状态更新路由。
- 新增 Model 创建、单项查询、能力查询和任务路由选择接口。
- 新增 Provider/Model 持久化、能力匹配、停用排除、用户隔离和密钥安全测试。

### 调整

- 后端状态进入“平台后端 3｜模型路由与 API 管理基础系统”，版本更新为 `0.3.0`。
- Provider 类型限定为 `openai`、`claude`、`glm`、`custom`，状态限定为 `enabled`、`disabled`。
- 模型能力标签限定为 `chat`、`vision`、`image`、`video`、`embedding`。
- Router 只从启用 Provider 中按稳定创建顺序选择首个能力匹配模型。
- API Key 引用字段当前强制为空，接口不接受或返回真实凭据。

### 验证

- 29 个后端 JavaScript 文件通过语法检查。
- `pnpm test`：7 项测试通过，0 项失败。
- 验证 Provider/Model 创建、查询、状态更新、能力检索和 Router 选择。
- 验证重启持久化、停用 Provider 排除、跨用户隔离、非法能力与密钥输入拒绝。
- 前端代码、前端依赖和前端构建配置无修改。

### 当前限制

- 未接入真实 API Key、密钥库、GPT、Claude、GLM 或其他模型 API。
- 未实现真实模型调用、连接测试、Token/费用统计、优先级、备用路由或故障切换。
- 未开发连续性引擎、权限、MCP、Skill、Tool 或设备系统。

## 0.18.0｜2026-07-25

### 新增

- 新增 Event 领域服务与 SQLite 仓储。
- 新增 `events` 数据表、归属约束和用户/主体/类型/状态/时间查询索引。
- 新增五种基础事件类型：`appearance_changed`、`subject_updated`、`permission_changed`、`life_record_created`、`device_changed`。
- 新增事件创建、单项查询和筛选列表路由。
- 新增事件持久化、筛选、类型约束、敏感字段和跨用户主体隔离测试。

### 调整

- 后端状态进入“平台后端 2｜事件总线基础系统”，版本更新为 `0.2.0`。
- 事件写入采用不可原地改写的历史记录，并默认标记为 `pending`。
- 主体级事件同时保存用户 ID 和主体 ID，并使用复合外键保证主体属于该用户。
- 事件接口继续使用用户嵌套路由和统一 JSON 响应格式。

### 验证

- `pnpm test`：5 项测试通过，0 项失败。
- 验证事件创建、SQLite 保存、服务重启后读取和多条件查询。
- 验证五类事件、用户隔离、主体归属和事件秘密字段拦截。
- 前端代码、前端依赖和前端构建配置无修改。

### 当前限制

- 当前只提供事件记录与查询，不提供消费、投递、重试或状态更新接口。
- 未接入连续性引擎、AI 模型、真实认证或正式数据库。
- 未开发权限、MCP、Tool 或设备业务模块；`permission_changed` 和 `device_changed` 目前仅是可记录的事件类型。

## 0.17.0｜2026-07-25

### 新增

- 新增首个可运行的 Vio Live 平台后端服务。
- 新增 Node.js 22 ESM 启动入口、配置管理和服务生命周期。
- 新增开发期 SQLite 数据库、顺序迁移及迁移记录。
- 新增 `users` 与 `subjects` 基础数据结构和归属外键。
- 新增 User/Subject 仓储与业务服务。
- 新增服务信息、健康检查、用户创建/查询和主体创建/查询路由。
- 新增启动、持久化、重复邮箱和跨用户主体隔离测试。

### 调整

- 后端状态由“逻辑设计”进入“平台后端 1｜基础服务、账号与数据库闭环”。
- 后端采用 Node.js 22 ESM、标准库 HTTP 和内置测试运行器。
- 开发数据库采用 `node:sqlite`、有序 SQL 迁移和仓储适配；正式数据库仍待决策。
- 基础业务路由使用 `/api/v1`、`{ data }` 成功结果和统一错误结果。
- SQLite 运行文件统一加入 Git 忽略范围。

### 验证

- 17 个后端 JavaScript 文件通过语法检查。
- `pnpm test`：3 项测试通过，0 项失败。
- 使用内存 SQLite 执行启动入口，健康检查返回服务与数据库 `ok`。
- 验证服务可以启动，健康检查返回数据库可用。
- 验证 User 和 Subject 可以创建、查询并在服务重启后继续读取。
- 验证重复邮箱冲突和跨用户主体查询隔离。
- 前端代码、前端依赖和前端构建配置无修改。

### 当前限制

- 当前用户创建不是 Google 或邮箱验证码真实认证。
- `node:sqlite` 仅用于开发环境，在当前 Node.js 22 中仍为实验模块。
- 当前基础路由未接入认证，不允许直接公开部署。
- 未实现事件总线、模型路由、权限、MCP、Tool、设备或 AI 私域。

## 0.16.0｜2026-07-25

### 新增

- 新增平台后端核心数据库逻辑设计。
- 新增核心实体关系图、数据归属视图、对话版本关系图和软件事件连续性数据流图。
- 新增 `User`、`Subject`、`SubjectState`、`Memory`、`Event`、`Conversation`、`MessageVersion`、`Permission`、`Device`、`PrivateDomain` 和 `APIProvider` 的逻辑定义。
- 新增数据唯一性、来源追溯、版本、导出、恢复和删除要求。

### 调整

- 将后端第一阶段路线中的领域与数据设计标记为已完成逻辑设计。
- 数据对象统一标注用户数据、AI 主体数据和平台数据，并允许按实际职责叠加分类。
- 主体状态、消息版本和软件事件统一采用保留可追溯历史的设计原则。
- 后端 ADR 新增逻辑模型先于物理选型、数据分类和历史版本三项决策。

### 验证

- 本次只新增和修改 Markdown 文档。
- 前端 `src/` 无修改，后端未新增可执行代码或依赖。
- 未创建数据库表、SQL、ORM、迁移脚本、API 或模型连接。

### 当前限制

- 数据库产品、物理字段类型、索引、事务和迁移工具尚未决定。
- 多对多关系、加密、并发、保留和删除级联仍需后续 ADR。
- 当前设计不能直接视为生产数据库实现。

## 0.15.0｜2026-07-25

### 新增

- 新增独立 `backend/` 平台后端工程设计目录。
- 新增后端工程 README、后端 ADR、后端开发日志和第一阶段实施路线。
- 新增共享内核、业务模块、外部集成、配置、脚本和测试目录说明。
- 新增下一阶段技术栈、数据库、认证、API、密钥、部署、日志和测试的阻塞性决策清单。

### 调整

- 将平台后端状态由“纯规划”调整为“第一阶段工程设计已开始，可运行工程尚未初始化”。
- 将第一阶段细化为范围冻结、技术决策、领域与数据设计、契约设计、安全测试设计和实现前审查。
- 后端局部技术决策记录在 `backend/docs/ADR.md`；影响平台总体边界的决策继续同步到 `docs/决策记录.md`。
- 后端详细执行记录写入 `backend/docs/开发日志.md`，项目级摘要继续写入 `docs/工程日志.md`。

### 验证

- 所有后端工程文件均位于独立 `backend/` 目录。
- 前端 `src/`、前端依赖和构建配置未修改。
- 未新增运行依赖、数据库连接、API、模型连接、环境变量、密钥或用户数据。

### 当前限制

- 后端目录当前只有工程设计和职责说明，没有可运行服务。
- 数据库产品、认证方案、API 风格、部署平台和测试框架仍等待 ADR 决策。
- continuity-engine、模型、MCP、Tool 和设备仍未接入。

## 0.14.0｜2026-07-24

### 新增

- 新增 `docs/后端/` 平台后端规划文档目录。
- 新增后端文档索引、总体架构与系统边界说明。
- 新增领域模型、数据库边界、账号认证和数据隔离规划。
- 新增软件事件总线、审计日志、消息版本与重置记录规划。
- 新增对话上下文装配、状态回写和连续性引擎接口规划。
- 新增模型路由、密钥、Token、扩展能力和设备适配规划。
- 新增权限安全、隐私治理、AI 私域和生活管理后端规则。
- 新增数据导出、备份、迁移、API/事件契约以及部署测试路线图。

### 调整

- 项目文档调整为“项目级记录 + `docs/后端/` 稳定规划”的结构。
- 工程日志、更新记录和 ADR 继续在 `docs/` 根目录统一维护，不建立重复的后端日志体系。
- 明确平台后端与连续性引擎是平行系统，只通过稳定接口协作。

### 验证

- 后端目录包含 README 和 13 份主题规划文档。
- 文档内容均可追溯到产品与开发总规划 v2.4 的现有范围。
- 文档未包含后端实现代码、数据库表、具体接口路径或真实密钥。
- 本次未修改前端源代码、依赖或构建配置。

### 当前限制

- 平台后端仍处于规划阶段，尚未创建后端工程。
- 尚未连接数据库、API、真实模型、MCP、设备或外部执行能力。
- 具体技术栈、数据库产品、部署平台和接口实现等待后续决策。

## 0.13.0｜2026-07-24

### 新增

- 新增「前端 13｜生活管理 - 体重/三围页面」。
- 新增当前体重、目标体重、身高与胸围／腰围／臀围状态卡。
- 新增最近 7 次体重变化曲线、日期坐标和最近测量记录列表。
- 新增今日体重、腰围、胸围、臀围与备注的本地模拟记录表单。
- 新增记录保存后的状态卡、曲线和日期记录即时联动。
- 新增分析最近变化与查看调整建议两个纯 UI 入口。
- 新增体重页头、当前状态、趋势记录、添加表单、AI 操作、共用图标与集中模拟数据。

### 调整

- 工作台顶部生活管理入口调整为“月历／管账／体重”三个并列入口。
- 体重页作为工作台下级本地视图，不新增主导航标签或路由依赖。
- 页面顶部和 AI 区域明确展示非医疗边界，不输出诊断或健康结论。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度、三入口、长内容滚动与双列记录表单检查。
- 通过新增记录联动、趋势曲线更新和两个 AI 本地反馈入口检查。
- 页面无横向溢出，浏览器控制台无警告或错误。

### 当前限制

- 所有体重、身高、三围、日期和趋势均为本地模拟数据。
- 新增记录与 AI 按钮仅改变当前页面状态或提示，不产生网络请求与持久化。
- 未连接后端、数据库或 API，不提供医疗诊断或健康建议。

## 0.12.0｜2026-07-23

### 新增

- 新增「前端 12｜生活管理 - 管账页面」。
- 新增本月收入、本月支出、剩余预算、预算余量与消费趋势总览。
- 新增包含日期、时间、项目、金额和分类的模拟账单列表。
- 新增餐饮、购物、娱乐、交通、住房和其他六类分类统计。
- 新增分析消费习惯与查看建议两个纯 UI 入口。
- 新增钱包连接、自动规则与安全设置预留入口。
- 新增管账页头、财务总览、账单、分类统计、AI 操作、安全入口、共用图标与集中模拟数据。

### 调整

- 工作台顶部生活管理入口调整为“月历／管账”并列入口。
- 管账作为工作台下级本地视图，不新增主导航标签或路由依赖。
- 所有资金相关入口统一展示无真实连接、无真实操作的安全说明。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度和长账单列表检查。
- 通过财务总览、消费趋势、六类统计、AI 操作与三个安全入口检查。
- 工作台双入口在 320px 下完整可见，页面无横向溢出或控制台错误。

### 当前限制

- 所有金额、预算、趋势、账单和分类统计均为本地模拟数据。
- AI、钱包、规则和安全按钮仅显示本地反馈，不读取账户或执行资金操作。
- 未连接支付接口、银行、后端、数据库或 API，未实现真实转账。

## 0.11.0｜2026-07-23

### 新增

- 新增「前端 11｜生活管理 - 月历页面」。
- 新增月份显示、上月/下月切换、星期标题与日期网格。
- 新增纪念日、生理期和亲密记录三类差异化日期标记。
- 新增选中日期的事件、备注和添加记录区域。
- 新增与 AI 讨论、AI 分析与添加提醒三个纯 UI 入口。
- 新增三类本地记录添加流程，并让新增记录即时更新当前页面标记与详情。
- 新增月历页头、月历网格、日期详情、添加记录、AI 操作、共用图标与集中模拟数据。

### 调整

- 工作台顶部新增“生活月历”快捷入口，进入月历后可返回工作台。
- 月历作为工作台下级本地视图，不新增主导航标签或路由依赖。
- 月份、选择日期、事件与新增记录均由页面本地状态管理。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度及 7 列日期网格检查。
- 通过月份切换、多类事件标记、日期详情、空日期、三类新增记录和 AI 入口检查。
- 页面无横向溢出或控制台错误，返回工作台流程正常。

### 当前限制

- 所有日期、事件、备注和新增记录均为本地模拟数据。
- AI、分析和提醒按钮仅显示 UI 反馈，不发送内容或执行真实任务。
- 未连接后端、数据库、API、MCP 或系统日历。

## 0.10.0｜2026-07-23

### 新增

- 新增「前端 10｜我的页面」。
- 新增账号信息、登录方式与账号绑定入口。
- 新增智能体名字、头像、性格、人设与强制要求展示。
- 新增主题、背景、气泡样式和页面装饰设置。
- 新增数据导出、数据删除与备份入口。
- 新增权限规则、默认询问方式和工具权限管理入口。
- 新增隐私政策、数据说明与 AI 私域设置入口。
- 新增我的页头、通用设置分组、开关、六类专用组件、共用图标和集中模拟数据。

### 调整

- 将主导航中的“我的”空白占位替换为完整设置页面。
- 我的页面采用固定页头与独立内容滚动区，底部主导航保持可见。
- 外观、询问方式与页面装饰使用确定性的本地状态，不影响其他页面或持久化存储。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度及长列表滚动检查。
- 通过主题、背景、气泡、装饰开关、默认询问方式、数据与隐私入口的本地交互检查。
- 页面无横向溢出或控制台错误。

### 当前限制

- 所有账号、智能体、外观、数据、安全和隐私信息均为本地模拟数据。
- 所有按钮与开关只改变当前页面 UI，不执行绑定、保存、导出、备份、删除或权限修改。
- 未连接后端、数据库或 API。

## 0.9.0｜2026-07-23

### 新增

- 新增「前端 9｜设备与家电页面」。
- 新增已连接设备列表，展示名称、位置、状态、关键参数、常用操作与详情入口。
- 新增设备详情页，包含基本信息、权限管理、自动化规则与访问日志。
- 新增高风险设备提示、立即停止与撤销权限入口。
- 新增未连接设备分类、添加适配器与连接服务入口。
- 新增设备页头、设备卡、详情、未连接设备、共用图标和集中模拟数据。

### 调整

- 能力中心“手机 / 设备入口”的管理按钮现在可进入设备中心。
- 设备中心采用本地两层视图，在设备总览与单设备详情之间切换，不引入路由或新的主导航标签。
- 所有设备类别均使用通用名称，并明确标注“模拟”或“未指定品牌”。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度及长列表滚动检查。
- 通过能力中心进入、设备详情返回、权限启停、自动化/日志展开、危险操作及未连接入口检查。
- 页面无横向溢出或控制台错误。

### 当前限制

- 所有设备、状态、参数、权限、规则与日志均为本地模拟数据。
- 所有按钮只改变当前页面 UI，不控制真实设备，不添加真实适配器，也不连接第三方服务。
- 未连接后端、数据库、API 或任何真实品牌服务。

## 0.8.0｜2026-07-23

### 新增

- 新增「前端 8｜能力中心页面」。
- 新增模型/API、MCP、Skill、插件、Tool 与手机/设备六类能力分组。
- 新增模型服务信息、Base URL、默认任务、新增服务和连接测试入口。
- 新增 MCP 与 Skill 启停、权限状态、适用范围和最近调用记录。
- 新增插件版本、更新与卸载入口。
- 新增工具权限、使用记录、主动推荐开关和设备管理入口。
- 新增能力页头、通用分组、开关、六类专用组件、共用图标和集中模拟数据。

### 调整

- 将主导航中的“能力”占位内容替换为能力中心页面。
- 我的继续保留原有空白占位内容。
- 能力中心使用固定页头和独立内容滚动区，底部主导航保持可见。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度及长列表滚动检查。
- 通过连接测试、MCP/Skill 启停、插件更新入口、工具主动推荐和设备管理状态检查。
- 页面无横向溢出或控制台错误。

### 当前限制

- 所有模型、服务、权限、调用、版本和设备信息均为本地模拟数据。
- 所有按钮与开关只改变当前页面 UI，不执行真实连接、安装、卸载、授权或设备控制。
- 未连接后端、数据库或 API。

## 0.7.0｜2026-07-23

### 新增

- 新增「前端 7｜AI 私域页面」。
- 新增四项默认锁定的私域目录和申请查看页面。
- 新增申请理由输入、发送申请与内容开放预览。
- 新增全部开放、部分开放、暂时拒绝和延后开放四种预设模拟回复状态。
- 新增申请时间、开放范围与操作记录组成的模拟查看记录。
- 新增 AI 私域页头、目录、申请面板、回复状态、记录与共用图标组件。

### 调整

- 将主导航中的“AI 私域”占位内容替换为 AI 私域页面。
- 能力和我的继续保留原有空白占位内容。
- 目录和申请页面使用本地 React 状态切换，进入新目录项目时重置申请内容与回复状态。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度布局检查。
- 通过目录锁定、申请理由校验、发送申请和四种预设回复状态检查。
- 全部/部分开放按范围显示模拟内容，拒绝/延后保持锁定；页面无横向溢出或控制台错误。

### 当前限制

- 所有私域条目、开放内容、回复状态和查看记录均为本地模拟数据。
- 申请不会发送、保存或触发真实 AI 判断。
- 未连接后端、数据库或 API。

## 0.6.0｜2026-07-23

### 新增

- 新增「前端 6｜连续性页面」。
- 新增智能体身份、关系脉络、近期延续、时间节点、形成中的想法和当前状态六类卡片。
- 新增状态强度指示、生命周期事件与连续性模拟数据说明。
- 新增连续性页头、通用卡片、状态卡片、共用图标组件和集中模拟数据。

### 调整

- 将主导航中的“连续性”占位内容替换为连续性页面。
- AI 私域、能力和我的继续保留原有空白占位内容。
- 连续性页面使用固定页头与独立内容滚动区，底部主导航保持可见。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度布局与长内容滚动检查。
- 六类卡片内容完整，页面无横向溢出，控制台无错误。

### 当前限制

- 所有身份、关系、时间、想法与状态内容均为本地模拟数据。
- 页面不读取、保存或更新真实记忆。
- 未连接后端、数据库或 API。

## 0.5.0｜2026-07-23

### 新增

- 新增「前端 5｜对话页面」。
- 新增智能体与会话顶部区域及会话切换入口。
- 新增用户、Vio 两类模拟消息气泡、时间显示与独立滚动消息区。
- 新增模型选择、四档上下文模式及记忆、状态、工具入口。
- 新增编辑消息、重新生成、删除和创建新分支操作界面。
- 新增文本输入、发送、图片、文件和语音入口。
- 新增对话页拆分组件、共用图标与集中模拟数据。

### 调整

- 将主导航中的“对话”占位内容替换为对话页面。
- 连续性、AI 私域、能力和我的继续保留原有空白占位内容。
- 对话页使用固定顶部控制与输入区域，只有消息列表独立滚动。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度布局与滚动检查。
- 页面无横向溢出，模型与上下文模式原生控件可切换。
- 消息操作与输入附件入口均完整显示，页面控制台无错误。

### 当前限制

- 所有会话、消息、记忆、状态、工具和模型内容均为本地模拟数据。
- 会话切换、消息操作、附件、语音和发送按钮只提供界面，不执行真实业务逻辑。
- 未连接后端、数据库、API 或真实 AI。

## 0.4.0｜2026-07-22

### 新增

- 新增「前端 4｜工作台第一屏」。
- 新增智能体头像与名字顶部区域。
- 新增当前关注、今日概览、最近变化和快捷入口组件。
- 新增底部通用输入框及纯前端模拟反馈。
- 新增集中管理的工作台模拟数据与共用图标组件。

### 调整

- 主导航框架改为固定视口高度，工作台内容区独立滚动。
- 通用输入框与底部导航在工作台滚动时保持可见。
- 其他五个主导航页面继续保留原有空白占位内容。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度布局与滚动检查。
- 工作台内容、输入框与导航无控件越界或文字截断。
- 在全新浏览器页面中验证无控制台错误。

### 当前限制

- 工作台所有内容均为本地模拟数据。
- 快捷入口不执行实际业务操作。
- 通用输入不会发送或持久化。
- 未连接后端、数据库或 API。

## 0.3.0｜2026-07-22

### 新增

- 新增「前端 3｜主导航框架」。
- 新增工作台、对话、连续性、AI 私域、能力和我的六个底部导航入口。
- 新增六个统一的空白占位页面。
- 新增底部导航本地切换、选中状态和浏览器标题同步。
- 首次设置完成或跳过高级设置后可进入主导航框架。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度布局检查。
- 六个导航标签均可切换，无文字截断或控件越界。
- 在全新浏览器页面中验证无控制台错误。

### 当前限制

- 六个入口仅包含占位内容，尚未实现具体页面功能。
- 导航状态不会持久化，刷新后仍从登录页开始。
- 未连接后端、数据库或 API。

## 0.2.0｜2026-07-22

### 新增

- 新增「前端 2｜首次设置页面」原型。
- 新增智能体名称和可选头像设置。
- 新增基础定位、记忆保存方式和默认上下文模式选择。
- 新增自定义定位补充输入框。
- 新增“跳过高级设置”和“完成设置”的纯前端交互反馈。
- 登录页完成前端登录流程后可进入首次设置页面。

### 验证

- 通过 TypeScript 与 Vite 生产构建。
- 通过 320px、390px 手机宽度布局检查。
- 页面无控件越界，浏览器控制台无错误。

### 当前限制

- 所有设置只存在于当前页面状态中。
- 未连接后端、数据库或 API。
- 尚未开发首次设置之后的页面。

## 0.1.0｜2026-07-22

### 新增

- 初始化 React + Vite + TypeScript 前端项目。
- 新增手机端优先的登录页原型。
- 新增 Vio Live 文字 Logo。
- 新增 Google 登录按钮。
- 新增邮箱与验证码输入区域。
- 新增用户协议、隐私政策、风险与免责声明勾选项。
- 新增登录/注册按钮及纯前端校验状态。
- 新增工程日志、决策记录和更新记录。

### 当前限制

- 未连接后端。
- 未连接数据库。
- 未接入任何 API。
- Google 登录、验证码发送和登录/注册不会产生真实网络请求。

### 下一步

- 开发「前端 2｜首次设置」页面。
