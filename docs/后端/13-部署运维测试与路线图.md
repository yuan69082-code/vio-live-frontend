# 13｜部署、运维、测试与路线图

## 状态

- 文档状态：初始路线规划
- 后端工程：基础服务与前后端开发连接已可运行
- 部署环境和数据库：开发期 SQLite 已建立；正式部署环境和数据库尚未创建
- 当前任务边界：平台后端 4“上下文、摘要与跨窗口连续”，只完成可追溯摘要、状态版本和只读 Context 基础；页面仍使用 mock，不调用模型、外部 API 或连续性引擎

## 总体开发顺序

总规划给出的阶段顺序如下：

| 阶段 | 主要任务 | 完成标准 |
| --- | --- | --- |
| 0. 整理现状 | 冻结核心代码、修正文档、确认测试 | 知道已有内容，不重复建设 |
| 1. 产品蓝图 | 明确核心页面与流程 | 页面和按钮流程明确 |
| 2. 后端数据设计 | 用户、主体、事件、消息版本、记忆、权限、设备和私域 | 每类数据知道存在哪里 |
| 3. 平台后端 | 登录、数据库、事件总线、模型路由和密钥管理 | 手机前端可以安全调用 |
| 4. 前端第一版 | 完成核心页面和手机布局 | 一个用户可以完整使用 |
| 5. 接入连续性引擎 | 状态、事件、摘要、跨窗口和重置版本 | 连续性真实生效 |
| 6. 扩展能力 | 至少一个 MCP 或 Tool | 完成一次真实调用闭环 |
| 7. 设备中心 | 统一接口和一个设备适配器 | 至少一种设备可安全读取或控制 |
| 8. AI 私域 | 申请、开放、拒绝和删除 | 规则真实可用 |
| 9. 生活模块 | 月历、管账或体重先完成一个闭环 | 数据与 AI 互动可用 |
| 10. 公开测试 | 用户隔离、备份、监控、隐私和安全 | 少量用户不会串数据 |
| 11. 开源发布 | 许可证、贡献指南、部署和安全说明 | 其他人可以部署、理解和贡献 |

## 当前阶段

截至 2026-07-28，已完成工程设计、核心逻辑数据模型、账号数据库、事件、模型路由、权限、安全、基础 API 连接、对话数据，以及本轮上下文/摘要/跨窗口基础。当前开发期 SQLite 可以保存和查询 User、Subject、Conversation、Message、MessageVersion、ConversationSummary、SubjectState、Event、APIProvider、Model、Permission、Confirmation 与 AuditLog；Context 只读装配已有事实。前端通过独立客户端和 Vite 同源代理进行非阻塞健康握手，但页面仍使用 mock。Security 只返回未执行的资格判断；真实资源执行、摘要生成、Memory、模型/连续性调用、事件消费、认证、正式数据库和后续平台模块仍未实现。

第一阶段的详细实施路线见 [`../../backend/docs/第一阶段实施路线.md`](../../backend/docs/第一阶段实施路线.md)，依次覆盖：

1. 范围冻结与工程边界
2. 技术决策准备
3. 领域与数据设计
4. 契约设计
5. 安全与测试设计
6. 进入实现前审查

当前已完成工程边界、核心领域数据设计、账号/主体、线性对话版本、事件、Provider/Model 规则路由、Permission 精确判断、Security/Confirmation/AuditLog 基础，以及统一 API 契约和开发期前后端连接。Node.js、开发期 SQLite、不可变消息版本、事件记录、模型选择、Key 禁止写入、权限默认拒绝、分层安全、审计隔离、确认防重放、同源代理和开发期当前用户上下文已有 ADR；分支/重置、正式数据库、真实能力适配、会话权限、密钥库、事件消费、认证、加密、备份、并发和部署仍需后续决策。

## 第一版必须完成

- 账号、数据库和用户/主体隔离
- 软件事件总线
- 消息版本、删除、分支和重置记录
- 上下文摘要、跨窗口和本地记忆基础
- 模型/API 路由基础和手动选择
- 权限五档、撤销和访问日志
- AI 私域基础和数据导出
- 至少一个 MCP 或 Tool

## 第一版暂缓

- 全品牌家电接入
- 无限后台扫描和无限 Token
- 复杂多用户协作编辑
- 自动永久人格变化
- 完整自动费用优化
- AI 无限制自动执行
- 真实转账、代扣和账户冻结
- 插件商店和商业分成

## 部署与运维准备

总规划要求公开测试前具备：

- 用户和主体隔离
- 数据库备份和恢复
- 错误与使用日志
- 运行监控
- 数据导出和删除
- 隐私与安全规则
- 密钥不进入 GitHub

具体部署平台、环境数量、容器、网络和监控产品不在当前规划中确定，等待后端技术栈 ADR。

## 测试范围

### 数据隔离

- 不同用户不能读取或修改彼此数据。
- 同一用户的不同主体不会混用状态、记忆、私域或设备权限。

### API 与前后端连接

- 所有 JSON 接口稳定返回 `success`、`data`、`error` 和 `timestamp`。
- User、Subject 与 Dashboard 接口保持用户归属隔离；Subject 更新与 `subject_updated` 事件原子提交。
- 前端开发服务器可以通过同源代理访问后端健康检查，后端不可达不会阻塞页面启动。
- 当前用户开发请求头不得被误认为登录认证；页面 mock 未经后续迁移不得删除。

### 对话与事件

- 当前实现必须验证 Conversation/Message 复合归属、稳定顺序、编辑/重生成父版本链、当前版本和陈旧写冲突。
- 删除、分支和重置仍是未来验收项，不能由当前线性父版本链冒充完成。
- 软件事件不包含秘密原文。
- 对话事件不得复制标题或消息正文；Event 失败必须回滚消息和版本写入。
- 窗口重置不自动删除连续状态。
- ConversationSummary 必须至少引用一个同范围 MessageVersion 或 Event，跨窗口读取必须排除当前 Conversation 且不能跨主体。
- SubjectState 必须保留历史、只存在一个当前指针，`state_update` 来源和未解决 Event 必须属于同一用户与主体。
- Context 必须按产品顺序投影已有事实，明确标记 Memory 未实现以及模型、外部 API、continuity-engine 未调用。
- 摘要/来源和状态/未解决事件/当前指针任一写入失败时必须整体回滚。

### 权限与安全

- 五档权限行为符合定义。
- 会话授权按时失效。
- 高风险操作需要具体确认。
- 撤销权限后调用停止。
- Permission 拒绝不能被确认绕过；长期允许不能跳过高风险确认。
- 确认不能跨用户、主体、资源、动作或权限版本复用，批准后只能消费一次。
- `allow_once` 在确认前不消费，安全链满足后才消费。
- 审计只保存最小字段，不保存秘密原文或任意请求正文。

### 模型与扩展

- 模型切换不影响主体状态。
- 打开应用不自动消耗 Token。
- 外部能力失败不会被伪造成成功。
- 推荐能力不会绕过执行权限。

### 数据治理

- 导出范围与用户选择一致。
- 备份恢复不破坏隔离关系。
- 删除账号、主体和单项数据的范围清晰。

## 阶段完成原则

- 每一阶段先规划、再实施、再测试、再记录和提交。
- 页面存在不等于后端能力完成。
- 本地 JSON 或模拟数据不等于公开运行数据库。
- 底层连续性原型不等于平台后端完成。
- 未通过安全和隔离测试，不进入公开测试。

## 下一步边界

先审查本阶段摘要/来源、SubjectState 当前指针、跨窗口隔离、Context 装配顺序、不可变历史和事务回滚边界。未经新的明确开发任务，不迁移对话页面 mock，不开发自动摘要、Memory、相关性检索、Token 预算、删除、分支、重置、MCP、Skill、Tool、真实模型、真实支付、真实设备、手机权限、AI 私域或连续性引擎；当前未认证路由不得直接公开部署。
