# 13｜部署、运维、测试与路线图

## 状态

- 文档状态：现行 R0-R13 路线与历史阶段材料分开维护，2026-09-04 四项决定已同步
- 后端工程：基础服务与前后端开发连接已可运行
- 部署环境和数据库：开发期 SQLite 已建立；正式部署环境和数据库尚未创建
- 当前任务边界：R0 已验收并推送；R2 个人访问、多助手、模型配置及账户删除、R1 真实个人会话/当前助手范围的 Vio 独立聊天、迁移 `025` 和恢复合同均已于 **2026-09-05 正式验收通过**。R3 多会话、迁移 `026`、前端接线和受控 loopback 页面闭环已于 **2026-09-06 正式验收通过**。既有 V5/F1/L1 与首次 S4-Live 已完成，但固定身份试聊不是 R1/R3 或完整生产产品验收。
- S4-Live 验收必须使用全新、仓库外、短绝对路径的可销毁同根沙箱，Windows 推荐 `C:\VioS4\first-001`；固定 v1.1 身份仅限 `disposable_test` 且禁止晋升。创建与 doctor 以 240 字符门禁检查 Engine WakeSession 最终/原子临时文件最坏路径，超限 fail closed。门禁上线前、唯一问题为超预算的旧沙箱可以在其余严格校验全部通过后由 cleanup 双确认整根删除，doctor 不因此变为 ready；启动、停止服务与清理按 `backend/scripts/README.md` 执行。首次正式验收已按该短路径规则完成并由官方 cleanup 整根删除。

## 总体开发顺序

<a id="r0-r13-order"></a>

### 现行 R0-R13 顺序与阶段出口

按 2026-09-04 四项决定修订版监工规划及 [ADR-034](../决策记录.md#adr-034) 执行：**R0 → R2 → R1 → R3 → R4 → R5 → R6 → R7 → R8 → R9 → R10 → R11 → R12 → R13**。保留编号，不增加阶段；上一施工阶段未验收不得开始下一阶段。本表只同步已确认规划，不授权施工或声称以下功能已实现。

| 阶段 | 既定职责和出口 | 当前状态 |
| --- | --- | --- |
| R0 | 冻结 Vio Core/可选运行时边界、通用端口、两模式规则、状态展示和证据对应 | A/B/C 及整阶段均已验收并推送 |
| R2 | 个人访问保护、用户归属、密钥库；同一用户创建多个助手、真实列表、选择/普通切换 | 2026-09-05 正式验收通过：后端专项 53/53、全量 329/330（1 既有跳过），前端 98/98、198/198，真实页面证据见 R2 合同/日志；不以开发身份或固定 Profile 替代 |
| R1 | 使用 R2 正式身份与基础会话记录实现独立聊天、业务解耦和 Vio 自有适配边界 | 2026-09-05 正式验收通过；不要求真实 Engine 安装、启动或陪测 |
| R3 | 每助手多个会话、消息版本、编辑/重生成/删除/分支/恢复，跨用户与助手隔离 | 2026-09-06 正式验收通过；不是多助手协作 |
| R4 | 上下文装配、折叠、跨窗口、来源和预算 | 未开始 |
| R5 | Vio 本地记忆的检索、管理、引用、导入导出与删除 | 未开始 |
| R6 | 真实模型/API、MCP、Skill、Plugin、Tool 及统一执行记录 | 未开始；原真实功能验收不减 |
| R7 | 原有五档权限、确认、撤销、暂停、取消、紧急停止、审计强制生效 | 未开始；不增加安全栏 |
| R8 | 公共页面状态、功能矩阵、事件总线；截至 R7 已完成模块的真实前端接线 | 未开始；后续模块元素只能标待施工，不计入完成 |
| R9 | 设备前端、后端、真实手机及至少一个非手机设备/网关联调 | 未开始；本阶段完整验收对应元素 |
| R10 | 项目/工作流/私域/生活模块前端、后端与真实 AI 互动 | 未开始；不留整块页面到 R13 |
| R11 | 后台/提醒/费用/导出/备份恢复等对应前端、后端与真实联调 | 未开始 |
| R12 | VPS 网络安全墙、真实部署、监控、备份恢复和运维 | 未开始；内部运行时不裸露公网 |
| R13 | 全量功能矩阵复验、真实用户旅程、生产演练和发布交接 | 未开始；不能把此前未施工模块补算已验收 |

六个导航保留：工作台、对话、连续性、AI 私域、能力、我的。每个元素在矩阵中标明归属阶段；登记、占位、禁用或 mock 不证明完成。同一用户多助手由 R2 实现，各助手多会话由 R3 实现；至少两名真实用户、每名至少两个助手、每助手多个会话，验证跨用户及同用户跨助手不串设定、版本、记忆、Context、私域或调用归属。助手市场、多助手协作、复杂协作切换不扩展。

### 历史 v2.4 阶段快照（不再作为当前施工顺序）

以下原 0—11 表保留历史来源，其中“接入连续性引擎”等不能解释为当前 Vio 发布必做项；当前只按上表及已确认监工规划执行：

| 阶段 | 主要任务 | 完成标准 |
| --- | --- | --- |
| 0. 整理现状 | 冻结核心代码、修正文档、确认测试 | 知道已有内容，不重复建设 |
| 1. 产品蓝图 | 明确核心页面与流程 | 页面和按钮流程明确 |
| 2. 后端数据设计 | 用户、主体、事件、消息版本、记忆、权限、设备和私域 | 每类数据知道存在哪里 |
| 3. 平台后端 | 登录、数据库、事件总线、模型路由和密钥管理 | 手机前端可以安全调用 |
| 4. 前端第一版 | 完成核心页面和手机布局 | 一个用户可以完整使用 |
| 5. 接入连续性引擎 | 状态、事件、摘要、跨窗口和重置版本 | 连续性真实生效 |
| 6. 扩展能力 | 至少一个 MCP 或 Tool | 完成一次真实调用闭环 |
| 7. 设备中心 | 统一接口和一个设备适配器 | 至少一种设备可安全读取或控制 |
| 8. AI 私域 | 数据层、版本、安全读取和导出预留已完成；申请、开放、拒绝和删除待后续 | 基础可验证，完整规则未完成 |
| 9. 生活模块 | 管账、预算、月历、身体管理和本地记忆基础已完成；真实 AI 互动/提醒/同步未完成 | 本地数据与安全闭环可验证 |
| 10. 公开测试 | 用户隔离、备份、监控、隐私和安全 | 少量用户不会串数据 |
| 11. 开源发布 | 许可证、贡献指南、部署和安全说明 | 其他人可以部署、理解和贡献 |

## 当前阶段

R2 已于 2026-09-05 正式验收通过，采用个人自用身份：本机管理员生成一次性邀请，浏览器以个人口令建立服务端会话；邮箱、Google 与公开注册暂缓。资料、多助手、会话撤销、访问审计/诊断、Provider/Model、加密凭据和 `/models` 认证检查已接真实页面。自动化与浏览器使用受控临时数据和 loopback Provider，不代表真实供应商、HTTPS、云服务器、备份或公开部署通过。账户/空间删除政策已确认，迁移 `024`、删除流程、自动化证据与真实页面证据均已完成验收。

R1 后端在该真实个人身份基础上建立每助手唯一默认会话与 `/api/v1/personal/chat/*` 合同。它由 Vio 自行完成默认 chat 模型/Provider 选择、Vault 凭据解析、Permission/Security/Token Budget 门控、OpenAI-compatible 调用和锁定结果发布，并用迁移 `025` 记录 turn、logical execution、逐次 Provider attempt、usage/cost、result 和 recovery action。后端专项、受影响回归及默认全量已在隔离启动器下完成；六导航“对话”已接入该合同，并以临时 SQLite、两个助手和受控 loopback Provider 完成真实页面闭环。总体协调窗口于 2026-09-05 正式验收 R1 通过；历史 F1 页面不能冒充 R1 证据。

R1 查询、刷新和启动不执行模型；只有用户新建轮次或明确安全恢复才能进入调用。确定未发送/明确 retryable 可在同一逻辑 execution 下新增 attempt，可能已发送的 `outcome_unknown` 保持 fail closed，已锁定结果只恢复发布。施工期间先以 test-support 受控注入解决 loopback 测试目标而不放宽生产 SSRF 保护，并修正 retry 发送边界；后续审计又补齐预算复验、个人 session/删除状态和 credential 调用边界、owner 范围 Idempotency-Key 与 pending recovery 重放。最终后端证据为 R1 61/61、受影响 V1—V5 组合 128/128、默认全量 399 项中 398 通过且 1 条既有 RFC 隔离跳过（未执行、不计通过）。

删除出口按 [R2 个人合同](../../backend/docs/R2_PERSONAL_CONTRACT.md) 验收：高风险确认后 7 天限权撤销窗口、到期真实执行与失败恢复、实际删除后 14 天受管备份处理和 30 天最小凭据。在线数据、受管文件、备份等待及 SQLite/WAL 必须分别报告；用户自存副本不在管理范围内。本项不代替 R3 单消息删除或 R11 完整备份，未执行的测试/实机/外部验证不得计为通过。

历史 R0 文档与验收材料收尾轮只检查规则和证据、未运行代码回归、共享测试或服务；其记录见 [R0矩阵](../../backend/docs/SUBJECT_RUNTIME_PORT_V1.md#r0-evidence)。随后 R0 已完成独立整体验收，该历史限制与当时结果继续保留。

### 历史 V1—V5、F1、L1 与 S4-Live 成果

截至 2026-08-14，已完成 V1–V4、S2/S3/S4 共享验收、Vio V5 固定本地 Profile 的公共 Conversation Turn API、F1 对话页接线、L1 安全准备入口和首次 S4-Live 真实供应商试聊。V5 通过独立 `022` 账本关联用户消息、V1 请求、V3/V4/V2 状态和唯一 Engine 最终主体回复，并在请求前后、V2 完成后和消息发布后的崩溃窗口恢复；F1 只消费该公共 API。首次真实验收使用不可晋升的可销毁测试身份和独立短路径沙箱，不能替代真实认证、正式数据库、通用 Binding 或生产部署。

### 已完成

- Vio 与 Continuity Engine 的平行系统边界、SubjectState 唯一权威、PlatformObservation/内部 Event 隔离和最终 Context 单一组织方已经对齐。
- User Private Data、AI Private Space、Shared Space 三层数据空间的长期边界已经接受，但实际跨系统读写明确不参加第一轮最小连接测试。
- 第一轮三份严格 Schema、正式 conformance vector、固定 SubjectBinding fixture/hash、requestHash、幂等错误、revision 冲突、最小投影和 ContractTestAdapter 入口语义已经闭合。
- Continuity Engine 已正式接受 v1.1，并允许双方进入工程档案同步。
- Engine E1/E2/E3 与共享 JSONL Runner 已在提交 `7a32a99` 实现，并由 Vio 独立共享测试实际启动。
- Vio V1 已实现三份严格本地 Schema、RFC 8785/hash、固定 Binding 测试装载、请求构造和请求输入跨重启恢复。
- Vio V2 已实现固定 success/error envelope 白名单、不可覆盖 result ledger、同 revision 内容唯一性、非空 engineUpdateId 唯一性、CAS 指针、reconciling/quarantine 和三阶段崩溃恢复。
- Engine E4 已提供只监听本机的正式 HTTP/JSON 服务；Vio V3 已提供对应 transport、Bearer token、独立 timeout、响应上限、六态 outbox、attempt 和 query-first 恢复，且不重建 V1 请求、不复制 V2 结果。
- S2/S3 正式本机 HTTP shared tests 15/15 通过，验证 revision 0→0、0→1、1→1、四类机器错误、completed/not_found/recovery_required 查询、响应丢失、Engine/Vio/双方重启与 hash/operationId 冲突隔离；Engine 修复后的 Wake、Thinking、Event、StateUpdateRecord 和 revision 以及 Vio result/projection/receipt 均保持幂等。
- Vio V4 已实现 E5-A 三份严格 Capability Schema、独立 inbox/多 execution/逐 attempt usage/result/outbox/attempt/incident 账本、受限环境变量 secretRef、现实层门控与 `openai_compatible` 非流式 HTTP adapter。模型候选只能先回传 Engine，Vio 不直接生成最终 Message 或状态更新。`FAILED_RETRYABLE` 被 Engine 接受后进入 `waiting_retry`，只有内部明确批准且重新通过路由、权限、安全、预算与 deadline 才能产生新调用；`UNKNOWN` 被明确接受后进入 `provider_outcome_unknown`，在没有 Provider 对账能力时保持 fail closed。
- Vio V5 已实现固定本地试聊 Profile 的公共 turn create/get/resume、严格 `Idempotency-Key`、单主体活动轮次、纯查询、显式确认/预算/retry 恢复，以及 V2 Engine response 到唯一主体 Message 的可恢复发布。V5 启动只做本地对账，不触发 Provider；V1 创建前后的中断由客户端以相同幂等键重放后继续。

### 既有专用链路的历史实现（与 R1 独立链路并存）

- Vio V1 逻辑请求仍由真实平台事实构造、校验和持久化；Vio V3 从该账本读取首次原请求，只有显式启用时才通过正式本机 HTTP transport 交付，结果仍由 Vio V2 保存。默认配置不创建 Engine 网络客户端。
- Vio Event、PlatformObservation、引擎 Event 和 StateMutation 保持结构与职责隔离；Vio V1/V2 不创建 Engine Event/StateMutation，也不推进 legacy SubjectState。
- 双方第一轮 test-only、正式本机 S2/S3、S4 及 V5 公共轮次 shared test 均已通过，F1 已接入 V5 公共 API。2026-08-14 首次真实供应商试聊也已在固定可销毁身份与短路径独立沙箱中通过；该结论仍不表示通用身份或生产产品入口已经验收。
- 首次 S4-Live 使用真实 Alibaba Cloud Model Studio OpenAI-compatible Provider 与 `qwen-flash-2025-07-28`，恰好一次 execution 报告 177/9/186 Token；CapabilityResult 首次回传成功，Turn completed，Engine `changed=false / revision=0`，无重复 execution/result/Message、incident 或 outcome_unknown。最终 Message 只来自 Engine FirstRoundSuccessResult。

### 下一阶段

- R0、R2 与 R1 已完成正式验收；当前施工阶段为 R3。R3 前后端实现、隔离自动化和受控页面闭环已完成，仍须由总体协调窗口完成阶段验收。V5/F1/L1/S4-Live 的测试身份与已删除沙箱不得转为正式身份或公开部署依据；R3 验收前不得开始 R4。

### 尚未实现

- R3 受控本机联调不等于真实供应商、手机实机、云部署或跨设备同步验收，这些项目仍未执行。
- 手机实机、HTTPS、云部署、跨设备云端同步、R11 完整备份及真实供应商验证；这些均未计入 R2 已执行项。
- 通用外部主体运行时身份/Binding 与真实接入；它们另行处理，不是 Vio 当前完成或发布的前置条件。
- 公开注册、邮箱/Google 登录、生产多租户、远程部署与运维对账；邮箱、Google 和公开注册按当前个人使用决定继续暂缓。

历史第一阶段资料见 [`../../backend/docs/第一阶段实施路线.md`](../../backend/docs/第一阶段实施路线.md)，当时依次覆盖以下内容；不是现行 R0-R13 的替代顺序：

1. 范围冻结与工程边界
2. 技术决策准备
3. 领域与数据设计
4. 契约设计
5. 安全与测试设计
6. 进入实现前审查

当前已完成工程边界、核心领域数据设计、账号/主体、线性对话版本、事件、Provider/Model 路由、扩展/设备注册与未执行准备、Permission/Policy/Confirmation/AuditLog、AI Private Space/ContentVersion、安全投影，以及统一跨模块 Export Schema/记录。Node.js、开发 SQLite、不可变版本、独立私域存储、导出安全资格与真实生成/迁移分离、秘密禁止写入、权限默认拒绝和高风险确认均已有 ADR；分支/重置、私域披露与删除、真实导出/下载、正式数据库、生产加密、真实能力/设备/机器人、认证、备份恢复、并发和部署仍需后续决策。

## 第一版必须完成

- 账号、数据库和用户/主体隔离
- 软件事件总线
- 消息版本、删除、分支和重置记录
- 上下文摘要、跨窗口和本地记忆基础
- 模型/API 路由基础和手动选择
- 权限五档、撤销和访问日志
- AI 私域基础和数据导出
- 至少一个 MCP 或 Tool

## 第一版暂缓

- 全品牌家电接入
- 无限后台扫描和无限 Token
- 复杂多用户协作编辑
- 自动永久人格变化
- 完整自动费用优化
- AI 无限制自动执行
- 真实转账、代扣和账户冻结
- 插件商店和商业分成

## 部署与运维准备

总规划要求公开测试前具备：

- 用户和主体隔离
- 数据库备份和恢复
- 错误与使用日志
- 运行监控
- 数据导出和删除
- 隐私与安全规则
- 密钥不进入 GitHub

现行 R12 已要求 VPS、开发/测试/生产隔离、网络安全墙、TLS、受控端口、监控及备份恢复；具体实现按该阶段审批，不在本轮选择或施工。原权限、撤销、暂停、取消、紧急停止及审计保留，不新增安全栏。

## 测试范围

以下保留既有模块回归职责，功能未完成项按上表所属阶段验收。通用端口与 Vio 自有适配边界测试必须完整；真实引擎或同类可选运行时联调仅在用户另行要求时进行，不作为 Vio 完成或发布门槛。首次彻底解耦后，没有重连授权就不探测、读取、启动或修改真实引擎；R0-C 历史目录检查授权不长期延续。本轮下列测试全部未执行。

### 数据隔离

- 不同用户不能读取或修改彼此数据。
- 同一用户的不同主体不会混用状态、记忆、私域或设备权限。
- 每个用户只有一个 User Space；首个助手选择、当前助手切换和重启后指针必须稳定，且不能改变任何助手数据归属。
- 所有权检查必须先按固定 `user_id + assistant_id + resource_id` 复合条件查询；错配组合按未找到处理，高敏感资源命中后仍必须通过 Permission 与 Security Policy。
- AI Private Space 查询必须同时限定用户、助手和 Space；User Space 与私域使用独立表，错误组合不得泄露内容存在性。

### API 与前后端连接

- 所有 JSON 接口稳定返回 `success`、`data`、`error` 和 `timestamp`。
- User、Subject 与 Dashboard 接口保持用户归属隔离；Subject 更新与 `subject_updated` 事件原子提交。
- 前端开发服务器可以通过同源代理访问后端健康检查，后端不可达不会阻塞页面启动。
- 当前用户开发请求头不得被误认为登录认证；页面 mock 未经后续迁移不得删除。

### 对话与事件

- 当前实现必须验证 Conversation/Message 复合归属、稳定顺序、编辑/重生成父版本链、当前版本和陈旧写冲突。
- 删除、分支和重置仍是未来验收项，不能由当前线性父版本链冒充完成。
- 软件事件不包含秘密原文。
- 对话事件不得复制标题或消息正文；Event 失败必须回滚消息和版本写入。
- 窗口重置不自动删除连续状态。
- ConversationSummary 必须至少引用一个同范围 MessageVersion 或 Event，跨窗口读取必须排除当前 Conversation 且不能跨主体。
- legacy/unverified SubjectState 的历史及来源归属必须保留，普通写入口由 R1 收口，不形成外部状态权威。仅在另行授权的真实 Engine 接入验收中，才要求其专用 revision/update ID 去重、乱序隔离和唯一权威；这些专用规则不是 Vio 日常发布必须启动真实引擎的理由。
- Assistant Global Settings 必须与 Subject 一对一、按用户/主体隔离；七类字段更新、无变化语义、Event 原子性和重启持久化必须验证。
- Global Settings 更新不能创建、覆盖或切换 SubjectState；长期要求和禁止事项不能替代平台安全规则。
- 现有 Context 公共 API 只读投影并保持未实现标记真实；独立模式的自有 Context 与记忆由 Vio 按 R4/R5 完成。只有既有 Engine 专用合同的最终认知 Context 由该引擎组织，不能把此规则套到独立模式。
- 摘要/来源和状态/未解决事件/当前指针任一写入失败时必须整体回滚。
- 通用连接边界在 Vio 自有环境验证幂等、断线/超时/降级/恢复、前端不直连和归属隔离；专用 Observation/StateMutation、Engine revision 对账的既有历史证据保留，真实引擎重连另行授权，不用测试替身冒充真实接通。

### 权限与安全

- 五档权限行为符合定义。
- 会话授权按时失效。
- 高风险操作需要具体确认。
- 撤销权限后调用停止。
- Permission 拒绝不能被确认绕过；长期允许不能跳过高风险确认。
- Security Policy 只能收紧 Permission；自动确认和 `session_allow` 不能跳过 `high` / `critical` 平台底线。
- `session_allow` 必须确认后建立，绑定用户、主体、策略版本、安全会话、资源、动作和风险，并按时失效。
- 策略/偏好变更、Permission 生命周期和确认请求必须产生对应最小审计或 Event。
- 确认不能跨用户、主体、资源、动作或权限版本复用，批准后只能消费一次。
- `allow_once` 在确认前不消费，安全链满足后才消费。
- 审计只保存最小字段，不保存秘密原文或任意请求正文。
- 私域 `read`、`write`、`manage`、`export` 必须经过 `private_domain` Permission 和高风险确认；Policy 拒绝时不得读取或写入正文。

### 模型与扩展

- 模型切换不影响主体状态。
- 六类任务的默认/备用规则必须保持用户归属与能力匹配，显式规则不可用时不得伪造成功。
- 历史 Provider/Model 测试状态不得被当作真实连接证明；R2 独立连接测试记录只证明当次受限 `/models` 认证/网络检查，不证明生成能力；Router 选择响应仍明确未执行模型与外部 API。
- API Key、Token 和 Secret 原值不得进入业务请求、SQLite、响应、日志或 Git；受限 secretRef 只能进入独立 credential binding，不能进入 Provider 公共投影或 Capability 外部 JSON。
- 打开应用不自动消耗 Token。
- Wake `voice` 规则不得被解释为麦克风或系统授权；启用必须有应用内授权，撤销后必须停用。
- 主动提示必须由同范围 Event 触发；静默优先级必须抑制投递，需要确认的规则不得绕过高风险逐次确认。
- Token Budget 必须分别限制 UTC 日与预算会话；阻止、延后和要求确认三种超额策略结果可验证。
- 旧显式 Token 使用记录必须保持平台未调用、未计费语义；V4 Provider 实际 usage 进入独立可信账本，预算检查本身仍不得写入用量或触发模型。
- 后台 `idle` / `active` 与限制只控制准备资格；当前不得启动调度器或驻留任务。
- 外部能力失败不会被伪造成成功。
- 推荐能力不会绕过执行权限。
- Registry 启用不得被解释为 MCP 已连接、Plugin 已安装或 Skill/Tool 可执行。
- Capability Permission 预览不得消费 `allow_once`，且跨用户、跨主体能力不得泄露。
- Tool 准备必须按 Permission 与 Security/Confirmation 处理并关联审计；当前执行状态必须保持 `not_executed`，不得接受真实执行载荷。

### 设备适配

- 七类 Device Registry 与四类 Device Capability 必须按用户隔离，品牌只决定未来 Adapter 类型，不表示已支持。
- 注册和启用不得被解释为物理连接或状态已读取；响应必须保持 `not_connected`、`not_observed`。
- 主体设备授权必须复用 Permission，且不能跨用户；操作准备必须经过 Permission、Security 与 Confirmation。
- 设备操作当前按极高风险每次确认；通过准备后仍必须保持 `not_executed`，不能接受控制参数或调用厂商 API。
- `device_changed`、DeviceOperationLog 与 AuditLog 必须只保存最小事实并保持事务一致，不得写入外部设备 ID、位置、凭据、实际状态或敏感参数。

### 数据治理

- 五类私域内容必须不可变追加并保留父版本，Event、AuditLog 和 Export Manifest 不得复制正文。
- 私域 Context 必须保持独立安全入口并明确零模型/外部/连续性调用；Export Manifest 当前不得生成文件。
- Export Schema 必须版本化并覆盖十二类稳定范围；导出范围与用户选择一致。
- 创建导出记录必须检查用户/主体归属、必需字段与外键，且记录不得复制业务正文。
- 导出准备必须使用精确 `data_export:export` Permission 并经过高风险逐次确认；`ready` 不得表示文件或迁移已完成。
- 机器人/其他载体契约必须保持未实现、未连接、未执行，不接受地址、凭据或业务数据。
- 备份恢复不破坏隔离关系。
- 删除账号、主体和单项数据的范围清晰。

## 阶段完成原则

- 每一阶段先规划、再实施、再测试、再记录和提交。
- 页面存在不等于后端能力完成。
- 本地 JSON 或模拟数据不等于公开运行数据库。
- 底层连续性原型不等于平台后端完成。
- 未通过安全和隔离测试，不进入公开测试。

## 下一步边界

R0 已由总体协调窗口验收。R2 个人身份、多助手、凭据库、受控连接检查与账户删除，以及 R1 独立聊天均已于 2026-09-05 正式验收通过。R3 多会话实现和受控证据已于 2026-09-06 通过总体协调窗口正式验收；历史首次试聊和状态卡不能替代 R3 或后续完成证据。
