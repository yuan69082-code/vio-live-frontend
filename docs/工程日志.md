# Vio Live 工程日志

## 2026-09-08｜本机个人初始化与桌面运行配置补修（待总体协调验收）

- 真实桌面首次准备/打开后暴露交付缺口：页面需要初始化邀请，但原正式 CLI 只能初始化仓库外数据库，本机入口又固定现有仓库内个人库。新增中英文个人初始化双击入口，不改正式 CLI 的仓库外安全规则；新入口必须读取严格绑定，确认 Vio 已停止，只操作绑定的既有 Vio 数据库，并通过正式个人身份服务交付 15 分钟邀请。
- 邀请只写当前 Windows 用户 `%LOCALAPPDATA%\VioLive\private` 的新文件，不回显、不入日志；未过期文件重复点击复用，过期或文件丢失可受控换新，写文件失败与数据库事实一起回滚。邀请码与 12—256 字符个人访问口令明确分离，口令只由本人在页面设置。
- 此前施工工具视图生成的 AppData 配置在真实桌面 Node 下实际为 `ENOENT`，证明工具视图产物不能替代桌面验收。补修后配置读取区分缺失、不可读、非法 JSON，`status` 不再绕过配置校验；测试路径在显式 test mode 下使用临时私密目录和随机 loopback 端口，生产仍固定 5173/8787。
- 过程结果保留：首轮扩充专项在已输出的 12 项中有 3 项失败、未形成有效总计；隔离端口后 13/14；补齐测试导入后功能断言通过但暴露测试只读 SQLite 句柄未在失败路径关闭，修正关闭边界后最终 **14/14**。R2 相关组合 **7/7**、前端 **332/332**、后端默认全量 **478 项：477 通过、0 失败、1 条既有 RFC 隔离跳过**，TypeScript/构建通过。所有个人初始化验证均使用临时 SQLite；没有触碰真实个人库或用户当前运行实例，没有 Engine、Provider、真实密钥、业务公网或费用。R7 未开始，本补修未提交、未推送。

## 2026-09-08｜本机启动交付补修（待总体协调验收）

- 基线 `ef24467ecdd7235cdc7e09aa5ec8c67bd4ece351` 下确认根 `node_modules` 的链接指向已缺失的 `.pnpm` 内容，Vite、TypeScript 和 Vitest 入口均不完整。经用户明确授权，仅删除并重建该仓库根 `node_modules`；使用本机已缓存 pnpm 11.9.0 执行 `install --offline --frozen-lockfile --force` 成功，未在线回退、未升级依赖，`pnpm-lock.yaml` SHA-256 前后均为 `2f4c298050b8ff41cc088d835bc6e6ccf2fe16247d8e512f165c18eceb382d42`。
- 新增独立首次准备和 Windows 中英文双击打开/停止入口。首次准备严格禁用 Corepack 网络，固定 pnpm 11.9.0，重建依赖后执行 TypeScript 与生产构建；日常打开不执行依赖安装、升级或构建。运行绑定锁定当前仓库和现有 `backend/data/vio-live.dev.sqlite`，不替换为空库；实际个人数据库没有用于破坏性验证。
- 单一运行进程同时托管 Vio 后端与已准备前端，只监听 127.0.0.1；同源 `/api`/`health` 代理只接受本机 origin-form 目标。重复打开依靠操作锁和所有权控制管道复用实例，停止只关闭经实例 ID/令牌证明属于本入口的进程。构建/绑定不一致、未知端口占用或所有权状态非法均 fail closed。
- 第一次执行正式 `首次准备 Vio.cmd` 暴露 Node 直接启动 `corepack.cmd` 返回 `EINVAL`，批处理的 `pause/endlocal` 又把真实失败吞成退出码 0；该次没有重建、构建或启动服务，个人数据库元数据未变。修正为由 `cmd.exe` 调用禁网 Corepack，并在所有中英文入口跨 `pause/endlocal` 保留原退出码；第二次正式执行返回 0，报告 `offline_frozen_rebuilt`，构建与绑定完成。
- 专项前两次均为 7/8：先发现中文批处理名在测试子进程代码页下不可移植，加入内容完全相同的英文同义入口；随后发现 Node 调用 `cmd.exe /s` 会破坏带空格绝对路径的引号。改为绝对路径与 verbatim 参数、补齐批处理真实退出码后，最终专项 **11/11**。正式首次准备退出 0，TypeScript 与 Vite 生产构建通过；前端全量 **332/332**，后端默认全量 **477 项：476 通过、0 失败、1 条既有 RFC 跨仓隔离跳过**。跳过项未执行、不计通过。
- 正式首次准备复用外部运行绑定并报告 `offline_frozen_rebuilt`；准备前后锁文件相同，个人 SQLite 的大小和最后写入时间完全相同。隔离专项真实覆盖并发重复打开只产生一个实例、第三次打开复用、停止、再次打开、数据 canary 保留、未知端口不被结束、非法所有权不被清理、绝对代理目标拒绝及失败退出码。本轮不访问 Engine、Provider 或真实凭据，不产生费用；R7 未开始，未提交或推送。
- 普通 Windows 权限下的隔离浏览器验收第一次因测试目录未建立运行绑定而按设计返回 `RUNTIME_CONFIG_MISSING`/退出码 1，未启动服务；使用与自动化相同的正式绑定函数重新建立全新临时夹具后，`Open Vio.cmd` 返回 `started / browserOpened=true`，前后端健康端点均为 HTTP 200，`Stop Vio.cmd` 返回 `stopped` 且隔离数据库仍存在。随后仅删除该次系统临时目录；5173/8787 无残留监听，现有个人数据库未参与启动测试。

## 2026-09-08｜R6 正式验收通过

- 总体协调窗口正式验收 R6：后端 R6 17/17、八文件迁移组合 50/50、默认全量 477 项中 476 通过/0 失败/1 条既有 RFC 隔离跳过；前端 R6 36/36、组件 17/17、全量 332/332，TypeScript/构建及受控页面闭环通过。唯一跳过未执行、不计通过。
- 下方待验收状态及全部首次失败、普通返修、浏览器 assistant 隔离缺陷和纠正证据保持历史原文。最终统一能力事实按 owner/current-assistant 隔离，真实 Engine、第三方 MCP/Provider、真实凭据、业务公网和费用均未进入验收。`PLANNING_CONFLICT = NONE`，`EVIDENCE_CONFLICT = NONE`；R7 未开始。

## 2026-09-08｜R6 统一能力执行闭环与助手隔离复验（待总体协调验收）

- R6 建立唯一公共合同和迁移 `029`：个人能力中心现在可真实安装/发现/执行受限 Tool、MCP、Skill 和 Plugin action，并把 R1 模型执行投影进同一历史；所有执行、确认、审计、retryable、`outcome_unknown`、usage/cost 和恢复事实不可变。生产 MCP 仅接受经过 DNS/IP 固定和重定向保护的公共 HTTPS，自动化的随机 loopback 明确不是第三方服务验收。
- 初始专项经历 8/13、14/14、15/15；迁移组合经历 25/30、28/30、30/30，另有 67/68 SQLite busy、人工误列不存在文件实际执行 0 项、Node 24 非接受性 469/475 和正式 Node 22 迁移修正前 469/475。每项失败与修复均保留，不以最终成绩倒写历史。
- 真实页面首轮暴露同一 owner 的 assistant 间共享安装定义。返修后，R6 Tool/MCP/Skill/Plugin 的定义/版本、发现、依赖、生命周期、执行/历史、by-key、重启恢复和删除全部按 `owner + current assistant` 隔离；同一 owner 的两个 assistant 可各自安装同名能力并复用操作键，事实仍不同。R2 Provider/Model 保持账户级配置。返修测试一次 14/15 为公共 envelope 取值错误，跨 owner HTTP 一次 3/4 为测试 token 长度错误，修正测试后未发现产品回归。
- 后端最终为 R6 **17/17**、八文件迁移组合 **50/50**、Node 22.23.1 / Corepack pnpm 11.9.0 默认全量 **477 项：476 通过、0 失败、1 条既有 RFC 隔离跳过**；跳过项未执行、不计通过。前端最终为 R6 **36/36**、组件 **17/17**、全量 **332/332**，TypeScript/构建通过。受控页面覆盖 Tool、MCP、Skill、Plugin、生命周期、丢响应恢复、UNKNOWN、同库重启、同 owner 双助手与跨 owner 隔离；390px 无溢出、console 0/0，夹具最终整根清理。
- 本轮只使用临时 SQLite、受控 loopback Provider/MCP 和非真实凭据；真实 Engine、真实第三方 MCP/Provider、真实密钥、业务公网与费用均为 0。`PLANNING_CONFLICT = NONE`，`EVIDENCE_CONFLICT = NONE`。实现与证据交总体协调窗口验收，不自行宣布 R6 正式通过；未 add/commit/fetch/push，R7 未开始。
- 最终环境核对中一次在仓库根运行的 `corepack pnpm --version` 因无法在该位置解析固定版本而尝试查询 npm registry；受限环境在连接前以 `EACCES` 阻止，未取得响应、未安装依赖、未发送业务数据，也不计验收通过。正式测试所用 pnpm 11.9.0 来自 `backend` 内已完成的接受性运行。

## 2026-09-08｜R5 本地长期记忆闭环与复验收尾（待总体协调验收）

- R5 建立 `vio-local-memory/v1` 与迁移 `028`：记忆按 R2 owner 和当前 assistant 隔离，以稳定 memory ID、不可变 version、精确 MessageVersion/Event 来源和独立 operation/import/export/deletion 账本保存。检索完全本地且确定性；读写复用原 Permission、Security、Confirmation 和 Audit 边界，页面读取不调用模型或外部服务。
- R4 原 `long_term_memory` 槽现在只接收 active、明确允许参与 Context 且通过安全链的当前记忆版本，并锁定 memory/version/source hash；后续编辑、归档和删除不能改写既有 turn。R5 导入导出不是 R11 备份或云同步，也不与生活模块、AI 私域或外部运行时 Memory 合并。
- 后端当前证据为 R5 **13/13**、受影响 R1/R3/R4 **109/109**、历史迁移兼容组合 **46/46**；首次 106/109 与各轮专项失败/修复保留。默认全量首次误在不受支持的 Node 24.19.0 运行，460 项中 458 通过、1 失败、1 跳过，暴露 R5 启动恢复在 SQLite 真实写锁下未延后；只对 busy 延后并由受影响测试 3/3 验证后，正式 Node 22.23.1 / Corepack pnpm 11.9.0 全量为 **460 项：459 通过、0 失败、1 条既有 RFC 隔离跳过，退出 0**。
- 真实页面联调随后发现 R4 公共 `selection` 多返回两个 R5 仓储计数字段；收口公共五字段后，旧 recovery 首次 11/12、十文件组合首次 108/109，定点修正旧测试对内部/公共 DTO 的错误等同比较后分别 **12/12** 与 **109/109**，R5 再验 **13/13**。另一次十文件命令因人工误列不存在文件而启动前退出、执行 0 项，按操作错误保留。前端窗口最终报告 MemoryManager **13/13**、受影响七文件 **91/91**、全量 **303/303**、TypeScript/构建通过；受控页面覆盖两条记忆、四版本、owner/assistant 隔离、原键丢包恢复、同库重启和一条锁定 memory 来源。390×844 仿真无横向溢出、console 0/0；导出从真实浏览器不可靠的自动点击修正为页面内存中的显式 Blob 下载链接，同一链接经浏览器专用读取成功且没有第二次服务端调用。普通 `download` 事件未上报保留为工具边界，不冒充事件证据。
- 全部自动化和页面证据只使用临时 SQLite、受控本地依赖及 loopback Provider；唯一一次模型执行为受控 loopback，真实 Engine、真实供应商、真实凭据、业务公网和费用均为 0。未 add/commit/fetch/push，未开始 R6；当前只建议交总体协调窗口阶段验收，不自行宣布正式通过。

## 2026-09-08｜R4 验收返修、最终证据与正式验收通过

- 在不改变 R1/R3 单一 Provider execution、既有身份/归属或 `027` 迁移编号的前提下，R4 补齐当前用户消息相关性排序、provisional/final 选择依据、最新仍有效 summary、原来源 hash 复核、失效排除项调和和长 preview 折叠/裁剪。隐藏、删除、分支变化或撤权后的正文证据读取 fail closed；锁定快照的 ref/hash 历史保持不可变。
- summary builder 输入使用不可变克隆，输出严格限制字段、scope、sourceRefs 和时间；五类恶意/错误输出均在 Provider 前拒绝。失败 assembly 的 candidate 来源不会被覆盖，恢复把 summary、locked assembly 与 locked sources 原子提交；注入数据库失败会完整回滚并可由新幂等键安全重试。
- 最终后端：R4 27/27、受影响 R1/R3 flow 61/61、R1/R3/027 migration 19/19；Node 22.23.1 / Corepack pnpm 11.9.0 默认全量 447 项中 446 通过、0 失败、1 条既有 RFC 隔离跳过，退出 0。此前 15/15、首次 migration fixture 失败、修复后的 435 项和本轮 Node 24 非接受性中间运行继续保留，不倒写为从未发生。
- 最终前端：R4 **42/42**、全量 **275/275**、TypeScript/构建通过；全新受控浏览器覆盖长历史折叠与带 hash 摘要证据、连续轮次 `planHash` 刷新、相关度排序、自定义排除与失效清理、响应丢失原键恢复、同库重启/Vault 解锁、双用户/双助手隔离及 390px 无溢出，最终新标签页 console 0 error/0 warning。返修前 35/35、268/268、首次 34/35 测试时序失败均保留；浏览器首次发现第二轮复用旧 plan 导致 `CONTEXT_PLAN_STALE`，修复为消息/终态变化后重读 plan，同时继续显示上一轮不可变锁定快照。
- 自动化与页面均使用临时数据、隔离注入和 loopback Provider；真实 Engine、真实供应商、真实凭据、业务公网、手机实机、部署和费用未执行。`PLANNING_CONFLICT = NONE`，`EVIDENCE_CONFLICT = NONE`。总体协调窗口已于 2026-09-08 正式验收 R4；R5 未开始。本条提交前未暂存、提交、fetch 或 push。

## 2026-09-07｜R4 上下文装配闭环完成（待总体协调验收）

- R4 在已验收的 R2 身份、R1 独立执行和 R3 多会话之上新增 `027` 账本与 `vio-context-assembly/v1` 合同。每个 turn 只锁定一份有 hash 的 Context snapshot，并复用原 standalone execution；查询、预览、刷新、启动和恢复查询不调用 Provider 或外部运行时。
- 装配固定保存系统规则、当前助手设定、可选已验证运行时投影、未解决 Event、当前/同助手跨窗口原始 MessageVersion、R5 memory 未实现槽和当前用户消息。来源归属/可见性/权限、模式/排除、预算/裁剪、summary 来源、失败 fallback 与显式恢复都有可审计事实；R5 未施工。
- 后端专项 15/15，受影响 R1/R3 flow 30/30、migration 19/19。默认全量首次 434 项中 5 项失败，原因是历史迁移夹具误把新 `027` 纳入旧边界；修复夹具并加入 plan hash 边界回归后，接受版本最终全量为 435 项中 434 通过、0 失败、1 条既有 RFC 隔离跳过，退出 0。首次失败和修复经过保留，跳过项未执行、不计通过。
- 一次中间全量由桌面捆绑 pnpm 误用未受支持的 Node 24.19.0，虽通过但只记作无效运行；接受证据重新使用项目声明的 Node 22.23.1 / Corepack pnpm 11.9.0，未升级依赖或放宽 engines。
- 严格前端解析先发现 preview 泄露内部 `budget.source`，受控页面运行时开关随后发现最近事件在 SQL 限制后才过滤消息事件，令合法 `expectedPlanHash` 因当前用户消息落盘而变陈旧。两项均在后端合同边界内修复：公共 DTO 只保留八个预算字段，事件排除在 `LIMIT 20` 前完成；新增真实边界回归证明计划 hash 稳定且 Provider 仅执行一次，伪 stale hash 仍拒绝。
- 前端专项最终 35/35、全量 268/268、TypeScript 与生产构建通过。受控浏览器在全新临时库验证标准/custom 快照、精确 event/message 证据、owner/assistant 隔离、一次响应丢失原键恢复、同库重启、运行时投影开关/独立模式拒绝和 390px 零横向溢出；控制台 0 error/0 warning，完整交接见 [`R4_FRONTEND_HANDOFF.md`](R4_FRONTEND_HANDOFF.md)。
- 全部自动化与页面验证只使用临时数据库、隔离依赖和随机 loopback Provider；未读取/启动/修改真实 Engine，未使用真实凭据或真实供应商，业务公网和费用为 0。当前实现交总体协调窗口验收，不自行宣布 R4 正式通过；不开始 R5，不 add/commit/fetch/push。

## 2026-09-06｜R3 正式验收通过

- 总体协调窗口依据冻结合同、迁移 `026`、R3 后端 21/21、受影响 V1—V5 128/128、默认后端全量 420 项中 419 通过/0 失败/1 条既有 RFC 隔离跳过，前端 R3 29/29、全量 253/253、TypeScript/构建，以及两个隔离 owner、每人两个助手的受控本机页面闭环，正式验收 R3 通过。唯一跳过项未执行、不计通过。
- 下方待验收记录、所有首次失败、普通返修、响应丢失、RFC 误探测及纠正过程继续作为历史原文保留。正式结论为 `PLANNING_CONFLICT = NONE`、`EVIDENCE_CONFLICT = RESOLVED`；这不覆盖历史，也不把测试替身或 loopback Provider 写成真实外部验收。
- R3 验收不包含真实 Engine、真实供应商、真实凭据、业务公网、手机实机、云部署、跨设备同步或 R4。本轮只同步验收档案，不重跑测试、不开始 R4，也不执行暂存、提交、fetch 或 push。

## 2026-09-06｜R3 多会话实现、回归与受控页面闭环完成（待总体协调验收）

- R3 已冻结独立合同并完成前后端实现：每助手多个隔离会话、服务端当前选择、目录筛选/搜索/排序/游标、重命名/归档/恢复/删除、MessageVersion 版本选择、分支/隐藏/清空、明确重新生成、受管附件、单会话 JSON/Markdown 导出和可查询 operation。迁移 `026` 原位登记 R1 默认会话及锁定事实，不改写 R1 或历史 Engine 专用链路。
- 后端 R3 四文件专项最终 21/21，受影响 V1—V5 八文件 128/128；默认全量 420 项中 419 通过、0 失败、1 条既有 RFC 跨仓对照因隔离路径不存在而未执行、不计通过。前端窗口报告 R3 29/29、全量 253/253、TypeScript 与生产构建通过。首次 9/13、10/13、11/13，operation 16/20、managed-copy 22/25、前端 26/27 与补验定点 19/20 的失败和定点修复均在后端开发日志/前端交接保留。
- 受控本机页面使用两个完全隔离 owner、每人两个助手、临时 SQLite/附件根和随机 loopback Provider，逐项验证会话目录/分页/生命周期、消息版本/分支/隐藏/清空、明确重新生成、三类附件与拒绝、单会话 JSON/Markdown 导出、待确认轮次终止、响应丢失原键恢复和同库重启。四个 owner/assistant 作用域互不泄漏；指定删除动作刷新后仍以服务端事实为准，不冒充物理擦除。
- 页面补验修复 429 后残留确认和重新生成响应丢失后旧批准按钮。真实丢失一次服务端已处理响应后，Provider 调用只增加 1 次且按原键恢复；390×844 无横向溢出，console 0 warning/0 error。本次没有真实 Engine、真实供应商、真实凭据、业务公网或费用。临时服务已停止，夹具整根清理返回 `fixture_removed=true`；导出下载属于用户侧文件，未擅自删除。
- R3 已整理到建议阶段验收和适合提交的工作区状态，但本条不自行宣布正式验收；`PLANNING_CONFLICT = NONE`，手工 RFC 误探测造成的 `EVIDENCE_CONFLICT = RESOLVED`。未暂存、提交、fetch 或 push，未开始 R4。

# 2026-09-05｜R1 正式验收通过与统一 Git 收尾

- 总体协调窗口已正式验收 R1：真实个人会话、服务端当前助手、每助手唯一默认会话、Vio 自有模型执行/恢复、迁移 `025`、个人“对话”页、隔离自动化及受控页面闭环均完成。
- 下方“待总体协调验收”标题、首次失败、RFC 越界误探测与后续隔离复验均保留为当时历史，不倒写。R1 当前 `PLANNING_CONFLICT = NONE`、`EVIDENCE_CONFLICT = RESOLVED`。
- R3 尚未开始；真实 Engine、真实供应商、真实凭据、手机实机、云部署与跨设备同步均未纳入 R1 验收。

## 2026-09-05｜R1 前后端真实本机闭环完成（待总体协调验收）

- 前端依据唯一后端合同把登录后六导航“对话”切到个人独立聊天：真实读取当前助手默认会话，创建/按 turnId/按幂等键查询，并对 confirmation、明确安全 retry、cancel 和未知结果执行合同恢复；不发送固定身份或消息正文恢复缓存。历史 F1 固定 Profile 页面只保留为非个人入口证据。
- 前端最终 R1 专项 26/26、全量 224/224、TypeScript 与生产构建通过，来源为 [`R1_FRONTEND_HANDOFF.md`](R1_FRONTEND_HANDOFF.md)。首次 23/24 的测试查询范围错误和修复过程保留，不倒写为从未失败。后端最终 R1 61/61、受影响组合 128/128、模型路由/权限/安全/预算 11/11、默认全量 398/399（1 条既有 RFC 条件跳过，未执行、不计通过）。
- 真实本机页面以临时 R2 个人身份、两个助手、临时 SQLite 和受控 loopback Provider 验证双助手历史隔离、切回/刷新/退出重登恢复、Model/Provider 停用、Vault 锁定和明确重试。POST 响应完整进入后端但浏览器响应丢失时，页面保留原键并先查询，最终仅 1 次 Provider 调用和 1 条用户消息；没有 mock 浏览器请求。
- 390×844 窄屏无横向溢出；正常与恢复页面 console 为 0 error/0 warning。前端窗口已停止并清理临时后端、Provider、转发层、Vite、数据库、脚本和构建产物。前端未改 backend，后端未重复运行前端测试。
- R1 前后端实现、隔离回归和受控页面闭环现可交总体协调窗口正式验收；本条不自行宣布 R1 已通过，R3 未开始。真实 Engine、真实 Provider、真实凭据、业务公网和费用均为 0；未暂存、提交、fetch 或 push。

## 2026-09-05｜R1 后端隔离复验完成（等待前端接线与阶段验收）

- 后端 R1 三文件专项 **61/61**、受影响 V1—V5 八文件组合 **128/128**、模型路由/权限/安全/预算组合 **11/11**，均为 0 失败、0 跳过。默认后端全量最终为 **399 项：398 通过、0 失败、1 条既有 RFC 跨仓对照条件跳过**；该跳过未执行、不计通过。
- 保留无效验证事故：一次手工直接运行 RFC 测试绕过隔离启动器，自动读取真实 Engine 的 HEAD/规范化实现并因固定历史基线不符失败（30/31）。该次不是 R1 验收证据，未启动、修改或提交 Engine。经正式启动器重跑，RFC 本地向量 8/8，通过；跨仓项因明确不存在的隔离路径跳过，`EVIDENCE_CONFLICT = RESOLVED`。
- 保留普通返修经过：R1 专项首次 60/61 暴露测试时钟未贯穿 model executor，统一注入后 61/61；默认全量首次 397/399（1 失败、1 跳过）暴露旧 R2 删除夹具使用已关闭的生产 legacy 写入口，改为显式 test-support 兼容后删除专项 12/12、最终全量通过。生产个人访问边界没有放宽。
- 有效验证仅使用临时 SQLite、受控 loopback Provider 与隔离路径；没有真实 Engine、真实 Provider、真实凭据、业务公网或费用。R1 后端已整理到可交前端联调与总体协调复核的状态，但前端尚未接线，不能宣布 R1 整阶段完成；R3 尚未开始。本轮未暂存、提交、fetch 或 push。

## 2026-09-05｜R1 后端独立聊天闭环施工（阶段验收前）

- R1 后端已从 R2 个人服务端会话和当前助手取得唯一身份，新增每助手唯一默认会话及 `/api/v1/personal/chat/*` 创建、查询和显式恢复合同。任何客户端自报 user/assistant/conversation 身份、固定 Profile 或数据库首位用户都不参与授权；切换助手只切换到另一条隔离默认会话，R3 多会话未提前实施。
- 迁移 `025` 独立保存 turn、单一逻辑 model execution、逐次 Provider attempt、usage/cost、锁定 Provider result 与 recovery action；Conversation、Message、MessageVersion 和 Event 仍是 Vio 自有消息事实。用户级幂等键、单活动轮次、完整 owner/assistant 复合外键及不可变/状态转换约束共同防止重复调用和跨助手串写。
- 正常独立模式由 Vio 自己完成默认 chat 模型选择、加密凭据瞬时解析、权限/外发安全/预算检查、OpenAI-compatible HTTP 执行及最终主体 Message 发布。模型输入限于当前助手明确设定、当前默认会话有界历史与本次消息；不读取固定 Profile、其他助手、Engine、私域或未来记忆。查询、刷新和启动不触发模型。
- 恢复边界区分“确定未发送/明确 retryable”与 `outcome_unknown`；后者禁止盲重试，已锁定结果只恢复发布。施工中先修复 loopback 测试注入和 retry 发送边界，随后按安全审计补齐预算复验、会话/删除状态及凭据调用边界、owner 范围幂等键和 pending recovery 重放；历史失败与补修经过见[后端开发日志](../backend/docs/开发日志.md)。
- 生产个人访问已收口可绕过 R1 的 legacy conversation/message/continuity-turn/state-update 写入口；历史 V1—V5、S2/S3/S4、F1 与首次 S4-Live 数据、读取与测试事实不删除、不冒充独立模式证据。测试兼容只能经显式 test-support；R1 不探测、读取、启动或修改真实 Engine，外部模式仍只能经 Subject Runtime Port/适配器另行接入。
- 当前前端尚未接入 R1 合同，最终专项、受影响回归、默认后端全量与静态检查仍待统一完成，因此本条不是 R1 整阶段通过记录；测试数量待最终真实结果后追加。R3 尚未开始，真实供应商、真实凭据、业务公网和费用均未用于本次施工。

## 2026-09-05｜R2 正式验收通过

- 总体协调窗口已正式验收 R2：个人访问、首次设置、多助手、个人资料、会话管理、审计诊断、Provider/Model、加密凭据、认证连接检查及受控账户删除完成真实本机闭环。`PLANNING_CONFLICT = NONE`，`EVIDENCE_CONFLICT = NONE`。
- 最终证据为后端 R2 53/53、默认全量 330 项中 329 通过/0 失败/1 既有隔离跳过；前端 98/98、198/198、类型检查和构建通过，以及真实页面对初始化、两助手、解锁、确认/取消、凭据掩码、loopback 认证和 7/14/30 删除恢复的完整验证。下方首次失败、中间成绩及返修历史不改写。
- 邮箱、Google 和公开注册继续暂缓；手机实机、HTTPS、云部署、跨设备云端同步及本轮真实供应商验证不计入 R2 已执行项。R1 独立聊天和 R3 多会话尚未开始，本次只做验收归档与获准的统一 Git 收尾。


## 2026-09-05｜R2 受控删除、恢复边界与协调返工

- 用户批准 7 天可撤销、实际删除后 14 天受管副本和 30 天最小凭据政策；此前政策未决的记录保留，但不再是当前规划阻塞。只在隔离数据上验证，不对真实空间执行删除。邮箱/Google/公开注册暂缓、个人服务端方向、多助手、六导航和原安全墙不变；不开始 R1，也不把偏好保存视为云同步。
- 后端新增 024 和实际 owner/行依赖范围的专用删除授权，保留普通历史不可变保护；限权删除 Cookie 与普通业务会话分离，撤销必须重新登录。到期任务、受管副本、WAL 与整体状态分别记录，失败/占用可恢复；不建设 R11 全套备份、不擦除用户自存副本或真实 Engine。解锁错误 403、双栈安全 IPv4 选择、已取消旧操作键的真实 HTTP 语义一起补齐。
- 本轮前端负责删除专页、未知结果隔离、取消新键和密钥恢复清理；后端负责真实服务与页面联调，测试/构建串行。后端首轮专项 51/51、默认 328 项（327 通过、0 失败、1 原有 RFC 隔离跳过），后续发现终态事务缺陷继续修复。首次受管路径测试 3/8、删除/副本组合 12/16 的失败及修复完整记录于[后端开发日志](../backend/docs/开发日志.md)，不以成功覆盖失败。
- 前端首次 82/83 的真实 UTC 日期缺陷及修复后 83/83、183/183；后续静态补强后 94/94、194/194、类型检查/构建通过，均为[前端窗口交接](R2_FRONTEND_HANDOFF.md)来源，不是后端重新执行。最终窄遗漏修复、复验和真实页面结果继续追加，不提前宣称验收通过。
- 终态事务修复后最终后端专项 53/53；原默认 `pnpm test` **330 项：329 通过、0 失败、1 既有 RFC 隔离跳过，退出 0**。前端最终专项 98/98、全量 198/198、typecheck/build 均通过（前端窗口报告）；首轮失败和每次中间成绩保留。
- 后端作为唯一浏览器窗口完成本次最终页面：初始化、两助手、错误解锁不退出、正确解锁、拒绝保存后新操作成功、掩码与 loopback 认证；删除拒绝/重提、限权刷新、错误证明拒绝、7 天内撤销及新会话；注入时钟到期、真实执行失败、真实停服/同库重启、在线删除而副本冲突仍待处理、恢复后整体完成与刷新查询。22 类/91 行临时 owner 数据实际删除，凭据/助手/受管副本登记为 0；其他 owner 保护由同库 HTTP/数据库专项验证，不冒称页面多租户验收。
- 窄屏 390×844 下宽度与滚动宽度均 376；未额外采集控制台，主动停服的代理错误如实保留。所有本轮服务正常停止、自建临时根归属校验后清理；没有真实 Engine 探测、真实密钥、真实模型/供应商、业务公网、真实用户删除或费用。只有受控本机 Provider 认证 HTTP，不做生成、云部署或 R1。整体交总体协调窗口验收，不自行宣布已获验收。
- 最终检查55个JS语法通过；22份Markdown的121链接/30锚点无断链；119文件空白及git diff --check通过，敏感模式0命中，无运行数据/构建产物混入。119项预期修改由backend69、src39、项目档案11组成；暂存区为空，HEAD保持05f848a、本地origin保持3847112、2/0。无已知未解决规划/证据冲突，整理为可供阶段验收和提交审查的状态；未暂存、提交、拉取或推送，R1未开始。


## 2026-09-04｜R2 个人访问、多助手、凭据与页面真实联调

- 用户将当前目标调整为个人自用、手机优先、未来同一服务端数据；邮箱验证码、Google 与公开注册暂缓，但访问保护、稳定身份、多助手、六导航、数据隔离和安全墙不减。本轮未采购服务器、部署或开放公网，也未提前进入 R1/R3。
- 后端用迁移 `023` 和个人模块实现受控初始化、个人口令会话、首次设置、资料、多助手、会话撤销、访问审计/诊断、Provider/Model、加密凭据和受限 `/models` 认证检查。正式业务路由统一从服务端会话取用户；旧开发身份、固定 Profile 和无归属恢复缓存不自动认领、迁入、重发或删除。
- 前端六导航保留；个人访问、首次设置、助手/资料、访问安全与能力页使用同源 R2 API。真实浏览器完成登录到再登录的一轮本机闭环，并确认 390px 窄屏无横向溢出；能力页明确 Provider/Model/凭据/连接已接线而其他能力组仍是原型。F1 对话仍是旧固定 Profile 链路，不冒充 R1 身份化独立聊天。
- 连接错误映射专项首次发现并保留 `response_too_large` 被同步 aborted 抢先覆盖及活动句柄问题；定点修复后 R2 后端专项 10/10。默认后端全量 287 项：286 通过、0 失败、1 条既有跨仓 RFC 条件跳过。前端窗口最终报告专项 50/50、全量 106/106、TypeScript 和生产构建通过。
- 收尾补齐真实初始化页面及后端中断恢复：全新隔离身份经页面初始化/首次设置成功；实际停止本轮后端时页面明确失败，同库重启后手动重试恢复原身份/助手，密钥库仍 locked。最新隔离启动器 R2 专项再次 10/10，默认全量再次 287 项（286 通过、0 失败、1 既有条件跳过，退出 0）。前期身份、邀请、库密钥和连接错误的首次失败及修复见后端开发日志，不以最终通过抹去。
- 后续安全审查补测发现旧配置写入口绕过 R2 确认、跨用户 Provider 更新归属检查晚于确认两项缺陷，各保留一次 4/5 的配置专项失败。修复后 R2 专项增至 11/11；个人会话使用旧重叠写入口明确 403，跨用户更新在创建确认前 404。旧业务服务层和历史测试通过显式依赖保留，不建立生产身份后门。
- 最终默认后端全量实际 **288 项，287 通过、0 失败、1 既有 RFC 条件跳过，退出 0**（26.309 秒）。后端专项 11/11；前端窗口的 50/50、106/106、类型检查及构建结果保持其明确来源。没有运行真实 Engine 联调，不将跳过项算作通过。
- 最终静态检查：41 个 JavaScript 语法检查通过；22 份 Markdown、109 个相对链接与 30 个锚点无断链；差异/空白检查通过，敏感凭据模式 0 命中；无运行数据或构建产物进入本批差异。本轮共 95 个预期变更文件，由后端/前端各自唯一写入范围组成，暂存区为空；不执行 add/commit/fetch/push。删除政策阻塞未消除，不交付虚假的 R2 完成结论。
- 自动化和浏览器只使用临时 SQLite、随机 loopback Provider 与非真实测试凭据；没有探测/读取/启动/修改真实 Engine，没有业务公网、真实供应商/模型、真实 API Key 或费用。联调端口已停止，所属临时根已核验并清理。
- 账户/空间删除的数据保留政策尚未由用户冻结。现行实现拒绝以永久停用冒充删除，也不解除普通历史不可变保护；删除接口保持未开放。此项为唯一 `PLANNING_CONFLICT`，因此 R2 尚不能整阶段验收，R1 未开始。

## 2026-09-04｜R2 第一批交付：测试隔离与独立多助手组件

- 用户已确认暂时仅本人使用、手机为主、电脑访问同一服务端数据且不依赖个人电脑持续开机，接受独立云服务器方向；服务商、地区、配置和预算未定，未授权购买或部署。邮箱验证码、Google 登录、公开注册暂缓；本人/获准设备访问保护、真实身份、密钥安全、同用户多助手隔离、六导航及真实功能仍保留。两端同一服务端数据不等于离线同步或备份已完成，不在本批重写监工 Word 或启动后续功能。
- 后端默认全量通过隔离启动器覆盖原 `tests/*.test.js` 及默认并发；路径发现被注入为临时受保护仓库/外部目录/默认数据目录，正式 CLI 及子进程仍调用原参数解析、业务函数和保护断言。生产保护默认值不变，未新增 HTTP 或迁移。Node 支持限定 `>=22.23.1 <23.0.0`，不支持版本/能力在创建临时目录和测试子进程前退出，不能非隔离回退。
- [后端开发日志](../backend/docs/开发日志.md)保留全部真实经过：最初专项 58 项（37 通过、20 失败、1 跳过），修复 Git 精确目录信任与异步 loader 问题后 58 项（57 通过、0 失败、1 跳过），此前默认全量 273 项（272 通过、0 失败、1 跳过）。本次 Node 兼容门禁后的第一次默认全量 276 项（270 通过、5 失败、1 跳过）也保留；五项为 pnpm 版本诊断中的 JSON 被测试解析器误识别，局部修复并新增回归后原默认 `pnpm test` **277 项：276 通过、0 失败、1 原有条件跳过，退出 0**。不拿历史通过覆盖首次失败。
- 唯一 RFC 跨仓实现对照通过原机制明确指向不存在的临时路径，因此未执行、不计通过；未新增其他跳过，未访问或探测真实 Engine，未运行共享联调。pnpm 自带运行时的版本警告仍保留；实际后端测试使用 Node 22.23.1。没有降低并发、断言、超时或进程隔离，也没有升级依赖。
- 前端交付仅 [`AssistantManager.tsx`](../src/components/assistant-management/AssistantManager.tsx)、[`AssistantManager.module.css`](../src/components/assistant-management/AssistantManager.module.css)、[`AssistantManager.test.tsx`](../src/components/assistant-management/AssistantManager.test.tsx) 三文件。组件以调用方提供的上下文标识、列表/当前助手数据和创建/选择/重读回调为边界；包含加载/空/错误/忙碌状态、同步防重复提交、取消及陈旧回调抑制，不自行 fetch 或持久化。后端窗口只核对和提交代码，三个文件 SHA-256 与前端验证后值完全一致，未修改前端实现。
- **以下成绩引用前端窗口完成报告，不是后端窗口重跑**：`pnpm test src/components/assistant-management/AssistantManager.test.tsx` 为 **27/27**；根目录 `pnpm test` 为 **87/87**；`pnpm run typecheck`、`pnpm run build` 均通过，四条命令退出 0。前端报告验证时未改三个文件，并仅清理自己生成的构建输出；本窗口不重复运行前端测试和构建。
- 组件仍未接入主应用或真实登录/账户数据。未来调用方仍须提供真实授权数据/回调，并防止旧账户或助手响应污染外部 store；前端同步请求锁不等于后端创建幂等，也不证明超时重试不会重复创建。未借用固定 Profile 冒充真实多助手产品验收，未完成 R2、R1 独立聊天、云端同步、部署或备份。
- 用户指定后端窗口为本批唯一 Git 执行者：先提交后端隔离、Node 门禁及后端记录，再独立提交三个前端文件和本工程日志/更新记录，逐文件核对和暂存，不夹带其他窗口改动、不改历史、不创建分支/PR/tag、不 fetch/push。静态检查、Markdown 链接、敏感信息、前端文件不变及仓库残留检查在交付前完成；临时诊断输出仅在仓库外保留，不入 Git。真实 Engine、凭据及业务库访问为 0，业务公网、真实模型/供应商调用及费用为 0。本批结束，等待协调窗口，不自动开始下一批。

## 2026-09-04｜R0 整体复验与验收记录收尾

- 沿用已审查文档基线 `0ab4e910577efb84a1e6d0cee14f157861488faa`，本地 `origin/main` 为 `948e201522750aea26aecd34b9be8136d9280951`，开始 `main` 领先/落后 1/0、工作区/暂存区干净。后端窗口统一执行，前端窗口待命；无 fetch、push 或历史改写。
- 完整保留本次失败链：首次默认后端全量 262 项、258 通过、3 失败、1 跳过、退出 1；其中两文件报告 `Fatal process out of memory: Zone`，另一文件异常退出。没有用历史成绩或随后成功覆盖该次崩溃。
- 经用户逐步授权，三个失败文件单独各运行一次，分别 2/2、2/2、3/3，通过且退出 0；临时 `--test-concurrency=1` 同范围串行诊断为 266 项、265 通过、0 失败、1 原有跳过、退出 0。提交内存仍约 96.51%、余量 0.90 GiB 时主动不启动默认复验，没有擅自关闭用户程序或修改系统/测试设置。
- 用户释放资源后，原默认后端 `pnpm test` 一次受控复验为 266 项、265 通过、0 失败、1 原有跳过，退出 0，未复现崩溃或业务断言失败。启动前提交内存约 22.95 / 25.82 GiB、可用物理内存约 4.04 GiB；仍使用 Node v22.23.1、x64、12 个可用处理器。该结果不证明全部崩溃根因已查明，串行结果也没有冒充默认命令结果。
- 同一基线前序已通过的专项/组合引用本次真实来源：R0-A 37、R0-B 9、R0-C 37、前端全量 60、V1—V4 六文件 101、R0-A/B/health 加 V1—V5 十一个文件 167，均 0 失败/0 跳过/退出 0。前端最初 `pnpm exec vitest run ...` 解析失败也保留为未运行、不计通过；正确既有命令及完整流水见 [追加矩阵](../backend/docs/SUBJECT_RUNTIME_PORT_V1.md#r0-review-20260904)。本次补齐 TypeScript、生产构建与 11 个 JavaScript 语法检查，均退出 0；迁移 021/022 fresh、upgrade、失败回滚和 019/020 升级的原有测试在默认全量实际通过，不以“未改迁移”代替验证。
- 所有后端测试进程显式继承不存在的 RFC 隔离路径，唯一跳过的真实跨仓规范化对照未执行、不计通过；不新增跳过，不运行真实 Engine 共享测试。仅按本次临时许可由既有沙箱保护逻辑检查 Engine 与 `.continuity-data` 目录元数据，不读源码/HEAD/配置/密钥/数据，不枚举内容、不启动、不修改、不真实连接 Engine。下方首次 R0-C 越界事实及其失败、停工、后续授权和纠正记录完整保留，不把新授权倒写到首次行为。
- 八项原 R0 要求已逐项对应本次合同/实现/文档/测试证据；只追加三份日志和既有矩阵，不改源码、测试、依赖、配置、迁移、前端实现、接口合同或桌面 Word。文档上下文、链接/锚点、历史保留、敏感信息、差异范围和残留检查执行；只清理本轮新生成的构建产物，保留既有临时文件和缓存。真实模型、供应商、API Key 使用、业务公网及费用为 0。
- 四项决定继续有效：R0 验收后 R2 → R1 → R3 至 R13；R8 公共机制及截至 R7 接线，R9—R11 各自前后端真实联调，R13 全量复验；同用户多助手、每助手多会话和隔离；六导航保留。None/状态接口不是独立聊天完成，真实登录/多助手与独立聊天尚未施工；真实引擎接入不作为 Vio 发布前置。本次复验与材料交总体协调窗口最终验收，未推送，未开始 R2 或 R1。

## 2026-09-04｜R0 文档与验收材料收尾

- 本轮唯一写入窗口为 Vio 后端，范围仅 Markdown 文档、索引、决策及日志。开始基线为 `948e201522750aea26aecd34b9be8136d9280951`，`main == origin/main`（本地引用）、领先/落后 0/0，工作区/暂存区干净；不联网刷新、不推送，不编辑桌面监工 Word。
- 采用四项已确认决定，不另立方案：R0 验收后 R2 → R1 → R3 至 R13，原编号不变；R8 公共机制与截至 R7 已完成模块接线，R9—R11 各自完成对应前后端真实联调，R13 全量复验；同一用户多个助手的创建/选择/普通切换，各助手多个会话和原有隔离；保留六导航，不通过删页面或占位减少验收。助手市场/协作不扩展，原真实功能与安全墙保持。
- README、通用合同说明、API/事件边界、架构、现行决策和路线索引已协调：R0-A/B/C 完成与整体待验收分开，R0-B 状态接口/装配和 R0-C 读取不再写成未新增；旧聊天仍未完成 R1 解耦，真实登录及多助手产品功能不能冒称已完成。F1 与 2026-08-14 首次 S4-Live PASS 引用已有正式记录，不重跑、不扩大为生产可用。
- 撤回旧报告对后端 README 原第45行的误判：原第43行已明确为适配器专用范围，整段权威原文保留。历史 R0-A 无接口、旧共享验收、真实试聊、Windows 路径/清理事故，以及下方 R0-C 首次读取 Engine 后失败、停工、获准有限目录检查和隔离复验历史均未覆盖。当前说明与当时记录分别标识。
- 引擎独立边界落实为当前规则：R1 整理 Vio 自有适配边界并真正解耦；首次彻底解耦后，未经用户另行要求重连，施工/测试/发布不探测、读取、启动或修改真实 Engine。通用接口必须完成和验证，具体真实引擎接入另行处理，不作 Vio 发布门槛；优先独立适配器或接口扩展，不擅改核心。本轮本身未做任何 Engine 访问。
- [R0 证据矩阵](../backend/docs/SUBJECT_RUNTIME_PORT_V1.md#r0-evidence)逐项对应原要求、现有位置、历史验收、当前状态与未完成项，共八项。历史 R0-A 37/37、R0-B 9/9、R0-C 37/37/前端 60/60 及后端 266 项（265 通过、0 失败、1 既有条件跳过）仅作为历史来源；本轮没有执行这些测试，也没有新增跳过。
- 实际检查：16 份 Markdown 上下文、八项规则与证据、相对链接（0 断链）、新增敏感信息（0 命中）、历史保留、差异白名单及 `git diff --check` 通过。未执行代码回归、共享测试、服务、语法/类型/构建或迁移验证，R0 整阶段复验仍待总体协调窗口授权；文档通过不是整阶段通过。
- 本轮无源码、测试代码、依赖、配置、迁移、前端实现、Word 或引擎修改，无真实密钥读取、业务公网、模型/供应商调用或费用；无运行数据/构建产物进入 Git。只创建独立本地文档提交，未推送；未开始 R2 或 R1。

## 2026-09-04｜R0-C 验收修正记录：StrictMode 请求生命周期与验证边界

- 保留原 R0-C 提交 `2a3382f867f6fbb7cd61b1b6ee0c4e5b6105282d`，只修复设置页初始读取的 effect 生命周期：以本次 effect 独有的取消标记保护微任务调度，让 StrictMode 的首次清理发生在请求发送之前；没有删除 StrictMode，没有等待计时器、永久缓存或轮询。
- 新增 7 项回归：StrictMode 首次进入为 1 次 `getStatus` 和 1 次正式客户端 GET；真实离开后重进累计 2 次；手动刷新每次增加 1 次；卸载取消唯一未完成请求；调度前卸载不发请求；模拟 120 秒不新增请求；刷新中及刷新失败后不展示旧成功状态。
- 如实补记首次验证越界：最初 R0-C 运行后端全量测试时，既有跨仓测试自动发现相邻真实 Engine checkout，间接读取了 Engine HEAD 和规范化实现，并因固定基线不符而失败。这违反了当时“不得读取 Engine”的边界；后续设置隔离路径并条件跳过，不能抹掉首次读取和失败。该次没有启动、修改或提交 Engine，没有外部调用、模型调用、API Key 使用或费用。总体协调窗口因此没有接受第一次 R0-C 报告，也没有授权推送。
- 本轮前端修复后，在再次执行后端测试前，通过只读检查 Vio 代码发现另一条访问路径：既有沙箱 `protectedPaths` 会检查相邻 Engine 目录及其 `.continuity-data` 的存在性并解析真实路径。因此主动停工并报告 `PLANNING_CONFLICT`。用户随后明确批准仅限既有沙箱保护逻辑的只读目录元数据检查；没有建立隔离整仓副本，没有修改后端产品代码或削弱目录保护。
- 最终纠正验证前，每个后端测试进程均将 `VIO_CONTINUITY_ENGINE_PATH` 设置为系统临时目录下不存在的 `vio-r0c-engine-disabled-do-not-create`，并先检查其不存在，未创建占位目录。该变量只隔离 RFC 跨仓源码对照，不控制沙箱目录保护检查。此次未进入 Engine 执行命令、未读取引擎源码/密钥/业务数据、未运行跨仓实现对照、未启动或修改 Engine；不得将获准的目录元数据检查写成“Engine 零探测”。
- 实际结果：R0-C 专项 37 通过、0 失败、0 跳过；前端全量 60 通过、0 失败、0 跳过；R0-A 37/37、R0-B 9/9。后端 `tests/*.test.js` 共 266 项，265 通过、0 失败、1 条原有条件跳过：`Vio and Engine 7a1daca produce identical canonical UTF-8 and SHA-256 for independent corpus`，原因是显式隔离路径不存在；该项未执行，不计为通过，没有新增其他跳过。TypeScript、前端生产构建和修改 TSX 的编译检查通过。
- 用户最新确认的架构口径：R0 冻结边界，R1 完成实际解耦。彻底解耦后，未经用户另行要求重连，Vio 施工、测试和发布不依赖真实 Engine；通用接口验证保留，真实 Engine 对接另行处理，不作为 Vio 发布前置条件。以后优先通过独立适配器或接口扩展接入外部系统，不擅改 Vio 核心。本轮只记录该确认，不编辑桌面 Word 监工文件，也不声称该文件已经同步。
- 本轮仅修改前端请求生命周期、对应回归测试和本次真实记录；后端、迁移、公共 API、聊天装配及 R0-A/R0-B 合同均未修改。业务公网请求、真实模型/Provider 调用、API Key 读取或使用及相关费用均为 0。本地修复交回后仅等待总体协调窗口复验，不推送；R0 总阶段未完成，R1 尚未开始，旧记录原文继续保留。

## 2026-08-25｜R0-C：前端通用主体运行时状态读取与展示

- 在现有前端 API 层新增 `GET /api/v1/subject-runtime/status` 正式只读客户端，沿用同源 `apiClient`，对标准 Envelope、Port v1 全部公共字段、七种连接状态、能力清单、版本协商及交叉关系执行严格校验；缺失、未知、非法或不一致响应 fail closed。
- “我的”设置页新增主体运行时卡片：进入页面只读取一次，手动“重新读取”每次只新增一次请求，页面卸载会取消未完成请求且没有自动轮询。None Adapter 显示为“Vio 可独立使用 / 未连接（正常）”，不会误报 Engine 或平台故障；第三方适配器按后端身份显示，不按名称猜测为 Continuity Engine。
- R0-C 专项 30/30、前端全量 53/53、R0-A 37/37、R0-B 9/9；后端全量在显式禁用 Engine 路径后共 266 项，265 通过、0 失败、1 项既有跨仓 RFC 对照按本轮禁止读取 Engine 的条件跳过。TypeScript 与前端生产构建通过。
- 本轮没有修改后端、迁移、聊天编排或 Engine，也没有运行时选择/连接/断开/重连写操作；业务公网、真实模型、Provider、API Key 与费用均为 0。R0-C 已完成，但 R0 整阶段尚未完成，R1 尚未开始。

## 2026-08-25｜R0-B：通用 Subject Runtime 状态接入后端

- 在 R0-A `vio-subject-runtime-port/v1` 之上新增通用状态服务：只接受完整 Adapter 合同，读取 Manifest、连接快照和版本协商结果后严格交叉校验并冻结；没有创建第二套状态枚举，也不依赖 Continuity Engine 私有合同。
- 后端应用默认使用 None Adapter，并新增 `GET /api/v1/subject-runtime/status` 与 `/health.subjectRuntime`。未配置外部运行时时，运行时明确为 `not_configured`，Vio 平台仍为 `available`。既有 `continuityEngine` 健康值保留为 `adapter_only_legacy` 兼容字段。
- 验证结果：R0-B 专项 9/9、R0-A 37/37、受影响组合 167/167；后端全量 266 项中 265 通过、0 失败、1 项既有跨仓 RFC 对照按本轮禁止读取 Engine 的原有条件跳过。
- 本轮只增加通用只读状态，不提供选择/连接/断开/重连写接口，不切换聊天，不读取或修改 Engine，不修改前端或迁移；公网、真实模型、API Key 与费用均为 0。R0-B 已完成，但 R0 尚未完成，R1 尚未开始。

## 2026-08-25｜R0-A 第二轮验收缺陷修复：deadline 任意小数秒精确比较

- 定点修复 RFC 3339 小数秒精度缺口：原实现最终用毫秒级 `Date.getTime()` 比较 deadline，第四位以后差异会被静默截断。现将已通过公历校验的 UTC 整秒与完整小数部分对齐到共同十进制精度，以整数精确比较 timeout，不限制、截断、舍入或归一化小数秒。
- 回归覆盖 `.1234` 保持、`.1234/.1235` 拒绝、`.1/.1000` 等价、跨日 `.9995 + 1ms` 进位和跨秒偏差拒绝；纯 JSON 递归、32768 字节、真实公历、闰日及原有 timeout 行为继续通过。
- 验证结果：R0-A 专项 37/37，V1—V4 既有六文件 101/101，R0-A 加全部受影响 V1—V5 组合 157/157；后端全量 257 项中 256 通过、0 失败、1 项既有跨仓 RFC 对照按本轮禁止读取 Engine 的原有条件跳过。
- 本轮仅闭合 R0-A 第二轮验收缺陷；前端、迁移、公共 API、聊天装配和 Engine 零修改，公网、真实模型、API Key 与费用均为 0。R0 尚未完成，R1 尚未开始。

## 2026-08-25｜R0-A 验收缺陷修复：纯 JSON 投影与真实 UTC 日历

- 定点修复 R0-A 投影 payload 的严格合同缺口：不再依赖会静默删除或替换非法值的 JSON 序列化往返，改为递归验证纯 JSON object、完整数组、有限数、普通数据属性和无循环引用，并保留 32768 字节限制与精确错误路径。
- 定点修复 UTC 时间的日历有效性：不存在的月日和非闰年 2 月 29 日 fail closed；合法闰日与小数秒继续通过，timeout/deadline 精确关系不变。
- 验证结果：专项 35/35、受影响组合 155/155；后端全量 255 项中 254 通过、0 失败、1 项既有跨仓 RFC 对照因本轮明确禁止读取 Engine 而按原有条件跳过。前端、迁移、公共 API、聊天装配和 Engine 零修改；公网、真实模型、API Key 与费用均为 0。
- 本轮只闭合 R0-A 验收缺陷，不进入 R0 后续子项；R0 尚未完成，R1 尚未开始。

## 2026-08-25｜R0-A：Subject Runtime Port v1 合同基础

- 依据已确认的 R0—R13 独立架构规划，只施工 R0-A，不进入 R0 后续接线或 R1。
- Vio Core 继续负责账号、助手、会话消息、模型供应商、扩展能力、设备、本地记忆、上下文、权限安全、工作流生活数据以及费用/导出/备份恢复；外部主体运行时仅提供可选的主体连续性、表达和投影增强。
- 建立 `vio-subject-runtime-port/v1`、机器连接状态机、None Adapter、Continuity Engine 适配器专用合同登记、第三方适配器入口、合同样例和兼容表；现有聊天业务未切换。
- Continuity Engine 专用的 v1.1、CapabilityRequest/Result、`model.generate` 与 `conversation_response` 不再表述为 Vio Core 通用合同；既有连接与历史验收事实未删除或重写。
- 验证结果：R0-A 专项 27/27、R0-A 加受影响 V1—V5 回归 147/147；后端全量共 247 项，246 通过、0 失败、1 项既有跨仓 RFC 对照因本轮禁止读取 Engine 而跳过。JavaScript 语法、Markdown 相对链接、敏感信息与 `git diff --check` 均通过；前端、迁移和 Engine 零修改，公网、真实模型、API Key 和费用均为 0。
- R0-A 已完成，但 R0 尚未完成，R1 尚未开始。

## 2026-08-14｜S4-Live 首次真实供应商试聊正式归档

- 正式结论：PASS。验收使用 `C:\VioS4\first-001` 仓库外短路径沙箱，`purpose=s4-live-acceptance`、`identityClass=disposable_test`、`promotionAllowed=false`；测试身份、Binding、Vio SQLite 与 Engine data 不得晋升为正式数据。
- 真实链路使用 Alibaba Cloud Model Studio OpenAI-compatible Provider 与 `qwen-flash-2025-07-28`。Provider execution 恰好一次，状态 succeeded，报告 input 177、output 9、total 186 Token，finish reason 为 `stop`。
- CapabilityResult 为 `SUCCEEDED`，首次回传 HTTP 200，result outbox completed；没有第二次 execution/result。Conversation Turn completed，最终主体表达“Vio首次真实试聊连接成功。”由 Engine `FirstRoundSuccessResult` 产生，不是 Provider 原始候选直接写入。
- Engine 保持 `changed=false / revision=0`，没有内部 Event、StateMutation 或越权写状态；无 capability incident、outcome_unknown 或重复 Message。该事实再次确认 Engine 的 SubjectState 与最终主体表达权威未被 Vio 绕过。
- 费用事实以 Vio 账本 `cost_status=not_reported` 为准。验收时供应商费用概览显示 ¥0，但官方用量和账单存在同步延迟，不能据此声明永久最终费用为零。
- 验收结束后 5173、8787、8766 均无监听。官方 cleanup 返回 `status=completed`、`sandboxRemoved=true`、`deletedRoot=C:\VioS4\first-001`、`protectedPathsTargeted=[]`、`repositoryFilesTargeted=[]`、`externalCall=not_performed`、`providerCharge=not_incurred`，随后 `Test-Path` 为 False。
- Vio 与 Engine 验收后均保持 `main == origin/main`、工作区干净，没有正式数据或仓库污染。本轮只同步工程档案，没有启动服务、再次调用真实供应商或产生新费用，也没有修改代码、测试、迁移、Schema、配置、前端或 Engine。
- 既有 Windows 路径预算事故和旧超预算沙箱 cleanup 阻断记录继续保留，未被覆盖或改写。首次受控验收不等于生产可用：通用正式身份/Binding、生产认证、多租户、加密密钥存储、备份和部署仍未完成，当前不得公开部署。

## 2026-08-14｜旧超预算沙箱 cleanup 阻断修复

- 路径门禁正确阻止 create 并使 doctor 返回 unsafe，但也让门禁上线前的隔离沙箱无法进入官方 cleanup plan；根因是三者共用了同一无条件预算校验入口。
- cleanup 现仅对精确的 `engine_persistence_path_budget_exceeded` 提供私有受限兼容，完整复用其余全部安全校验、只读 plan、双确认和整根删除边界；create/default read/inspection/doctor 没有放宽。
- 真实旧沙箱只读 plan 已返回 `cleanupEligible=true`、唯一整根目标且退出码 0；旧沙箱未清理、未移动、未改写，新试聊未开始。
- 回归通过沙箱 18/18、L1 24/24、V1–V4 101/101、V5 19/19、S4/V5 shared 7/7 与 6/6、后端 220/220、前端 23/23、类型检查和生产构建；零公网、零真实密钥、零供应商调用、零费用。

## 2026-08-14｜S4-Live Windows 路径长度事故定点修复

- 首次真实试聊在 Provider 调用前因 Engine WakeSession 原子写入路径超出 Windows 安全预算而停止；同一 Engine 账本复制到短路径后可用原 requestId/operationId 恢复，确认 Binding、请求契约、账本与恢复逻辑未损坏。
- Vio 沙箱创建、严格 manifest 校验和 doctor 现统一预算 Engine 最深持久化路径，240 字符以上以 `unsafe / engine_persistence_path_budget_exceeded` fail closed；创建失败不遗留目录、Manifest、Binding 或数据库。
- 文档改为推荐全新仓库外短路径 `C:\VioS4\first-001`，禁止截断、移动旧沙箱或用链接/映射绕过；未修改 Engine、前端业务代码、契约或迁移。
- 验证结果：沙箱 13/13、L1 24/24、V1–V4 101/101、V5 19/19、S4 shared 7/7、V5 shared 6/6、后端 215/215、前端 23/23，类型检查与生产构建通过。真实供应商调用 0、真实费用 0；尚未开始新一轮真实试聊。

## 2026-08-14｜S4-Live 可销毁测试沙箱

- 为固定 v1.1 验收身份增加 `disposable_test`、`promotionAllowed=false` 清单和仓库外同根数据边界。
- 新增创建、doctor 门禁、只读清理计划及双确认整根清理；没有新增业务 API、数据库迁移或 Engine 修改。
- 明确保留：真实供应商试聊尚未执行；本阶段零 Key、零模型、零公网、零费用。
- 验证结果：沙箱 9/9、L1 24/24、V1–V4 101/101、V5 19/19、S4 shared 7/7、V5 shared 6/6、后端 211/211、前端 23/23，类型检查与生产构建通过。

## 2026-08-12｜L1 真实供应商首次试聊安全准备入口

- 为已完成的 F1/V5 本地对话链路增加施工前安全准备，而不改变 Engine 权威、V1–V5 机器契约或最终 Message 来源。
- 新增从正式 Binding fixture/hash 导出仓库外文件、只读配置计划、双确认幂等应用和只读 readiness doctor。
- Provider 外发配置继续由 Permission → Security/Confirmation → Audit 保护，预算有限且超额固定 `block`；API Key 不落盘、不回显。
- 自动验证通过 L1 18/18、V1–V4 101/101、V5 19/19、S4 7/7、V5 shared 6/6、后端 196/196、前端 19/19、类型检查和生产构建；只访问随机 loopback 测试服务。
- 本阶段未进行真实 Provider 调用或 live smoke，未修改前端与 Continuity Engine。

## 2026-08-12｜F1：ConversationPage 接入 Vio V5 公共轮次 API

- 从正式基线 `9bd273f9c0c10924cb1e391356d8c7c80bd45043` 开始，只修改 F1 允许的前端 API、对话组件、样式、测试配置和当前档案；`backend/**`、Continuity Engine、迁移与连接契约 v1.1 均保持零修改。
- 对话页改为固定本地 Profile `user-001 / assistant-001 / conversation-001`：历史记录读取公共 Message 列表并按 `sequenceNumber` 稳定排序；新消息使用 `x-vio-user-id` 和每次显式发送唯一的 `Idempotency-Key` 创建 V5 Turn。
- 前端完整呈现 `processing`、确认、预算、重试、结果未知和终态。确认、预算检查、Provider retry 与 outcome unknown 恢复只在用户明确点击后进行；`processing` 只做可取消、最多六次的 GET 查询，不创建等待中的助手气泡。
- 超时、断线或无法校验响应时不判定成功或失败，也不创建第二条用户消息；`sessionStorage` 只保存一个待恢复事实。取得 `turnId` 后立即丢弃不再需要的正文，恢复时优先 GET，同一创建事实只复用原 key 与正文。
- 只有 Turn `completed` 的 `subjectMessage` 会作为 Vio 气泡；同一 `messageId` 去重。编辑、重生成、删除、分支、附件、模型/上下文选择和会话切换均未伪装为可用能力。
- 新增 Vitest + Testing Library 最小前端测试体系，覆盖公共请求形态、历史映射、严格响应校验、正常完成、确认/预算/重试、结果未知、超时恢复、重载恢复、有限轮询、终态、键盘输入、错误映射和消息去重；前端 19/19、类型检查、生产构建、后端全量 178/178 与正式 V5 shared 6/6 通过。
- 受控浏览器联调完成真实“发送 → 等待确认 → 用户点击 → Engine 最终主体 Message”链路；桌面与 320px 窄屏均可见输入区和底部导航、无实质横向溢出，浏览器控制台无警告或错误。测试只使用随机本机端口、临时双数据库与 loopback Provider，零公网、零真实密钥、零真实费用。
- 本阶段没有引入登录、认证、多租户、通用 Binding、MCP/Tool/设备接线、真实供应商 live smoke 或部署。

## 2026-08-12｜Vio V5 账本 MessageVersion 投影阻断修复

- 验收确认 V5 原实现错误跟随通用 Message 的 currentVersion，旧用户编辑或主体 regeneration 会改变已完成 Turn 的公共投影；迁移 `022` 中锁定版本关联本身正确，无需改变。
- 公共 Turn 现严格按账本的 user/subject `message_id + message_version_id` 读取不可变 MessageVersion；通用 currentVersion 可继续演化，但不能改写该轮次的原始输入或 Engine/V2 最终回复。锁定事实缺失或身份不一致时 fail closed。
- HTTP 回归实际让两个 Message 的 currentVersion 发生变化，并验证 GET、幂等重放、终态恢复和重启仍返回首次完成响应，不重复 Provider、V1/V2、主体 Message 或 Engine 经历。
- 最终验证：V5 专项 19/19、真实 V5 shared 6/6、V1–V4 101/101、S4 7/7、后端全量 178/178；锁定版本身份损坏时明确 fail closed。
- 本轮只修改 Vio 后端定点实现、V5 测试和当前文档；前端、Engine、迁移与 v1.1 均保持不变，未执行真实供应商调用。

## 2026-08-12｜Vio 后端 V5 公共对话轮次 API

- 基线为 Vio `0b68e3209cd11c662d4cb973084a18825ed3d03e` 与只读 Continuity Engine E5-A `cba52126db2fb5eca57d9b5c0c80884693c59a6f`；本阶段只修改 Vio 后端、迁移、测试和当前工程档案，前端与 Engine 保持零变化。
- 新增迁移 `022` 的持久化轮次协调账本，以及固定本地 Profile 的公共轮次创建、纯查询和显式恢复 API。轮次把现有 Conversation/Message 接入 V1 → V3 → Engine → V4 → V2，不复制各层 canonical 事实。
- 用户 Message、来源 Event、V1 请求和最终主体 Message 具备稳定一对一关联；同一幂等键只精确重放第一次轮次，同一 Engine subject 最多一个活动轮次。启动按 V4 → V3/V2 → V5 顺序只做本地事实对账，V5 不在启动时重新调用 Provider；V2 完成后和主体 Message 发布后的窗口可直接本地恢复，V1 保存前后的中断由客户端以同一幂等键重放后继续原轮次。
- 最终主体正文只取自 V2 已严格验证并保存的 Engine `FirstRoundSuccessResult.response.content`。Provider 原始候选仍只能作为 CapabilityResult 返回 Engine，不会绕过 Thinking/Action Gate 写入 Message，也不会修改 legacy SubjectState。
- 固定本地试聊准备命令只装载 `user-001`、`assistant-001`、`subject-001`、`binding-001` v1 和 `conversation-001`，不发网络请求；冲突时拒绝猜测或覆盖。
- V5 专项 17/17、正式 V5 shared test 6/6、V1–V4 101/101、后端全量 176/176 已通过；共享测试使用真实 Engine E5-A 进程、Vio 公共 HTTP 和随机 loopback Provider，零公网、零真实密钥、零真实费用。
- 当前仍未执行真实供应商 live smoke，也未开始 F1 前端接线、通用 Binding、生产认证、多租户或部署。V5 完成不表示产品已经公开可用。

## 2026-08-11｜S4：Vio V4 × Continuity Engine E5-A 正式本机 Capability 共享验收

- 使用 Vio V4 `7054577fb55bcc26a28a9c9a90201f5f07f0c0ee` 与只读 Engine E5-A `cba52126db2fb5eca57d9b5c0c80884693c59a6f`，通过正式 Engine capability HTTP 服务、Vio V1–V4 和随机 loopback Provider 完成真实双进程、真实 TCP/HTTP、双方持久化与重启验收。
- S4-A—G 共 7/7 通过，覆盖三份 Schema 3/3、完整成功闭环、精确重放、双方重启、429 明确批准重试、400 终止、UNKNOWN fail closed、回传响应丢失 query-first 以及身份/未知字段/同 ID 冲突隔离。
- 重放与重启没有复制 Provider 收费调用、Vio execution/result/usage/V2 receipt，或 Engine Wake/Thinking/Action/capability attempt/revision；模型候选始终先作为 CapabilityResult 返回 Engine，最终表达来自 Engine，未直接新增 assistant Message 或改写 legacy SubjectState。
- 验证结果：S4 7/7、S2/S3 15/15、V1–V4 101/101、Vio 后端 159/159、Engine E5 54/54、Engine 全量 355/355。测试使用临时数据、随机本机端口与受控 secretRef，零公网、零真实密钥、零真实费用。
- 本阶段只新增 Vio shared test、命令和通过后的状态档案；未修改 Vio/Engine 生产代码、前端、旧迁移、v1.1 或 S2/S3 语义。真实供应商 live smoke、V5、F1、生产认证、多租户和部署仍未开始。

## 2026-08-11｜Vio V4 验收返工：E5-A Capability 多次执行合同闭合

- 在保留原 39 个未提交 V4 文件的基础上，只读核对 Engine E5-A `cba52126db2fb5eca57d9b5c0c80884693c59a6f` 的 `CapabilityRequiredEnvelope`、`CapabilityFailedEnvelope`、交互服务和 retryable/recovery 测试，确认原实现存在 Capability 外层版本与单 attempt 约束两项合同偏差。
- Capability 外层版本已统一为 `continuity-capability/v1`，completed FirstRoundSuccessResult 继续为 `continuity-integration/v1.1`；独立字面量测试同时验证正确值被接受、错误混用被拒绝。
- 尚未发布的迁移 `021` 改为一个 CapabilityRequest 对应多 execution/result/usage/outbox attempt，每个 attempt 不可变且独立计量，只允许一个成功结果。一个或多个 `FAILED_RETRYABLE` 可在明确批准并重跑全部现实门控后继续；成功、终止或 `UNKNOWN` 后禁止新调用。
- HTTP 重传与 Provider 真实重试已分离：POST 结果未知先查询并最多精确重发同一 Result；Engine 已明确接受 retryable 后进入 `waiting_retry`，已明确接受 UNKNOWN 后进入 `provider_outcome_unknown` 并保持 fail closed。外发对话统一使用既有 `privacy_access_request + private_record` 安全分类，不根据正文推断其他敏感类别。
- V4 专项 46/46、V1–V4 指定组合 101/101 通过；fresh 021、001–020 升级、失败回滚、真实 loopback 429→成功、多 retryable、UNKNOWN 重启与安全快照均已验证。最终后端全量结果见提交报告。
- 本次仍未修改前端、Continuity Engine、v1.1、旧迁移或公共对话 API；未开始 S4、V5、F1。

## 2026-08-10｜Vio 后端 V4：Continuity Capability 真实模型执行与可靠回传

### 基线与目标

- Vio 从固定基线 `2288338661c01c37dde9c7e15d5da75641b44c46` 开始；只读核对 Continuity Engine E5-A `cba52126db2fb5eca57d9b5c0c80884693c59a6f` 的 Capability 合同、Schema、领域流程和恢复测试，Engine 仓库保持零修改。
- 目标是在既有 V1 请求、V3 交付和 V2 结果投影账本之间补齐持久化、可恢复的真实模型 Capability 闭环，不新增公共对话 API，不修改前端，不开始 S4、V5 或 F1。

### 完成内容

- 新增 E5-A 三份严格 Capability Schema 本地副本、registry/validator 和身份/归属校验；账户与主体只从已持久化 V1 请求反查，`permissionRef` 不能代替 Vio 授权。
- 新增顺序迁移 `021_create_continuity_capability_execution_ledger.sql`：CapabilityRequest、现实门控决策、模型执行、Provider Token/费用事实、canonical CapabilityResult、回传 outbox/attempt 及 incident/quarantine 独立持久化，并保护稳定 ID、hash 与终态不可覆盖。
- 新增 `openai_compatible` 生产形态非流式 HTTP adapter、受限环境变量 secretRef 绑定/轮换、安全解析、请求/响应/连接上限和禁止重定向规则。其他 Provider interface 明确 fail closed，不自动 fallback 或二次收费调用。
- V4 串联 Model Router → Permission → Security/Confirmation → Token Budget → Provider → CapabilityResult → Engine；completed 继续交给 V2，capability_failed 终止。发送后结果未知先查询；完整 Result 已落盘时只允许精确重发；Provider 调用结果不可恢复时隔离。

### 验证与仍未完成

- 指定 V1–V4 组合 93/93、后端全量 151/151 通过；`021` fresh、`001–020` upgrade、失败回滚通过。随机 loopback 受控 Provider 验证真实 HTTP adapter，测试不访问公网、不使用真实密钥、不产生真实费用。
- 真实供应商 live smoke 未执行，不能把 loopback 测试写成 GPT、Claude、GLM 或其他供应商验收。Engine 仍是 SubjectState、Event、StateMutation、Evolution、revision 和最终主体表达的唯一权威；Vio 不把模型原始输出直接保存为 Message。
- 未修改前端、Continuity Engine、v1.1 或旧迁移；S4 双仓共享验收、V5 公共对话 API、F1 前端接线、真实供应商配置、MCP/Tool/设备仍未开始。

## 2026-08-09｜D1 正式本机 Continuity Engine 连接验收档案同步

### 基线与正式结论

- D1 开始时 Vio `main`/`origin/main` 为 `35780da56c72b822fc018702dfe5e90674ab0fcb`，Continuity Engine `main`/`origin/main` 为 `189441f9bad2a34119b4ef10365a4385ed0949cc`，双方工作区和暂存区均为空。
- Engine E4 正式本机 HTTP Integration Adapter，以及 Vio V3 正式 HTTP transport、Delivery Service、迁移 020 的 delivery outbox/attempt 已完成。双方通过真实 `127.0.0.1` TCP/HTTP 通信，数据库继续独立。
- Vio V1 持久化请求已能进入真实 Engine E4；Engine 正式 result/stateProjection 已由 Vio V2 严格验证并持久化。S2 正常共享连接验收与 S3 故障、崩溃、重启和恢复验收均通过，正式本机 Vio ↔ Continuity Engine 连接阶段完成。

### S2/S3 证据与缺陷历史

- S2/S3 正式 HTTP shared tests 15/15，Vio V1 + RFC 8785 + V2 + V3 64/64，Vio 后端全量 113/113；Engine crash-recovery 15/15、E4 67/67、全量 301/301。
- 已验证 changed=false revision 0→0、changed=true 0→1、后续 1→1，四类机器契约错误，以及 completed/not_found/recovery_required 查询。
- 已验证 POST 响应丢失恢复、Engine 中途退出恢复、查询阶段 Engine 不可达后继续、Vio 在 V2 落盘后退出的本地恢复、双方同时重启、requestHash/operationId 冲突隔离和 `IDEMPOTENCY_KEY_REUSED / never` 不覆盖首次 success。Wake、Thinking、Event、StateUpdateRecord、revision、result、projection 和 receipt 均保持幂等。
- S3 首次运行曾真实发现 Engine 在 `reserved` 非终态、已有部分 Wake/Thinking 副作用但 domain checkpoint 尚未持久化时无法恢复；Vio 正确保留 `outcome_unknown`，问题归属 Engine crash-recovery。Engine 以 `189441f9bad2a34119b4ef10365a4385ed0949cc` 完成定点修复后，原失败场景和全部 S3 场景通过。该首次失败作为工程事实保留，没有删除或改写。

### D1 文档范围与下一步

- 本阶段只同步 Vio 根/后端 README、API/模块/集成/迁移/测试索引、当前状态、路线图、ADR 实现状态、v1.1/14a 当前入口、数据库设计/关系图、两级日志和更新记录；没有修改前端、后端源码、测试、迁移、配置或 Continuity Engine。
- 正式本机连接完成不表示产品已经可用：真实 AI 模型、CapabilityRequest/CapabilityResult、Vio 公共对话 API、现有前端真实 Conversation/Message API 接线、MCP/Tool/设备、生产认证、多租户与部署仍未完成。
- D1 不重复执行刚完成的双方全量代码测试；提交前只执行文档范围核对、Markdown 相对链接检查和 `git diff --check`。
- 下一阶段：共同确定真实模型/Capability、Vio 公共对话 API 与现有前端接线的最短施工顺序。

## 2026-08-08｜Vio V3 正式本机 HTTP Integration Adapter 与交付恢复

### 基线与目标

- 施工前确认 Vio `main`/`origin/main` 为 `673983901b38127b15f772a8be8507defec7384e`，Continuity Engine E4 `main`/`origin/main` 为 `c5ebbf9b7583f3fb50198a3bf37ea0553edc131f`，双方工作区和暂存区均为空。
- 只读核对 Engine E4 的本机 HTTP 服务实现：POST `/internal/v1/continuity/interactions`、GET `/internal/v1/continuity/requests/{requestId}`、`/health/live`、`/health/ready`、Bearer service token、JSON Content-Type、1 MiB 请求上限及 completed/recovery_required/not_found 语义。
- 本阶段目标仅为 Vio 正式本机 HTTP transport、delivery/outbox/attempt 和恢复基础；不修改前端或 Engine，不接真实模型，不进入双方 S2/S3。

### 实现与恢复语义

- 新增 Node 内置 HTTP transport，严格发送 V1 SQLite 首次保存的 canonical UTF-8 请求；只接受无凭据的 `http://127.0.0.1` origin，并区分 connect/response timeout、401、5xx、非法 Content-Type/UTF-8/JSON 与响应体超限。success 和四类机器契约 error 的 HTTP 200 envelope 原样交给 V2。
- 新增迁移 `020` 的六态 delivery outbox 和逐次 attempt 账本。只保存 requestId/hash、首次 operationId、脱敏 transport 结果、错误码和恢复原因，不复制 V1 请求、V2 结果、token、正文或 Engine 私密状态。
- Delivery Service 从 V1 账本读取并重新验证原请求、Binding 与 hash；正常结果委托 V2 严格保存。POST 结果未知时不盲目重投，先查询 Engine：completed 直接接收结果，recovery_required/not_found 才重投同一请求，查询失败继续保持 outcome_unknown；hash 不匹配或 operationId 漂移进入 quarantined。
- 应用集成默认关闭；启用时 URL/token 必须显式合法。启动恢复优先完成已有本地 V2 checkpoint，再把中断的 in_flight 视为 outcome_unknown 并查询。Engine 不可达不阻止 Vio 启动，只把 `/health` 的 `continuityEngine` 标为 degraded。

### 验证与边界

- JavaScript 语法检查通过；V3 专项 18/18、RFC 8785/Engine E4 跨语言 9/9、V1 8/8、V2 29/29、V1/V2/健康组合 38/38、后端全量 113/113 通过。
- 迁移专项实际验证 `001→020`、`018→020`、`019→020`，两张新表、三个索引、五个触发器、外键、不可变终态和 attempt 事务回滚均通过。
- RFC 跨语言回归仅把只读 Engine SHA 断言从旧 Runner 基线更新为 E4 `c5ebbf9`，canonical JSON、UTF-8 与 SHA-256 输出未变化，未修改 hashing 实现或机器契约。
- 本阶段未修改前端、Continuity Engine、迁移 018/019、公共对话 API、legacy SubjectState、模型、Capability、MCP、Tool 或设备。V3 完成不等于双方正式链路已经联验；下一阶段仍为新的明确任务下执行 S2/S3 正式本机共享验收。

## 2026-08-02｜Continuity Engine 与 Vio 第一轮 test-only 端到端共享验收

### 基线与核对

- 施工前确认 Vio `main`/`origin/main` 为 `97874eeaf3ad01876617d74051222e3773b80c02`，Continuity Engine `main`/`origin/main` 为 `7a32a99e60330782c1caf6d6adda5d08d0077a6c`；两边工作区和暂存区均为空。
- 实际核对 Engine JSONL Runner、共享装配、ContractTestAdapter/Bootstrap、确定性 test double、结果/operation 账本、SubjectState JSON 恢复、Runner 测试与当前档案；核对 Vio V1 请求构造/仓储、V2 transport seam/result validator/service/仓储、迁移 018/019 与 V1/V2 测试。
- 三份 Schema 的文件排版不同，但解析后的 JSON 结构逐层完全相等；URN、`$ref`、Binding/hash、success/error、projection 和 revision 语义一致，未发现机器契约冲突。

### 共享测试实现与真实结果

- 新增仅测试使用的 Node JSONL Runner transport。它要求显式 `CONTINUITY_ENGINE_REPO`，以 Engine 根目录为 cwd，保留环境并设置 `PYTHONPATH=src`，使用受控临时 Engine data-dir 启动 `python -m tests.shared.continuity_contract_jsonl_runner`；输入输出均为单行紧凑 UTF-8 JSON，正常 stderr 必须为空。
- 共享测试从 Vio V1 SQLite 读取首次持久化逻辑请求，不重建或替换 requestId、requestHash、createdAt、Observation/fact 或 expected revision；Engine 原始 envelope 交给 Vio V2 严格接收服务保存。
- 场景 A `hello` 返回 `changed=false / 0→0 / engineUpdateId=null`，Vio 保存 result/response/revision 0 projection/receipt 并初始化 head 0，Engine 保持 revision 0。
- 场景 B `remember continuity test focus` 经真实 E3 Perception、Thinking、Action Gate 与 Evolution 返回 `changed=true / 0→1`；Engine 只有一条 StateUpdateRecord，内部 Event ID 与 Vio sourceEventId 不同；Vio 只有一份 revision 1 投影并将 head 0→1，legacy SubjectState 表保持为空。
- 场景 C 在 expected revision 1 返回 `changed=false / 1→1 / engineUpdateId=null`；Vio 保存回执且 head 保持 1。
- 同进程重放 B 返回首次 operationId、responseId、engineUpdateId、completedAt 和投影；双方关闭并使用相同临时数据重启后仍逐字段一致，Engine revision/更新数保持 1/1，Vio 投影/回执不重复。本地 V2 完整 checkpoint 存在时不会再次调用 Runner。
- 真实 Runner 四类错误均通过：`SCHEMA_INVALID/never`、非泄露 `SUBJECT_BINDING_MISMATCH/never`、`REVISION_CONFLICT/reassemble` 及 `IDEMPOTENCY_KEY_REUSED/never`。前三类保存终态且无投影；幂等键复用错误不能覆盖首次 success，只形成非敏感 quarantined incident，head 保持 1。

### 验证、定点校准与边界

- 提交前回归发现 RFC 8785 跨语言测试仍固定旧 Engine E3 SHA `c732f35`；只将该只读基线断言和测试名称更新为正式 Runner SHA `7a32a99`，未修改规范化/哈希实现、Schema 或机器契约。更新后双方额外语料的 canonical JSON、UTF-8 和 SHA-256 继续完全一致。
- JavaScript 语法检查通过；共享专项 6/6、Vio V1 + RFC 8785 17/17、Vio V2 29/29、迁移 `001→019`/`018→019` 定点 1/1、后端全量 95/95、Engine Runner 16/16、Engine 全量 219/219 通过。
- test-only transport 进程错误、超时、非法输出均明确失败；共享测试还验证生产 `src`、HTTP、前端和默认 transport 未注册 Runner。测试只使用彼此独立的临时 SQLite/Engine JSON 数据并完成清理。
- 本轮未修改前端或 Continuity Engine，未增加网络、HTTP/RPC、生产 Adapter、真实模型、CapabilityRequest/Result、MCP、Tool、设备或 legacy state_update。确定性回复只来自 Engine 进程内 test double，前端尚不能通过该链路试用。
- 最终结论：**双方第一轮 test-only 端到端共享验收通过。** 下一阶段须另行授权设计正式本地服务连接和生产形态 Integration Adapter。

## 2026-08-02｜Vio V2 提交前 changed=false head 初始化定点修复

### 验收发现与修复

- 提交前验收重点确认首次 `changed=false` 权威投影的 head 初始化语义。原实现会保存 result、revision 0 projection version 和 receipt，但在 head 不存在时不创建 head；该实现不符合本轮明确的首次确认语义，属于提交阻塞。
- result service 已定点修正：只有 `expectedEngineRevision=0`、`previousRevision=0`、`currentRevision=0`、`changed=false` 的合法首轮结果可以初始化 head；无 head 的非零 revision 或 changed=true 结果进入 `reconciling`，不能跳号初始化。
- 迁移 `019` 在不增加表、索引或触发器数量的前提下收紧现有 head 触发器：INSERT 必须关联匹配的 revision 0 unchanged receipt；UPDATE 必须关联从旧 revision 到下一 revision 的 changed=true receipt。
- 初始化 revision 0 head 只是登记“Vio 当前已确认的 Engine 投影”，revision 在操作前后均为 0；它不是状态演化或 revision 推进。已有 head 时 changed=false 只允许内容一致的幂等确认，不替换 currentRequestId，也不改变 revision。
- stateHash 在 Vio 运行时按格式、subject/revision 关联及同 revision 一致性验证，并作为 Engine 权威状态内容的不透明标识保存；Vio 实际复算的是最小 snapshot 的 projection.contentHash。测试可用完整固定 Engine fixture 计算期望 stateHash，但运行时不声称拥有或证明完整权威 SubjectState。

### 新增验收与结果

- 新增首次 revision 0 初始化、相同结果重放、第二份相同 revision 回执、已有 head=N 的 changed=false 不变、无 head 非零/changed 隔离，以及初始化前后真实 SQLite 重启恢复测试。
- 补充四类 error envelope 跨重启终态恢复、success/error 互相覆盖拒绝、operationId/responseId 冲突隔离，以及迁移 019 的 5 表/6 索引/10 触发器精确清单断言。
- JavaScript 语法检查通过；Vio V2 专项 29/29、Vio V1 + RFC 8785 回归 17/17、迁移相关套件 56/56、后端全量 95/95 通过；Markdown 相对链接与 `git diff --check` 通过。
- 未修改前端或 Continuity Engine，未连接 Engine，未开始双方共享测试、网络或生产 Adapter。

## 2026-08-02｜第一轮最小连接 Vio V2 结果与投影基础

### 基线与目标

- 施工前确认 Vio `main` 基线为 `c1e1336899b9225346f92a5d1cd108cb29db3b3b`，本地 Continuity Engine 基线为 `c732f357c0c6f62dc2c16d1edef0e003b2fbfb11`；两边开始时均干净。
- 完整核对 Vio V1 Schema/hash/Binding/请求仓储、v1.1 第 19.5—19.8 节，以及 Engine `integration_results.py`、结果工厂/hash/持久化和 E3 ContractTestAdapter/测试；字段、hash、revision 和四类错误语义一致。
- 本阶段目标仅是 Vio 对第一轮 operation/response/error envelope/stateProjection 的严格验证、幂等保存、revision 隔离和跨重启恢复，不建立双方调用或网络连接。

### 实现与数据边界

- 新增固定 success/error envelope 模型和严格白名单 validator；拒绝未知字段、旧别名、历史 `IDEMPOTENCY_CONFLICT`，只接受 `IDEMPOTENCY_KEY_REUSED / never` 及 v1.1 固定四类错误。
- 新增默认未配置的 transport seam 和显式 test-only fixture。发送侧始终读取 V1 第一次持久化的完整逻辑请求，不重建 createdAt/hash/Observation/fact；应用和 HTTP 未注册该 seam。
- 迁移 `019` 新增不可覆盖结果账本、独立 Engine 投影版本/回执/当前指针和非敏感隔离事件。它不复用或改写 legacy/unverified `state_updates`。
- 首次合法 `changed=false / revision 0` 保存结果、投影版本和请求回执并初始化已确认 head，但不推进 revision；已有 head 时 changed=false 不改变指针。`changed=true` 仅在 hash、唯一性、revision 和 CAS 全部通过后推进一次。旧 revision、跳号、错 subject/binding/version、引用/hash/ID 冲突进入 reconciling 或 quarantine，不自动修复或盲重试。
- 结果接收、投影保存和指针推进使用明确阶段与事务；在三个阶段故障后均可由新 SQLite/服务实例继续，且不再次调用 test transport。

### 验证与未完成边界

- Vio V2 专项 29/29 通过；Vio V1 + RFC 8785 回归 17/17 通过；迁移相关套件 56/56 通过；后端全量 95/95 通过。
- 覆盖迁移 `019` 全新安装与 `018 → 019` 升级、外键/唯一/检查/触发器、不可变结果、同 revision 内容唯一性、engineUpdateId 冲突、事务回滚及真实临时 SQLite 重启恢复。
- 未修改前端、Continuity Engine、旧 `state_updates`、应用 HTTP 路由或软件版本；未调用 Engine、Python 子进程、模型、MCP、Tool、设备或网络。
- 双方仍未连接，第一轮共享测试尚未开始。下一阶段是在新的明确授权下进行双方共享验收；网络和生产 Adapter 继续留待后续。

## 2026-08-02｜Vio V1 提交前 RFC 8785 定点修复与验收

### 验收问题与定点处理

- 提交前严格验收确认 Vio V1 的唯一阻塞为 RFC 8785 多类型一致性测试证据不足；本轮没有扩大 Vio V1 范围。
- 新增独立 RFC 8785 测试，使用固定官方标准期望覆盖 Section 3.2.2/3.2.3 综合向量、Appendix B 代表性数字、Unicode 与属性排序、混合嵌套结构、UTF-8 固定字节，以及非法数字、孤立代理项和非 JSON JavaScript 值。
- 新增与本地 Continuity Engine 提交 `c732f35` 的只读跨语言对照；双方对独立语料生成的 canonical JSON、UTF-8 字节和 SHA-256 全部一致，Vio 运行时代码未依赖 Python 或 Engine。
- 新测试发现一个真实边界问题：字符串以孤立高代理项结尾时原实现未拒绝。仅在 `first-round-hashing.js` 补齐末尾边界判断；未引入依赖、未修改机器契约或架构。

### 完整验证与边界

- RFC 8785 专项 9/9 通过；Vio V1 专项合计 17/17 通过；后端全量 66/66 通过；迁移相关 27/27 通过。
- 已有数据库临时副本从迁移 017 顺序升级到 018：18 个迁移记录齐全、两张新表和四个不可变触发器存在、外键检查 0 违规；全新数据库路径也由专项和全量测试覆盖。
- 三项正式结果保持为 `sha256:2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824`、`sha256:ec07ad9ba66d1ffcdfa9177cd61bec1b880ad6ee99a6ec6449e732c1b86002d0`、`sha256:c75b72194c0158a549f3fb30f04a5147ea11a4e777cb1a9cc1a54da6b93359f6`。
- 没有修改 Continuity Engine、前端、现有连接契约或其他业务模块；没有连接 Engine、调用外部能力、开始 Vio V2 或双方共享测试。下一阶段仍为 Vio V2，须等待新的明确任务。

## 2026-08-02｜第一轮最小连接 Vio V1 严格机器契约基础

### 阶段目标与基线

- Continuity Engine E1/E2/E3 已独立完成并同步至提交 `c732f35`，引擎侧基线为 203 项测试；本阶段只读核对该提交和相关实现，没有修改引擎。
- 本阶段只实现 Vio 侧“第一轮严格机器契约构造与固定 Binding 基础”，不连接 Engine，不实现投影接收或双方共享测试。

### 实际实现

- 新增第一轮三份 Draft 2020-12 严格 Schema 的封闭本地 registry 和验证器；固定三个绝对 URN `$id`、两个本地 `$ref`、全层未知字段拒绝、UTC `Z` 时间和状态写字段禁令。
- 新增显式 RFC 8785 JSON 规范化与 UTF-8 SHA-256，复算 `contentHash`、`requestHash`、`bindingFixtureHash` 三项正式向量。
- 新增固定 SubjectBinding 测试装载：只接受 `binding-001`/`user-001`/`assistant-001`/`subject-001`、版本 1、`active` 和固定时间，不提供创建、更新、解绑或重绑定 API。
- 新增专用请求构造服务：复用现有仓储验证 User、平台 assistant、Conversation、Message、MessageVersion 和来源 `message_created` Event，生成唯一 fact 与 Observation，严格验证后只保存、不发送。
- 新增迁移 `018_create_continuity_v1_contract_foundation.sql`，持久化不可变 Binding fixture 和逻辑请求输入。相同 requestId 重试及进程重启后返回第一次的 requestId、createdAt、requestHash 和规范化请求；不保存伪造的 operation、response 或 stateProjection。

### 验证结果

- Vio V1 专项：`node --test tests/continuity-integration-v1-flow.test.js`，8/8 通过。
- 后端全量：`pnpm test`，57/57 通过，0 失败、0 跳过、0 取消。
- 三项结果分别为 `sha256:2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824`、`sha256:ec07ad9ba66d1ffcdfa9177cd61bec1b880ad6ee99a6ec6449e732c1b86002d0`、`sha256:c75b72194c0158a549f3fb30f04a5147ea11a4e777cb1a9cc1a54da6b93359f6`。

### 边界与下一步

- Vio Event、PlatformObservation、引擎 Event 和 StateMutation 保持结构隔离；构造过程不创建内部 Event、不推进 Vio SubjectState、不读取 ConversationSummary 替代消息事实。
- 没有修改前端、Continuity Engine、现有 `state-updates` 接口或软件版本；没有调用 Engine、模型、MCP、Tool、设备或网络。
- Vio V2 仍需实现受控 Engine 调用、operation/response/stateProjection 幂等接收、revision 冲突隔离与跨重启恢复；之后才能运行双方共享测试。网络和生产 Adapter 继续留待后续阶段。

## 2026-07-30｜Continuity 双方工程档案最终只读复核通过

### 本阶段结果

- Vio 与 Continuity Engine 双方工程档案同步已经完成。
- 最终只读核对曾在 Continuity Engine 当前档案中发现两处旧描述：旧直连拓扑和无条件 `StateMutation`。
- Continuity Engine 已完成上述两处定点文档修正；Vio 已完成根 README、后端 README、后端 API 状态、后端规划当前状态、路线图、ADR-032、BE-ADR-035、v1.1 当前入口和 14a 当前状态共九处阶段状态同步。
- 双方再次只读核对后，当前连接拓扑、Action Gate、SubjectState 唯一权威、机器契约、历史记录保留和运行时未实现边界一致。
- 最终结论：**双方档案一致，可以制定第一轮最小连接施工提示词。**

### 历史与边界

- 旧直连拓扑和无条件 `StateMutation` 只作为历史快照、修订原因或失效说明保留，不再代表当前有效架构。
- 本次只追加施工前工程记录并推进当前状态，没有删除、覆盖或伪造改写历史日志。
- 第一轮最小连接施工提示词尚未共同制定；第一轮代码施工、共享测试和运行时连接仍未开始。
- 没有修改前端、后端业务代码、测试、数据库迁移、配置或 Continuity Engine，也没有开始连接施工。

### 下一步入口

- 双方共同制定第一轮最小连接施工提示词，并在新的明确开发任务中分别确定施工范围和顺序。
- 在提示词共同确认和后续施工授权下达前，不编写连接代码、不运行共享测试、不宣称运行时连接已经实现。

## 2026-07-30｜Continuity Integration Contract v1.1 正式接受与 Vio 工程档案同步

### 当前结论与阶段目标

- Continuity Engine 已通过《Engine Contract Final Read-Only Short Confirmation v1》正式接受 `Continuity Integration Contract v1.1`。
- 最终短确认只复核上轮阻塞项、第 19 节、14a 对齐文字和相关引擎源码边界；没有重新讨论或推翻长期架构，也未发现新的架构问题。
- 本阶段只把最终接受结果同步到 Vio 的契约状态、README/导航、两级 ADR、两级工程日志、路线图、API/架构状态和更新记录；没有开始连接施工。

### 最终确认覆盖

- 三份 Draft 2020-12 Schema、三个唯一绝对 URN `$id`、两个可唯一解析 `$ref`、全层严格未知字段拒绝、`facts`/`observations` 各一项，以及带 `Z` 的 RFC 3339 UTC/格式断言。
- 正式 conformance vector 的 Observation/fact/identity/conversation/message/version 引用和正文唯一位置。
- 固定 SubjectBinding fixture 及 `contentHash`、`requestHash`、`bindingFixtureHash` 的独立复算结果。
- 第一轮唯一幂等错误 `IDEMPOTENCY_KEY_REUSED / never`、revision 全不等冲突、投影唯一性和无变化 `engineUpdateId=null`。
- 三个确定性 test double 只在 Continuity Engine 进程内，第一轮无 Capability 往返；唯一摄取入口是独立、test-only、进程内 ContractTestAdapter。

### 历史与当前状态

- 第一次、第二次及最终校准前的“暂不接受”结论继续作为当时真实审核历史保留；Vio 完成对应纯文档修订后，引擎最终正式接受 v1.1。
- 已完成的是长期架构、SubjectState 权威、Observation/内部 Event 边界和第一轮机器契约对齐。
- 尚未实现 PlatformObservation/Schema validator、SubjectBinding/hash 运行时能力、ContractTestAdapter、双方持久化幂等账本、跨重启完整重放、Vio 投影接收器、revision 冲突隔离、实际连接和第一轮共享测试。

### 文档同步与验证边界

- 新增 `docs/后端/14c-Engine-Contract-Final-Read-Only-Short-Confirmation-v1.md`，按现有 14/14a/14b 编号链保存引擎最终接受证据。
- 更新 v1.1、14a、README/导航、API/架构/关系图、ADR、路线图、两级日志和更新记录；14b 的第二次审核“暂不接受”历史事实保持不变。
- 本轮只修改 Markdown，没有修改 Vio 前端/后端代码、测试、数据库迁移或 continuity-engine，没有暂存、提交或推送。

### 下一步入口

- 等待 Continuity Engine 完成自身工程档案同步。
- 双方核对档案中的状态、范围、边界和下一步一致后，共同制定第一轮最小连接施工提示词并分别确定施工顺序。
- 当前没有授权开始代码、共享测试或生产连接。

## 2026-07-29｜continuity-engine 与 Vio 后端连接设计（第一阶段）

### 阶段目标与真实现状

- 本阶段只进行两个仓库的架构核对和连接设计，制定 `Continuity Integration Contract v1`；状态是“连接设计完成，尚未开始实际接入”。
- Vio 已具备用户/助手/会话、消息版本、摘要来源、Event、私域安全投影、Permission/Security 和 Token 基础，但没有引擎客户端、主体绑定、投递回执、幂等状态和权威状态投影协议。
- continuity-engine 已具备 SubjectState revision、Event/Evolution 和 Wake/Perception/Thinking/Action/Learning 链，但没有面向 Vio 的生产端点、鉴权、多租户、平台事实包或外部 Observation 契约。
- continuity-engine 当前使用本地 JSON repository、没有数据库迁移目录；其本地 frontend/HTTP 聊天链只是调试入口，不能成为 Vio 前端生产连接。

### 核对依据

- 完整检查 Vio 的 README、后端规划、工程日志、更新记录、两级 ADR、API、数据库设计、`001`—`017` 迁移，以及 SubjectState、Context、Event、ConversationSummary、Private Space、Permission/Security Policy、Token Budget 的源码和测试。
- 只读检查 continuity-engine 的 README、`docs/project_memory` 规划/日志/决策/API相关说明、SubjectState/Event/Evolution/Perception/Thinking/Learning/Wake/Action/Revision/Resource/接口/存储源码和测试。
- 以源码、迁移和测试为进度依据；没有生产代码支撑的接口没有计为完成。

### 发现的冲突与决定

- Vio 现有开发 `state_update` 可推进平台当前状态，缺少引擎 revision/update ID，与唯一权威原则冲突；后续必须收口，旧数据不能自动覆盖引擎。
- Vio 现有 Context 只能解释为权限筛选前的平台事实投影，不能和引擎各自组装一套最终 Context。
- 决定 continuity-engine 是 SubjectState 唯一权威源；Vio 只保存投影、快照、缓存和审计。Vio Event 只变为无状态修改能力的 Observation，由引擎决定是否影响状态。
- 前端只连接 Vio；Vio负责用户数据、权限安全、私域安全存储、Token 和模型/Tool/设备安全通道，引擎负责最终认知 Context、连续性判断和状态演化；数据库不合并。

### 文档变更

- 新增 `docs/后端/14-continuity-engine连接契约v1.md`。
- 更新根/后端/规划 README、总体架构、对话连续性、API/事件契约和后端 API 文档。
- 新增后端 BE-ADR-035 与项目 ADR-032，并更新两级日志和更新记录。
- 所有拟议接口均标为“拟新增”或“待引擎确认”，未写成已实现。

### 验证

- `cd backend && pnpm test`：49/49 项通过，0 项失败，0 项跳过。
- `git diff --check`：通过。
- 本阶段只改文档，没有为了测试修改业务代码。

### 后续任务、引擎依赖与未决问题

- Vio 后续需在单独阶段设计/实现助手—引擎主体绑定、continuity 请求/状态、平台事实包、Observation 投递、状态投影、幂等、故障对账、服务鉴权和可信 Token 账本。
- continuity-engine 需确认生产传输/鉴权、Interaction/Observation、请求状态、能力调用往返、状态快照/增量、错误/重试、多租户和 Token 关联协议。
- 尚未确定同步/异步/流式形态、具体端点、ID 绑定、摘要回查、私域读写提案、部分成功恢复、主动 Wake 结果和历史 Vio 状态迁移。

### 本阶段未做与下一阶段

- 未修改前端 `src`，未修改 continuity-engine，未写连接代码，未新增数据库迁移，未接真实模型/MCP/Tool/设备，未新增业务模块。
- 下一阶段应先由规划窗口审核连接契约，并等待引擎窗口确认阻塞接口；未经新指令不开始接入。

## 2026-07-28｜平台后端 14：数据导出与未来迁移

### 目标

- 为用户数据、AI 主体数据、安全配置、能力/设备与生活数据建立统一版本化导出边界。
- 在不生成文件、不连接外部存储/机器人、不执行迁移的前提下，验证归属、完整性和高风险安全流程。

### 完成

- 新增 Export Schema v1，支持 `full`、`selected`、`migration` 与十二类数据范围。
- 新增导出预检与记录：保存版本、创建时间、范围、计数、字段/外键检查、Permission 状态、结果及 AuditLog 引用。
- 新增 `data_export` Permission/Security 资源；导出准备固定经过高风险逐次 Confirmation。
- 新增机器人/其他载体未配置契约，所有响应固定不连接、不传输、不执行。
- 后端版本更新为 `0.18.0`，项目更新记录为 `0.34.0`；README、API、ADR、数据库与路线规划同步更新。

### 验证

- `pnpm test`：49 项后端测试通过，0 项失败。
- 迁移保留既有 Permission、Security Policy、Confirmation 与 AuditLog；`PRAGMA foreign_key_check` 无异常。
- 前端 `src` 无改动；未生成导出载荷/文件，未连接外部存储、机器人、设备或第三方服务，未执行真实迁移。

## 2026-07-28｜平台后端 13：语音唤醒、主动提示与 Token 控制

### 当前状态

- 平台后端新增 Wake、主动提示、Token 预算/使用记录和助手后台策略基础。
- 所有主动准备均经过用户/主体隔离，并按需要接入 Permission、Security Policy 与 Confirmation。
- 前端 `src` 保持不变；没有真实语音、系统唤醒、后台执行、消息投递或模型调用。

### 已完成内容

- 支持四类 Wake、应用内授权和后台允许范围；真实麦克风与系统能力固定未连接。
- 支持 Event 驱动提示规则、四级消息优先级、确认要求和固定未投递的准备记录。
- 支持每日/会话 Token 预算、三类超额策略和显式计量账本，明确平台未调用、未计费。
- 支持 `idle` / `active` 后台状态、频率限制、允许类型和安静时段配置，不启动调度器。
- 新增 `proactive_interaction` 安全范围与六类最小 Event；后端版本更新为 `0.17.0`，项目记录更新为 `0.33.0`。

### 验证

- `pnpm test`：46 项后端测试通过，0 项失败。
- 验证旧数据迁移、外键、隔离、授权、提示确认、静默抑制、Token 日/会话限额和超额策略。

### 下一步

- 本阶段完成后停止；不进入平台后端 14，不接真实语音、AI 或外部服务。

## 2026-07-28｜平台后端 12：账号、数据库与数据隔离

### 当前状态

- 平台后端新增 User Space、助手列表/当前助手和统一数据隔离检查。
- 五类数据先按数据库复合归属过滤，AI 私域、设备和生活资源再经过 Permission 与 Security Policy。
- 前端 `src` 保持不变；没有真实登录、第三方账号、语音、模型或外部连接。

### 已完成内容

- 为每个用户建立唯一空间根，保存明确未认证的开发期身份状态和同用户当前助手指针。
- User/Space、首个 Subject/当前助手分别原子提交；既有用户升级时稳定回填空间和当前助手。
- 当前助手切换不修改 Global Settings、Private Space、SubjectState、对话、事件或生活数据。
- 新增用户、AI、设备、生活、事件五类数据边界描述及具体资源访问预检 API。
- 后端版本更新为 `0.16.0`，项目记录更新为 `0.32.0`。

### 验证

- `pnpm test`：43 项后端测试通过，0 项失败。
- 验证迁移、外键、多用户/多助手隔离、当前助手持久化、固定查询过滤和 Permission/Security Policy 联动。

### 下一步

- 本阶段完成后停止；不进入平台后端 13，不开发语音、真实认证或外部服务。

## 2026-07-28｜平台后端 11：生活管理模块

### 当前状态

- 平台后端新增管账、月历、身体管理和本地记忆数据闭环。
- 所有生活数据按用户与主体隔离，并通过 `life_data` Permission 与 Security Policy 保护。
- 前端 `src` 保持不变；没有支付、银行、穿戴设备、真实模型或第三方连接。

### 已完成内容

- 管账支持收入/支出、预算提醒、分类统计和月度汇总；金额以最小货币单位保存。
- 月历支持纪念日、生理期、亲密记录、普通事件与提醒。
- 身体管理支持体重、三围、目标、趋势和显式 AI 建议字段，明确不用于医疗诊断。
- 本地记忆支持保存、查询、参与 Context 和导出标记，并提供独立受控投影。
- 生活数据读写经过 Permission → Security Policy → Confirmation；Event 不复制敏感正文。
- 后端版本更新为 `0.15.0`，项目记录更新为 `0.31.0`。

### 验证

- `pnpm test`：40 项后端测试通过，0 项失败。
- 验证功能闭环、重启持久化、隔离、安全策略拒绝、高风险确认、事件脱敏及外键完整性。

### 下一步

- 本阶段完成后停止；不进入平台后端 12，不接支付、银行、健康设备或真实 AI。

## 2026-07-28｜AI 私域数据层

### 当前状态

- 平台后端新增 AI Private Space 数据、版本、安全访问、独立 Context 投影与导出结构预留。
- 前端 `src`、页面和 mock 保持不变；没有模型、continuity-engine、机器人或外部设备调用。

### 已完成内容

- 使用独立 SQLite 表隔离 User Space 与 Assistant Private Space，并以用户、助手、Space 复合归属阻止串用。
- 支持 AI 状态、认知、长期偏好、工作记录和私有笔记五类显式输入；内容历史不可变追加。
- 私域正文操作经过 `private_domain` Permission → Security Policy → Confirmation，事件与审计不复制正文。
- 新增 `private_space_created`、`private_memory_updated`、`private_state_changed`，以及未来 Context/导出接口。
- 后端版本更新为 `0.14.0`，项目记录更新为 `0.30.0`。

### 验证

- `pnpm test`：37 项后端测试通过，0 项失败。
- 验证版本历史、持久化、隔离、策略拒绝、高风险确认、事件脱敏、上下文投影、导出清单和外键完整性。
- 前端 `src` 无修改，无真实 AI、外部 API、设备或机器人连接。

### 下一步

- 本阶段完成后停止；等待新的明确开发指令。

## 2026-07-28｜权限与自定义安全栏

### 当前状态

- 已在 Permission、Security、Confirmation 和 AuditLog 基础上完成用户可配置的安全策略系统。
- 安全检查固定经过 Permission → Security Policy → Confirmation，任何结果仍只表示执行准备，不执行真实资源。
- 前端 `src`、页面、组件和 mock 未修改；未连接设备、未执行 Tool、未连接外部服务。

### 已完成内容

- 新增 `012` SQLite 迁移与 Security Policy、用户安全偏好、短时策略会话授权数据结构。
- 支持五种策略规则、默认安全等级、高风险操作策略、低/中风险自动确认范围和禁止范围。
- `session_allow` 只在具体确认消费后建立 30 分钟精确授权；高/极高风险始终逐次确认。
- Confirmation 新增确认原因、风险说明、用户选择、策略版本和开发期安全会话作用域。
- Permission 创建、变化和撤销分别生成 `permission_created`、`permission_changed`、`permission_revoked`；需要确认生成 `confirmation_required`。
- 策略/偏好变更和安全结果进入最小 AuditLog；后端版本更新为 `0.13.0`。

### 验证

- `pnpm test`：34 项后端测试通过，0 项失败。
- 验证策略/偏好 CRUD、精确匹配、软删除、持久化、用户隔离、审计和生命周期 Event。
- 验证自动确认、高风险底线、会话授权精确作用域/过期/版本失配及确认防重放。
- 验证从 `011` 升级时保留 Event/AuditLog 子表引用并通过外键完整性检查。

### 下一步

- 本阶段完成后停止；不进入平台后端 10，不开发真实 AI 私域、设备/Tool 执行或外部服务。

## 2026-07-28｜手机、家电与穿戴设备适配层

### 当前状态

- 已完成 Device Registry、统一 Capability、未来厂商 Adapter 契约、设备授权与操作准备日志。
- 设备操作已接入 Permission、Security、Confirmation、Event 和 AuditLog，但任何结果都不连接或控制真实设备。
- 前端 `src`、页面、组件和 mock 未修改；未调用厂商 API、手机权限、摄像头或家电。

### 已完成内容

- 新增 `011` SQLite 迁移，保存七类设备、四种能力和主体范围未执行操作日志。
- 新增 Xiaomi、Midea、Apple、Android、Generic 五个未来 Adapter 描述，所有契约方法固定不支持。
- 新增设备注册、筛选、启停、授权、操作准备和日志 API；设备创建默认停用，连接状态固定未连接。
- Device Permission 按能力映射 `read` / `control`；设备操作按极高风险每次确认，并记录最小 Device Event、AuditLog 和未执行日志。
- 后端版本更新为 `0.12.0`，同步 README、API、ADR、设备规划和阶段记录。

### 验证

- `pnpm test`：30 项后端测试通过，0 项失败。
- 验证设备类型、能力、Adapter 分派、注册持久化、授权事件、极高风险确认和操作日志。
- 验证设备参数输入、跨用户/主体访问和部分事务写入被阻止。
- 验证所有设备结果保持 `not_connected`、`not_observed`、`not_executed`，没有厂商请求。

### 下一步

- 本阶段完成后停止；不继续开发真实设备连接、手机控制、家电厂商接口或连续性引擎下一阶段。

## 2026-07-28｜MCP、Skill、插件与 Tool 基础系统

### 当前状态

- 已完成 Tool、MCP、Skill、Plugin 注册元数据和主体统一能力视图。
- Tool 执行准备已接入 Permission、Security、Confirmation 与 AuditLog，但任何结果都不执行真实 Tool。
- 前端 `src`、页面、组件和 mock 未修改；未连接 MCP、设备、第三方服务或真实模型。

### 已完成内容

- 新增 `010` SQLite 迁移和四类用户范围注册表，以及主体范围的 Tool 使用准备记录。
- 支持注册项创建、查询和启停；Capability Service 统一返回分类、权限状态和 Tool 最近使用记录。
- MCP 地址只保存无凭据配置，响应固定未连接；Plugin 只保存版本与依赖，响应固定未安装。
- Tool 准备固定经过注册状态、Permission 和 Security/Confirmation，记录执行状态 `not_executed` 与零外部消耗。
- 后端版本更新为 `0.11.0`，同步 README、API、ADR、扩展能力规划和阶段记录。

### 验证

- `pnpm test`：27 项后端测试通过，0 项失败。
- 验证四类注册、启停、持久化、分类、权限投影、确认流程、审计关联和最近使用。
- 验证执行参数、含凭据服务地址、非法枚举及跨用户/主体访问被拒绝。
- 验证 MCP 未连接、Plugin 未安装、Tool 未执行，外部调用和 Token 消耗均为零。

### 下一步

- 本阶段完成后停止；不继续开发真实 MCP、手机权限、家电控制、设备或 continuity-engine 下一阶段。

## 2026-07-28｜平台后端 6：模型与 API 路由

### 当前状态

- 已完成用户范围的 Provider、Model、默认/备用路由规则与本地模型选择闭环。
- 六类任务可配置明确默认和备用模型；选择只返回配置结果，不连接 GPT、Claude、GLM 或其他外部服务。
- 前端 `src`、页面、组件和 mock 未修改。

### 已完成内容

- Provider 增加 Base URL、接口格式与测试状态；Model 增加八类能力标签、费用说明与测试状态。
- 新增聊天、长文本、图片、视频、语音、搜索六类 `model_routing_rules` 数据、服务和 API。
- Router 支持默认模型、Provider 停用时的已配置备用模型，以及无启用规则时的稳定目录回退。
- API Key 只保留拒绝写入的安全存储端口，数据库和接口不保存或返回真实密钥。
- 后端版本更新为 `0.10.0`，同步 API 文档、README、数据库说明、ADR 与阶段规划。

### 验证

- `pnpm test`：24 项后端测试通过，0 项失败。
- 验证六类任务、规则创建/读取/更新、默认/备用选择、重启持久化、0.9.0 数据升级、归属隔离、能力约束和密钥拦截。
- 验证 Router 不调用模型或外部 API，不读取或修改 Context、SubjectState、Assistant Global Settings。

### 下一步

- 本阶段完成后停止；不提前进入平台后端 7，不开发 MCP、Tool、真实模型或前端接入。

## 2026-07-28｜平台后端 5：AI 助手全局设定

### 当前状态

- 已完成 AI Assistant Global Settings 数据、服务、接口与 Context 投影基础。
- 助手长期身份/偏好与动态 SubjectState 分离，不接入模型、外部 API 或 continuity-engine。
- 前端 `src`、页面、组件和 mock 未修改。

### 已完成内容

- 新增 `008` SQLite 迁移和 Subject 一对一全局设定关系；既有主体回填空白默认设定。
- 支持名称、头像/标识、人格描述、表达方式配置、关系定义、长期要求和禁止事项。
- 名称/头像保持 Subject 唯一来源，扩展设定独立保存，并通过单一 API 对象读取与更新。
- 实际更新与 `subject_updated` Event 原子提交，无变化不写入；Context 读取完整设定但不调用模型。
- 后端版本更新为 `0.9.0`，同步 API 文档、数据库说明、ADR 与阶段规划。

### 验证

- `pnpm test`：23 项后端测试通过，0 项失败。
- 验证持久化、Context 投影、跨用户隔离、输入校验、无变化语义、SubjectState 分离与事务回滚。
- 原有后端模块测试全部通过；前端生产构建将在提交前回归验证。

### 下一步

- 等待本阶段审查；不提前进入平台后端 6，不接入模型、外部 API、continuity-engine 或前端页面。

## 2026-07-28｜平台后端 4：上下文、摘要与跨窗口连续

### 当前状态

- 已完成可追溯 ConversationSummary、跨窗口摘要读取、不可变 SubjectState / `state_update` 和只读 Context 装配基础。
- 摘要与状态均由开发调用方提交；平台不调用 AI、外部 API 或 continuity-engine，也不生成摘要或推演状态。
- 前端 `src`、页面和 mock 未修改。

### 已完成内容

- 新增 `007` SQLite 迁移和摘要、来源、主体状态、当前指针、未解决 Event 关系。
- 摘要至少引用同会话 MessageVersion 或同主体 Event；跨窗口只返回同一主体其他 Conversation 的最新摘要。
- `state_update` 保存状态、情绪、强度、原因、未解决事件、连续性约束及 MessageVersion/Event/ConversationSummary 来源。
- Context 按产品顺序装配主体设定、当前状态、事件、近期消息和跨窗口摘要，并对未实现的系统规则内容与 Memory 明确占位。
- 新增相关 API、后端 ADR 与项目级决策记录，后端版本更新为 `0.8.0`。

### 验证

- `pnpm test`：20 项后端测试通过，0 项失败。
- 验证持久化、来源追溯、状态版本、当前指针、上下文顺序、跨窗口读取、隔离、不可变约束与事务回滚。
- 原有后端模块测试全部通过；前端生产构建将在提交前回归验证。

### 下一步

- 等待本阶段审查；不提前进入后续平台后端阶段，不接入模型、外部 API、continuity-engine、MCP、Tool、设备或 AI 私域。

## 2026-07-28｜平台后端 7：对话系统基础

### 当前状态

- 已完成主体范围的 Conversation、稳定 Message 和不可变 MessageVersion 基础闭环。
- 当前只保存与查询线性文本对话；前端对话页和 mock 未修改，没有接入真实 AI 或连续性引擎。
- 用户消息编辑与主体消息重生成均追加新版本，旧正文不覆盖；主体内容由开发调用方显式提交，不代表模型生成。
- 未实现删除、分支、重置、上下文装配、摘要、Memory 或 SubjectState。

### 已完成内容

- 新增 `006` SQLite 迁移、三张对话表、用户/主体/会话复合外键、稳定消息序号和当前版本指针。
- 新增 Conversation 创建/列表/详情、Message 创建/列表/详情、用户编辑、主体重生成记录及版本历史/单项接口。
- 编辑与重生成要求匹配当前 `baseVersionId`；陈旧写入返回冲突，不产生半成品版本。
- 新增 `conversation_created`、`message_created`、`message_updated`、`message_regenerated`，事件只保存 ID 和版本关系，不复制标题或正文。
- 消息、初始版本、当前指针、会话活动时间与对应 Event 在同一事务提交。
- 后端版本更新为 `0.7.0`，同步更新 API 文档、ADR 和后端规划。

### 验证

- `pnpm test`：17 项后端测试通过，0 项失败。
- 验证 Conversation/Message 创建查询、三类发送者、消息顺序、编辑/重生成、父版本链和重启持久化。
- 验证跨用户、跨主体、跨会话隔离，非法操作、未知字段和陈旧版本冲突。
- 使用 SQLite 触发器验证 Message/Version/当前指针/会话时间/Event 失败时整体回滚。
- 前端页面、组件、mock、前端依赖和 `src/` 均未修改。

### 下一步

- 等待“平台后端 7”审查；未收到新指令前不迁移对话页面，不进入后续模块。
- 不开发 AI 调用、连续性引擎、上下文装配、MCP、Tool、分支、删除或重置。

## 2026-07-27｜平台后端 6：基础 API 层与前后端连接

### 当前状态

- 已完成统一 API 返回格式、User/Subject 扩展接口和 Dashboard 基础接口。
- 前端首次通过独立 API 客户端与 Vite 同源代理连接真实后端，并在应用入口执行非阻塞健康握手。
- 页面、组件和所有 mock 文件保持原样；登录、首次设置和工作台尚未消费真实数据。
- 未接入真实认证、模型、MCP、Skill、Tool、设备、支付、AI 私域或 continuity-engine。

### 已完成内容

- 所有 JSON 接口统一返回 `success`、`data`、`error` 和 `timestamp`，错误包含稳定 `code`、`message` 与可选 `details`。
- 新增开发期当前用户上下文、Subject 列表与基础信息更新，以及用户/主体范围的 Dashboard 聚合接口。
- Subject 实际更新与最小化 `subject_updated` Event 同事务提交；无变化更新不写库、不产生事件。
- Dashboard 只投影现有用户、主体和基础状态，连续性状态明确返回不可用，不模拟连续性结果。
- 新增前端类型化 API 客户端、超时/取消和结构化错误处理、开发代理及非阻塞健康状态记录。
- 新增后端 API 说明文档和相关 ADR，后端版本更新为 `0.6.0`。

### 验证

- `pnpm test`：14 项后端测试通过，0 项失败。
- 验证 User、Subject、Dashboard 接口、统一成功/错误格式、跨用户隔离和 SQLite 持久化。
- 验证 Subject 更新事件、无变化更新，以及事件写入失败时主体更新整体回滚。
- 前端生产构建通过；开发代理已实际完成健康检查和 User → Subject → Dashboard 闭环。
- `src/pages`、`src/components`、模拟数据和 mock 文件未修改或删除。

### 下一步

- 等待“平台后端 6”审查和后续明确指令，再决定页面真实数据迁移与认证方案。
- 未收到下一阶段指令前，不接入其他模块，不开发 MCP、Tool、设备或连续性引擎。

## 2026-07-27｜平台后端 5：安全系统基础

### 当前状态

- 已完成 Permission 之上的统一安全预检、风险判断、确认流程和独立 AuditLog 闭环。
- 安全层只返回 `allow`、`confirm`、`deny` 与未执行状态，不调用真实支付、设备、MCP、Tool、模型或 AI 私域。
- 敏感数据仅建立五类分类元数据和只读查询，没有建立真实 API Key 或敏感正文的写入结构。
- 前端与 `src/` 未修改，continuity-engine 未开发。

### 已完成内容

- 新增安全确认与审计 SQLite 迁移、用户/主体归属约束和查询索引。
- 新增四级风险、敏感操作规则和三种确认要求。
- 新增完整作用域、权限快照与策略指纹绑定，五分钟过期、单次消费、防跨用户及防重放确认。
- 新增只追加最小 AuditLog，并接入 Permission 变更、APIProvider 配置变更和隐私访问安全检查。
- 修正安全预检中的 `allow_once` 时点：确认前不消费，安全链满足后才消费。
- 后端版本更新为 `0.5.0`，新增分层安全、审计隔离和确认防重放 ADR。

### 验证

- `pnpm test`：12 项测试通过，0 项失败。
- 验证无确认、每次确认、用户自定义确认以及四级风险判断。
- 验证权限拒绝不可绕过、确认作用域/策略隔离、过期、防重放和单次权限最终消费。
- 验证审计生成、筛选、持久化及 Permission/APIProvider 变更记录。
- 前端代码、前端依赖和前端构建配置无修改。

### 下一步

- 等待“平台后端 5”审查。
- 未收到下一阶段明确指令前，不开发 MCP、Tool、设备、AI 私域或连续性引擎。

## 2026-07-26｜平台后端 4：权限系统基础

### 当前状态

- 已完成用户/主体范围的 Permission CRUD、三态判断和 Event 联动闭环。
- 权限按资源类型、资源 ID 和具体操作精确匹配；没有活动规则时默认拒绝。
- 当前只管理和判断权限，不执行真实 MCP、Skill、Tool、设备、手机权限或 AI 私域操作。
- 前端、连续性引擎和真实外部能力未修改或接入。

### 已完成内容

- 新增 Permission SQLite 迁移、用户/主体归属约束、当前规则唯一性和判断索引。
- 新增七类资源、五档权限等级和八种基础操作的稳定枚举。
- 新增权限创建、查询、更新、软删除及条件列表接口。
- 新增 Permission Checker，返回 `allow`、`ask` 或 `deny`，并区分是否允许再次询问。
- 新增 `allow_once` 原子消费，以及权限变更与 `permission_changed` Event 同事务提交。
- 后端版本更新为 `0.4.0`，并记录精确匹配/默认拒绝与事务化审计两项 ADR。

### 验证

- 34 个后端 JavaScript 文件通过语法检查。
- `pnpm test`：9 项测试通过，0 项失败。
- 验证 Permission CRUD、五档判断、单次许可、删除、事件联动与重启持久化。
- 验证七类资源、无规则默认拒绝、跨用户/主体隔离和非法枚举拒绝。
- 前端 `src/`、前端依赖和前端构建配置无修改。

### 下一步

- 等待“平台后端 4”审查。
- 未收到下一阶段明确指令前，不开发 MCP、Tool、设备、手机权限、AI 私域或连续性引擎。

## 2026-07-26｜平台后端 3：模型路由与 API 管理基础系统

### 当前状态

- 已完成 APIProvider、Model 与 Model Router 本地配置闭环。
- 平台可以保存用户所属 Provider 与模型目录，按能力查询，并从启用 Provider 中返回规则匹配模型。
- 当前没有真实 API Key、外部网络请求、模型调用或 Token 消耗。
- 前端未修改；连续性引擎、权限、MCP、Skill、Tool 和设备未开发。

### 已完成内容

- 新增 Provider/Model SQLite 迁移、归属外键、能力标签关系和查询索引。
- 新增 Provider 创建、列表/单项查询和启停状态更新接口。
- 新增 Model 创建、单项查询和五类能力查询接口。
- 新增 Model Router 选择接口，按任务类型执行稳定、可解释的本地规则匹配。
- 新增 API Key 空引用占位、安全状态响应和凭据输入/Base URL 凭据拦截。
- 后端版本更新为 `0.3.0`，并记录路由与真实调用分离、Key 禁止写入两项 ADR。

### 验证

- 29 个后端 JavaScript 文件通过语法检查。
- `pnpm test`：7 项测试通过，0 项失败。
- 验证 Provider 与 Model 创建、查询、状态更新、能力检索、路由选择及重启持久化。
- 验证停用 Provider 不参与路由，跨用户访问、非法能力与密钥输入被拒绝。
- 前端 `src/`、前端依赖和前端构建配置无修改。

### 下一步

- 等待“平台后端 3”审查。
- 未收到下一阶段明确指令前，不接入真实模型，不开发权限、MCP、Tool、连续性引擎或设备。

## 2026-07-25｜平台后端 2：事件总线基础系统

### 当前状态

- 已建立独立 Event 模块、开发数据库迁移、事件记录能力和用户范围内的事件查询接口。
- 事件可以绑定用户，并可选绑定该用户所属的 AI 主体；归属关系同时由服务校验和数据库外键约束。
- 当前只保存和查询事件，不消费事件，不调用连续性引擎或 AI 模型。
- 前端未修改；权限、MCP、Tool 和设备业务模块未开发。

### 已完成内容

- 新增 `events` 表，保存事件 ID、用户、可选主体、事件类型、来源、发生/记录时间、事件数据、摘要和状态。
- 新增 `appearance_changed`、`subject_updated`、`permission_changed`、`life_record_created` 和 `device_changed` 五种基础事件类型。
- 新增事件创建、单项查询及按用户、主体、发生时间、类型和状态筛选能力。
- 新增 `/api/v1/users/:userId/events` 创建/列表接口和 `/api/v1/users/:userId/events/:eventId` 单项查询接口。
- 事件按用户范围访问；主体级事件拒绝跨用户绑定；事件正文拒绝明显的密钥、密码、验证码和证件号字段。
- 后端版本更新为 `0.2.0`，并记录事件不可原地改写、用户嵌套路由和复合归属外键决策。

### 验证

- `pnpm test`：5 项测试通过，0 项失败。
- 验证五类事件均可创建和保存，重启服务后仍可读取。
- 验证按用户、主体、时间范围和事件类型查询。
- 验证非法事件类型、秘密字段和跨用户主体绑定均被拒绝。
- 前端 `src/`、前端依赖和前端构建配置无修改。

### 下一步

- 等待“平台后端 2”审查。
- 未收到下一阶段明确指令前，不开发事件消费、连续性引擎、模型路由、权限系统、MCP、Tool 或设备。

## 2026-07-25｜平台后端 1：基础服务、账号与数据库闭环

### 当前状态

- Vio Live 平台后端首次可以独立启动并监听 HTTP。
- 已建立开发期 SQLite 数据库、迁移机制、User 和 Subject 基础模块。
- 已完成用户创建、主体创建、持久化、查询和 JSON 返回闭环。
- 前端未修改；未开发连续性引擎、事件、权限、模型或扩展能力。

### 已完成内容

- 新增 Node.js 22 ESM 后端入口、配置加载和服务生命周期管理。
- 新增 `schema_migrations`、`users`、`subjects` 开发数据库结构。
- 新增 User/Subject 仓储、业务校验和用户—主体归属查询。
- 新增服务信息、健康检查和 `/api/v1` 最小账号/主体路由。
- 新增统一 JSON 结果、请求 ID、请求体校验和大小限制。
- 新增开发数据库忽略规则，运行数据不进入 Git。

### 验证

- 17 个后端 JavaScript 文件通过 `node --check`。
- `pnpm test`：3 项测试通过，0 项失败。
- 使用内存 SQLite 真实启动后端，`GET /health` 返回服务和数据库 `ok`。
- 服务启动、健康检查、创建、读取、重启后持久化、重复邮箱和跨用户隔离均通过。
- 前端 `src/`、前端依赖和前端构建配置无修改。

### 下一步

- 等待平台后端 1 审查。
- 未收到下一阶段指令前，不开发事件总线、模型路由、权限、MCP、Tool、设备或 AI 私域。

## 2026-07-25｜后端 2：数据库与核心数据模型设计

### 当前状态

- 已完成平台后端核心逻辑数据模型和数据关系设计。
- 本阶段只说明未来数据如何归属、关联、版本化、导出和删除，不选择或连接真实数据库。
- 未修改前端代码，未开发 API，未接入模型或 continuity-engine。

### 已完成内容

- 新增 `docs/后端/数据库设计.md`，定义 11 个核心对象的作用、字段语义、数据分类、关系与约束。
- 新增 `docs/后端/数据关系图.md`，提供核心实体、数据归属、消息版本、事件连续性和删除影响图示。
- 明确用户数据、AI 主体数据和平台数据三类标记允许叠加，敏感等级独立判断。
- 明确主体级数据双重归属，以及主体状态、消息版本和事件的可追溯历史原则。
- 同步更新后端 ADR、开发日志、实施路线和后端规划索引。

### 下一步

- 等待第二阶段数据模型审查。
- 未收到新的明确任务前，不创建物理数据库结构、迁移脚本或 API。

## 2026-07-25｜平台后端第一阶段工程设计

### 当前状态

- 已确认开始 Vio Live 平台后端第一阶段。
- 已创建独立 `backend/` 工程设计目录，不修改前端 `src/`、前端依赖或构建配置。
- 当前只建立目录、职责、ADR、开发日志和实施路线，不创建可运行服务。
- 未连接真实数据库、API、模型、MCP、Tool、设备或第三方服务。

### 已完成内容

- 新增后端工程 README，说明平台职责、系统边界、目录结构、设计原则和下一阶段门槛。
- 新增后端 ADR 记录，固定独立目录、设计阶段边界、模块与集成分离、用户/主体双重归属以及 continuity-engine 平行边界。
- 新增后端开发日志与第一阶段实施路线。
- 新增 `core`、`modules`、`integrations`、`config`、`scripts` 和 `tests` 目录说明，不包含实现代码。
- 将第一阶段细化为范围冻结、技术决策、领域与数据设计、契约设计、安全测试设计和实现前审查。
- 同步调整后端规划文档的当前状态和维护规则。

### 验证

- 所有后端新增文件均位于 `backend/`，项目级记录位于 `docs/`。
- 前端 `src/` 无修改。
- 未新增后端依赖、环境变量、密钥、数据库迁移、API 路由、启动入口或模型调用。

### 下一步

- 等待第一阶段工程设计审查。
- 下一项应先形成运行时、数据库、认证、API、密钥、部署与测试 ADR；未收到新的明确任务前不初始化可运行后端。

## 2026-07-24｜平台后端规划文档体系

### 当前状态

- 已依据《Vio Live 产品与开发总规划 v2.4｜平台后端与前端版》建立平台后端规划文档体系。
- 本次只整理 `docs/后端/` 文档，不创建后端工程，不连接数据库、API、模型、MCP、设备或外部服务。
- 平台后端与连续性引擎保持平行系统边界；本目录只记录平台后端职责和双方接口原则。

### 已完成内容

- 新增后端文档索引，说明五层结构、当前状态、阅读顺序和维护规则。
- 新增总体架构、领域模型、账号认证、数据隔离、事件总线和对话版本规划。
- 新增每轮上下文装配、状态回写、连续性接口、模型路由、密钥和 Token 管理规划。
- 新增 MCP、Skill、插件、Tool、手机、家电、穿戴设备和统一适配层规划。
- 新增权限五档、高风险确认、隐私治理和 AI 私域后端规则。
- 新增月历、管账、体重/三围、本地记忆等生活模块的后端边界。
- 新增数据导出、备份、恢复、删除、迁移、API/事件契约和开发路线规划。
- 所有文档均标明当前为规划状态，不包含实现代码、数据库表、接口路径或超出总规划的新功能。

### 下一步

- 等待平台后端规划文档审查与范围确认。
- 未收到新的明确开发任务前，不创建后端、不连接数据库或 API。

## 2026-07-24｜生活管理体重/三围页面

### 当前状态

- 已完成「前端 13｜生活管理 - 体重/三围页面」。
- 页面全部使用固定模拟数据与 React 本地状态，不连接后端、数据库或 API。
- 页面明确标注“个人记录模式”和“非医疗”，不提供医疗诊断或健康结论。
- 体重页从工作台顶部“体重”入口进入，与“月历／管账”并列，不新增底部导航或其他页面。

### 已完成内容

- 新增当前状态卡，展示当前体重、目标体重、身高和胸围／腰围／臀围数据。
- 新增最近 7 次体重变化曲线、日期坐标与最近 4 次测量记录。
- 新增今日记录表单，支持模拟填写体重、腰围、胸围、臀围和备注。
- 新增记录保存后会即时更新页面内当前状态、趋势曲线和日期记录，刷新或离开后重置。
- 新增“分析最近变化”和“查看调整建议”两个纯 UI 入口，统一返回本地模拟反馈。
- 将页面拆分为页头、当前状态、趋势记录、添加表单、AI 操作和共用图标组件；模拟数据集中维护。
- 完成 320px、390px 手机宽度、工作台三入口、趋势曲线、记录表单、保存联动和 AI 入口检查；无横向溢出或控制台错误。

### 下一步

- 等待生活管理体重/三围页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-23｜生活管理管账页面

### 当前状态

- 已完成「前端 12｜生活管理 - 管账页面」。
- 管账页面全部使用固定模拟数据与本地 UI 反馈，不连接支付接口、银行、后端、数据库或 API。
- 管账从工作台顶部“管账”入口进入，与“月历”并列，不新增底部导航，不开发其他页面。

### 已完成内容

- 新增财务总览卡片，展示本月收入、本月支出、剩余预算、预算余量和消费趋势。
- 新增 8 条模拟账单，展示日期、时间、项目名称、金额与分类。
- 新增餐饮、购物、娱乐、交通、住房和其他六类支出统计及占比进度。
- 新增“分析消费习惯”和“查看建议”两个纯 UI 入口。
- 新增钱包连接、自动规则和安全设置预留入口，明确显示未连接、未启用与仅 UI 状态。
- 页面明确说明不提供充值、付款、转账或自动扣款操作。
- 将页面拆分为页头、财务总览、账单列表、分类统计、AI 操作、安全入口与共用图标组件，模拟数据集中维护。
- 完成 320px、390px 手机宽度、工作台双入口、长列表滚动、AI 与安全入口检查，无横向溢出，页面控制台无错误。

### 下一步

- 等待生活管理管账页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-23｜生活管理月历页面

### 当前状态

- 已完成「前端 11｜生活管理 - 月历页面」。
- 月历页面全部使用固定模拟数据与 React 页面状态，不连接后端、数据库、API 或 MCP。
- 月历从工作台顶部“生活月历”入口进入，不新增底部导航，不开发其他页面。

### 已完成内容

- 新增当前月份、上一个月/下一个月切换、星期标题与 7 列日期网格。
- 新增日期点击与详情联动，展示完整日期、当天事件、备注和添加记录按钮。
- 新增纪念日、生理期和亲密记录三类模拟事件，并使用金色、玫红和紫色视觉标记区分。
- 支持同一天展示多类事件标记与多条详情记录。
- 新增“与 AI 讨论今天”“让 AI 分析记录”和“添加提醒”三个纯 UI 入口。
- 新增记录面板，支持在选中日期添加纪念日、生理期或亲密记录，保存后立即更新本地网格与详情。
- 将页面拆分为页头、月历网格、日期详情、添加记录、AI 操作与共用图标组件，模拟数据集中维护。
- 完成 320px、390px 手机宽度、月份切换、空日期、三类新增记录与返回工作台检查，无横向溢出，页面控制台无错误。

### 下一步

- 等待生活管理月历页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-23｜我的页面

### 当前状态

- 已完成「前端 10｜我的页面」。
- 我的页面全部使用本地模拟数据与 React 页面状态，不连接后端、数据库或 API。
- 底部主导航六个页面均已有对应前端内容；本次未开发其他页面。

### 已完成内容

- 新增账号设置，展示用户信息、模拟登录方式和账号绑定入口。
- 新增智能体设置，展示名字、头像、性格、人设和强制要求。
- 新增外观设置，提供主题、背景、气泡样式与页面装饰的本地切换。
- 新增数据管理，提供数据导出、删除与备份入口，并明确所有操作不会处理真实数据。
- 新增安全栏，提供权限规则、默认询问方式和工具权限管理入口。
- 新增隐私设置，提供隐私政策、数据说明和 AI 私域设置入口。
- 将页面拆分为页头、通用分组、开关、六类设置组件、共用图标与集中模拟数据模块。
- 完成 320px、390px 手机宽度、长列表滚动和主要本地设置交互检查，无横向溢出，页面控制台无错误。

### 下一步

- 等待我的页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-23｜设备与家电页面

### 当前状态

- 已完成「前端 9｜设备与家电页面」。
- 设备中心全部使用本地模拟数据与 React 页面状态，不连接真实设备、第三方接口、后端、数据库或 API。
- 设备中心作为能力中心“手机 / 设备入口”的下级页面进入，不新增主导航标签，不开发其他页面。

### 已完成内容

- 新增设备中心总览，展示 4 个已连接模拟设备及连接数、运行数与需注意数量摘要。
- 设备卡展示名称、房间/位置、当前状态、关键参数、常用操作和详情入口。
- 新增设备详情页，展示基本信息、权限管理、自动化规则入口和访问日志入口。
- 新增高风险设备样例，提供“立即停止”和“撤销权限”的独立模拟操作区。
- 新增未连接设备区域，提供“添加适配器”和“连接服务”模拟入口，并明确不代表支持任何真实品牌。
- 将页面拆分为页头、设备卡、详情、未连接设备、共用图标与集中模拟数据模块。
- 完成设备列表、详情页、返回流程、权限开关、自动化/日志展开及危险操作的纯前端验证。
- 完成 320px、390px 手机宽度及长列表滚动检查，无横向溢出，页面控制台无错误。

### 下一步

- 等待设备与家电页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-23｜能力中心页面

### 当前状态

- 已完成「前端 8｜能力中心页面」。
- 能力中心全部使用本地模拟数据与 React 页面状态，不连接后端、数据库或 API。
- 工作台、对话、连续性、AI 私域与能力页面已实现；我的继续保持空白占位状态。

### 已完成内容

- 新增模型/API 分组，展示已连接模型、Base URL、模型名称、默认任务、新增服务和连接测试入口。
- 新增 MCP 列表、开启/关闭、权限状态和最近调用记录。
- 新增 Skill 列表、适用范围与启停按钮。
- 新增插件名称、版本、更新和卸载入口。
- 新增 Tool 名称、权限等级、使用记录和主动推荐开关。
- 新增手机/设备入口，展示已连接设备、权限状态和管理入口。
- 新增模型连接测试、能力启停、插件操作、主动推荐和设备管理的纯前端模拟反馈。
- 将页面拆分为页头、通用分组、开关、六类能力分组与共用图标组件，模拟数据集中维护。
- 完成 320px、390px 手机宽度及长列表滚动检查，无横向溢出，页面控制台无错误。

### 下一步

- 等待能力中心页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-23｜AI 私域页面

### 当前状态

- 已完成「前端 7｜AI 私域页面」。
- AI 私域页面全部使用本地模拟数据与预设回复，不连接后端、数据库、API，也不执行真实 AI 判断。
- 工作台、对话、连续性与 AI 私域页面已实现；能力和我的继续保持空白占位状态。

### 已完成内容

- 新增关于你的印象、最近的小日记、没说出口的话和重要纪念四项私域目录，默认全部锁定。
- 新增目录项目点击进入申请查看页面的本地页面状态切换。
- 新增查看内容锁定预览、申请理由输入框和发送申请按钮。
- 新增全部开放、部分开放、暂时拒绝和延后开放四种预设模拟回复。
- 完整开放与部分开放状态展示对应模拟内容；拒绝与延后状态继续保持内容锁定。
- 新增包含申请时间、开放范围和操作记录的模拟查看记录。
- 将页面拆分为页头、目录、申请面板、回复状态、查看记录和共用图标组件，模拟数据集中维护。
- 完成 320px、390px 手机宽度及目录/申请流程检查，无横向溢出，页面控制台无错误。

### 下一步

- 等待 AI 私域页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-23｜连续性页面

### 当前状态

- 已完成「前端 6｜连续性页面」。
- 连续性页面全部使用本地模拟数据，不连接后端、数据库、API 或真实记忆。
- 工作台、对话与连续性页面已实现；AI 私域、能力和我的继续保持空白占位状态。

### 已完成内容

- 新增“这个智能体是谁”卡片，展示稳定特征、表达方式、判断原则和自我认知。
- 新增“我们与他人的关系”卡片，展示关系定义、互动偏好、重要节点和关系变化。
- 新增“最近正在延续什么”卡片，展示当前关注、最近变化和未完成内容。
- 新增“时间与重要节点”卡片，展示最后互动、等待时间和生命周期事件。
- 新增“正在形成的想法”卡片，展示计划、倾向和待验证判断。
- 新增“当前状态”卡片，展示情绪、状态强度和变化原因。
- 将页面拆分为页头、通用连续性卡片、状态卡片和共用图标组件，模拟内容集中维护。
- 采用固定页头与独立内容滚动区，保持底部主导航始终可见。
- 完成 320px、390px 手机宽度检查，卡片、文字与导航无横向溢出，页面控制台无错误。

### 下一步

- 等待连续性页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-23｜对话页面

### 当前状态

- 已完成「前端 5｜对话页面」。
- 对话页全部使用本地模拟数据与原生表单状态，不连接后端、数据库、API 或真实 AI。
- 工作台与对话页面已实现；连续性、AI 私域、能力和我的继续保持空白占位状态。

### 已完成内容

- 新增智能体名字、会话名称与会话切换入口组成的顶部区域。
- 新增带用户和 Vio 两类消息气泡、时间显示的独立滚动消息区。
- 新增当前模型选择和精简、标准、完整、自定义四档上下文模式选择。
- 新增本次使用记忆、当前状态和使用工具三个轻量入口。
- 新增编辑消息、重新生成、删除和创建新分支四类消息操作界面。
- 新增文本输入、发送、图片、文件和语音入口组成的固定输入区。
- 将对话页拆分为顶部、工具控制、会话上下文、消息列表、消息气泡、输入区和共用图标组件，模拟内容集中维护。
- 完成 320px、390px 手机宽度检查，页面无横向溢出，消息区可独立滚动。

### 下一步

- 等待对话页面审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-22｜工作台第一屏

### 当前状态

- 已完成「前端 4｜工作台第一屏」。
- 工作台使用本地模拟数据展示，不连接后端、数据库或 API。
- 对话、连续性、AI 私域、能力和我的仍保持空白占位状态。

### 已完成内容

- 顶部仅显示智能体头像与名字，不显示在线状态或强制角色标签。
- 新增当前关注卡片。
- 新增今日重点、待办事项、提醒和设备状态摘要。
- 新增记忆、状态、工具、权限和未完成事项五类最近变化。
- 新增开始对话、查看连续性、打开设备和添加能力四个快捷入口。
- 新增底部通用输入框及纯前端未发送提示。
- 将工作台拆分为独立页面、展示组件、图标组件和集中模拟数据。
- 采用固定视口框架，让工作台内容独立滚动，并保持输入框和主导航固定可见。
- 完成 320px、390px 手机宽度检查，无控件越界或文字截断。

### 下一步

- 等待工作台第一屏审查与测试反馈。
- 在确认通过前，不开发其他页面。

## 2026-07-22｜主导航框架

### 当前状态

- 已完成「前端 3｜主导航框架」。
- 首次设置完成或跳过高级设置后进入主导航，默认显示工作台占位页。
- 当前仍未连接后端、数据库或 API，导航状态只存在于当前 React 页面状态中。

### 已完成内容

- 新增工作台、对话、连续性、AI 私域、能力和我的六个底部导航入口。
- 为六个入口建立统一的空白占位页面。
- 支持六个占位页之间的本地切换，并同步页面标题与选中状态。
- 延续 Vio Live 紫色玻璃质感与轻量渐变背景。
- 完成 320px、390px 手机宽度检查，六个导航标签无截断或越界。

### 下一步

- 等待主导航框架审查与测试反馈。
- 在确认通过前，不实现任何具体页面功能。

## 2026-07-22｜首次设置页面原型

### 当前状态

- 已完成「前端 2｜首次设置页面」原型。
- 登录页完成前端校验后进入首次设置页面；Google 登录按钮在原型阶段模拟进入该页面。
- 当前仍未连接后端、数据库或 API，所有设置只保存在 React 页面状态中。

### 已完成内容

- 新增智能体名称输入。
- 新增可跳过的头像选择。
- 新增工作伙伴、生活管家、陪伴和自定义四种基础定位。
- 新增云端、本地和混合三种记忆保存方式。
- 新增精简、标准、完整和自定义四种默认上下文模式。
- 新增“跳过高级设置”和“完成设置”按钮及对应的前端状态校验。
- 延续登录页的紫色玻璃卡片视觉，并完成 320px、390px 手机宽度检查。

### 下一步

- 等待首次设置页面审查与测试反馈。
- 在确认通过前，不开始开发其他页面。

## 2026-07-22｜前端项目初始化与登录页原型

### 当前状态

- Vio Live 前端项目初始化完成。
- 已完成「前端 1｜登录页」原型。
- 技术栈：React + Vite + TypeScript。
- 当前未连接后端、数据库或 API。

### 已完成内容

- 建立基础前端项目结构与开发、构建、预览命令。
- 完成手机端优先的登录页布局。
- 完成 Vio Live 文字 Logo、Google 登录按钮、邮箱验证码输入、三份协议勾选及登录/注册按钮。
- 完成本地表单校验与演示提示；所有交互均为纯前端行为。
- 通过生产构建及 320px、390px 手机宽度的布局检查。

### 下一步

- 开发「前端 2｜首次设置」页面。
- 在首次设置页面开始前，先确认单页范围与交互要求。
