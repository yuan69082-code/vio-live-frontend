export const API_PROVIDER_TYPES = Object.freeze([
  'openai',
  'claude',
  'glm',
  'custom',
]);

export const API_PROVIDER_STATUSES = Object.freeze([
  'enabled',
  'disabled',
]);

export const API_INTERFACE_FORMATS = Object.freeze([
  'openai_compatible',
  'anthropic_messages',
  'glm_compatible',
  'custom_http',
]);
